export interface DragDetail<To extends HTMLElement = HTMLElement, From extends HTMLElement = HTMLElement, Drag extends HTMLElement = HTMLElement> {
  toEl: To;
  fromEl: From;
  dragEl: Drag;
  newIndex: number;
  oldIndex: number;
}