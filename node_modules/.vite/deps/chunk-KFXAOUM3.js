// node_modules/@arcgis/core/core/mathUtils.js
var t = new Float32Array(1);
function n(t2) {
  --t2;
  for (let n2 = 1; n2 < 32; n2 <<= 1) t2 |= t2 >> n2;
  return t2 + 1;
}
function r(t2, n2, r2) {
  return Math.min(Math.max(t2, n2), r2);
}
function u(t2, n2) {
  return 0 === n2 ? 0 : Math.floor(t2 / n2) * n2;
}
function i(t2, n2) {
  return 0 === n2 ? 0 : Math.ceil(t2 / n2) * n2;
}
function o(t2) {
  return !(t2 & t2 - 1);
}
function a(t2) {
  return t2--, t2 |= t2 >> 1, t2 |= t2 >> 2, t2 |= t2 >> 4, t2 |= t2 >> 8, t2 |= t2 >> 16, ++t2;
}
function c(t2, n2, r2) {
  return t2 + (n2 - t2) * r2;
}
function M(t2, n2, r2, e, u2) {
  return c(e, u2, (t2 - n2) / (r2 - n2));
}
function h(t2) {
  return t2 * Math.PI / 180;
}
function b(t2) {
  return 180 * t2 / Math.PI;
}
function l(t2, n2 = 1e-6) {
  return (t2 < 0 ? -1 : 1) / Math.max(Math.abs(t2), n2);
}
function m(t2) {
  return Math.acos(r(t2, -1, 1));
}
function N(t2) {
  return Math.asin(r(t2, -1, 1));
}
function F(t2, n2, r2 = 1e-6) {
  if (t2 === n2) return true;
  if (!Number.isFinite(t2) || !Number.isFinite(n2)) return false;
  return (t2 > n2 ? t2 - n2 : n2 - t2) <= r2;
}
function g(t2, n2, r2 = 1e-6) {
  return !F(t2, n2, r2) && t2 > n2;
}
function E(t2, n2, r2 = 1e-6) {
  return !F(t2, n2, r2) && t2 < n2;
}
function p(t2, n2, r2 = 1e-6) {
  return F(t2, n2, r2) || t2 > n2;
}
var I = new DataView(new ArrayBuffer(Float64Array.BYTES_PER_ELEMENT));
function w(t2) {
  return I.setFloat64(0, t2), I.getBigInt64(0);
}
function y(t2) {
  return I.setBigInt64(0, t2), I.getFloat64(0);
}
var B = 1000000n;
var P = A(1);
function A(t2) {
  const n2 = w(t2 = Math.abs(t2)), r2 = y(n2 <= B ? B : n2 - B);
  return Math.abs(t2 - r2);
}
function S(t2, n2, r2 = P) {
  if (t2 === n2) return true;
  if (!Number.isFinite(t2) || !Number.isFinite(n2)) return false;
  if (null != r2) {
    if (A(Math.min(Math.abs(t2), Math.abs(n2))) < r2) return Math.abs(t2 - n2) <= r2;
  }
  const e = w(t2), u2 = w(n2);
  if (e < 0 != u2 < 0) return false;
  return !((e < u2 ? u2 - e : e - u2) > B);
}
function d(t2, n2, r2 = 1e-6) {
  if (t2 === n2) return true;
  if (!Number.isFinite(t2) || !Number.isFinite(n2)) return false;
  const e = Math.abs(t2 - n2), u2 = Math.abs(t2), i2 = Math.abs(n2);
  if (0 === t2 || 0 === n2 || u2 < 1e-12 && i2 < 1e-12) {
    if (e > 0.01 * r2) return false;
  } else if (e / (u2 + i2) > r2) return false;
  return true;
}
function L(t2) {
  return T(Math.max(-D, Math.min(t2, D)));
}
function T(n2) {
  return t[0] = n2, t[0];
}
function q(t2) {
  const n2 = t2[0] * t2[0] + t2[1] * t2[1] + t2[2] * t2[2], r2 = t2[3] * t2[3] + t2[4] * t2[4] + t2[5] * t2[5], e = t2[6] * t2[6] + t2[7] * t2[7] + t2[8] * t2[8];
  return !(F(n2, 1) && F(r2, 1) && F(e, 1));
}
function v(t2, n2) {
  return (t2 % n2 + n2) % n2;
}
var D = T(34028234663852886e22);
function G(t2, n2, r2) {
  if (void 0 === r2 || 0 === +r2) return Math[t2](n2);
  if (n2 = +n2, r2 = +r2, isNaN(n2) || "number" != typeof r2 || r2 % 1 != 0) return NaN;
  let e = n2.toString().split("e");
  return e = (n2 = Math[t2](+(e[0] + "e" + (e[1] ? +e[1] - r2 : -r2)))).toString().split("e"), +(e[0] + "e" + (e[1] ? +e[1] + r2 : r2));
}

export {
  n,
  r,
  u,
  i,
  o,
  a,
  c,
  M,
  h,
  b,
  l,
  m,
  N,
  F,
  g,
  E,
  p,
  S,
  d,
  L,
  q,
  v,
  D,
  G
};
//# sourceMappingURL=chunk-KFXAOUM3.js.map
