import {
  d as d4,
  l as l2
} from "./chunk-J5LKBXGW.js";
import {
  f,
  n as n4
} from "./chunk-JYGTXCLF.js";
import {
  a as a4
} from "./chunk-EH7RNYGW.js";
import {
  T,
  u2
} from "./chunk-UJNZQRXR.js";
import {
  d,
  t
} from "./chunk-VCZYABZ2.js";
import {
  O,
  g
} from "./chunk-4H57TSW2.js";
import {
  s as s3
} from "./chunk-WQUJR66M.js";
import {
  i,
  s as s2,
  t as t3
} from "./chunk-E4HM4TG2.js";
import {
  u as u3
} from "./chunk-NUVDH4IM.js";
import {
  d as d2
} from "./chunk-F6VYKY5P.js";
import {
  S
} from "./chunk-TYTH7ZJV.js";
import {
  a as a3,
  d2 as d3,
  e as e3,
  u as u4,
  w
} from "./chunk-BUQYRXN7.js";
import {
  e as e2
} from "./chunk-B6MIGAJF.js";
import {
  p
} from "./chunk-DI4PQ4KF.js";
import {
  e as e5
} from "./chunk-YVHZBCEM.js";
import {
  e as e6
} from "./chunk-ODKNVHZK.js";
import {
  e as e4
} from "./chunk-HYIKO77H.js";
import {
  r
} from "./chunk-URRK3ZOJ.js";
import {
  e
} from "./chunk-VKNLVFIM.js";
import {
  o as o2
} from "./chunk-727R2UNB.js";
import {
  s
} from "./chunk-BCEJLVA4.js";
import {
  n as n3,
  t as t2
} from "./chunk-NYA5CJQN.js";
import {
  n as n2
} from "./chunk-UP5TBSCS.js";
import {
  n
} from "./chunk-S2HMSOEZ.js";
import {
  a as a2
} from "./chunk-I5UAHQ4J.js";
import {
  o,
  u
} from "./chunk-OSHDQB3V.js";
import {
  l
} from "./chunk-QDSSZK56.js";
import {
  a3 as a,
  c
} from "./chunk-Q7QWHSGF.js";
import {
  __decorate
} from "./chunk-SS5CLIUY.js";

// node_modules/@arcgis/core/views/3d/webgl-engine/effects/focusArea/FocusAreaColorTechnique.js
var a5 = class extends d {
  constructor() {
    super(...arguments), this.shader = new t(n4, () => import("./FocusAreaColor.glsl-R3LONBO2.js")), this.ignoreUnused = true;
  }
  initializePipeline() {
    return O({ colorWrite: g });
  }
};
a5 = __decorate([c("esri.views.3d.webgl-engine.effects.focusArea.FocusAreaColorTechnique")], a5);

// node_modules/@arcgis/core/views/3d/webgl-engine/effects/focusArea/FocusAreaColorNode.js
var d5 = class extends u2 {
  constructor(e7) {
    super({ ...e7, view: e7.focusAreasView.view }), this.consumes = { required: [T.FOCUSAREA_COLOR, T.FOCUSAREA] }, this.produces = T.FOCUSAREA_COLOR, this._fadeDirection = 0, this._passParameters = new f();
  }
  fadeOut(e7) {
    this.removeAllHandles(), this._startTime = null, this._fadeDirection = 1, this.addHandles(l(() => this._passParameters.fadeFactor.value, (t4) => {
      0 === t4 && (this.removeAllHandles(), e7());
    })), this.requestRender(2);
  }
  render(e7) {
    var _a, _b;
    const t4 = e7.find(({ name: e8 }) => e8 === this.produces), s4 = this.techniques.getCompiled(a5);
    if (!s4) return this.requestRender(1), t4;
    const r2 = this.focusAreasView.style, o3 = this.bindParameters, a6 = o3.camera, d6 = a6.fullViewport[2], m = a6.fullViewport[3];
    this._startTime ?? (this._startTime = (_a = this.view.stage) == null ? void 0 : _a.renderer.renderContext.time);
    const u5 = this.view.qualitySettings.fadeDuration, f2 = u5 > 0 ? Math.min(u5, ((_b = this.view.stage) == null ? void 0 : _b.renderer.renderContext.time) - this._startTime) / u5 : 1, l3 = e7.find(({ name: e8 }) => e8 === T.FOCUSAREA), p2 = this.fboCache.acquire(d6, m, this.produces), A = this.renderingContext;
    return A.bindFramebuffer(p2.fbo), this._passParameters.color = t4.getTexture(), this._passParameters.focusArea = l3.getTexture(), this._passParameters.effect = h[r2], this._passParameters.fadeFactor.value = 0 === this._fadeDirection ? f2 : 1 - f2, A.bindTechnique(s4, o3, this._passParameters), A.screen.draw(), p2.attachDepth(t4.getAttachment(n2)), f2 < 1 && this.requestRender(2), p2;
  }
};
__decorate([a()], d5.prototype, "consumes", void 0), __decorate([a()], d5.prototype, "produces", void 0), __decorate([a({ constructOnly: true })], d5.prototype, "focusAreasView", void 0), d5 = __decorate([c("esri.views.3d.webgl-engine.effects.focusArea.FocusAreaColorNode")], d5);
var h = { bright: 0, dark: 1 };

// node_modules/@arcgis/core/chunks/HUDMaterial.glsl.js
var B = (e7) => e7 ? h[e7] : 0;
function $(e7) {
  const r2 = new s();
  r2.include(d4, e7), r2.vertex.include(S, e7);
  const { output: A, hasOcclusionTexture: $2, signedDistanceFieldEnabled: T3, pixelSnappingEnabled: _2, hasEmission: q, hasScreenSizePerspective: R, debugDrawLabelBorder: k, hasVVSize: H, hasVVColor: I, hasRotation: E, occludedFragmentFade: G, sampleSignedDistanceFieldTexelCenter: N, hasVertexColor: J, hasVertexSize: K, hasVertexRotation: Q, hasVertexUVi: W } = e7;
  r2.include(s2), r2.include(u3, e7), r2.include(d2, e7), r2.include(u4, e7);
  const { vertex: X, fragment: Y } = r2;
  Y.include(e3), Y.code.add(t2`
    vec4 applyFocusAreaStyle(vec4 color, int style) {
      const float factor = 0.46;
      const float factorBright = 0.32;

      if (style == ${t2.int(0)}) {
        float luma = (color.r + color.g + color.b) / 3.0;
        float bright = luma * (1.0 - 0.6 * factorBright) + 0.6 * factorBright * color.a;
        float brightScaled = bright * factorBright;
        return vec4(brightScaled, brightScaled, brightScaled, color.a * factorBright);
      }

      float darkScaled = factor * factor;
      return vec4(color.rgb * darkScaled, color.a * factor);
    }
  `), r2.varyings.add("vcolor", "vec4"), r2.varyings.add("vtc", "vec2"), r2.varyings.add("vsize", "vec2");
  const Z = 10 === A;
  X.uniforms.add(new e4("viewport", (e8) => e8.camera.fullViewport), new e5("screenOffset", (e8, r3) => u(M, 2 * e8.screenOffset[0] * r3.camera.pixelRatio, 2 * e8.screenOffset[1] * r3.camera.pixelRatio)), new e5("anchorPosition", (e8) => V(e8)), new e2("materialColor", ({ color: e8 }) => e8), new r("materialRotation", (e8) => e8.rotation), new e5("materialSize", (e8) => e8.size), new e("tex", (e8) => e8.texture)), w(X), T3 && (X.uniforms.add(new e2("outlineColor", (e8) => e8.outlineColor)), Y.uniforms.add(new e2("outlineColor", (e8) => L(e8) ? e8.outlineColor : a2), new r("outlineSize", (e8) => L(e8) ? e8.outlineSize : 0))), _2 && X.include(l2), R && (t3(X), i(X)), k && r2.varyings.add("debugBorderCoords", "vec4"), r2.attributes.add("uv0", "vec2"), W && r2.attributes.add("uvi", "vec4"), J && r2.attributes.add("color", "vec4"), K && r2.attributes.add("size", "vec2"), Q && r2.attributes.add("rotation", "float"), (H || I) && r2.attributes.add("featureAttribute", "vec4"), X.main.add(t2`
    ProjectHUDAux projectAux;
    vec4 posProj = projectPositionHUD(projectAux);
    forwardObjectAndLayerIdColor();

    if (rejectBySlice(projectAux.posModel)) {
      gl_Position = ${s3};
      return;
    }

    vec2 vertexSize = materialSize${n3(K, " * size")};
    vec2 inputSize;
    ${n3(R, t2`
        inputSize = screenSizePerspectiveScaleVec2(vertexSize, projectAux.absCosAngle, projectAux.distanceToCamera, screenSizePerspective);
        vec2 screenOffsetScaled = screenSizePerspectiveScaleVec2(screenOffset, projectAux.absCosAngle, projectAux.distanceToCamera, screenSizePerspectiveAlignment);`, t2`
        inputSize = vertexSize;
        vec2 screenOffsetScaled = screenOffset;`)}
    ${n3(H, t2`inputSize *= vvScale(featureAttribute).xx;`)}

    vec2 combinedSize = inputSize * pixelRatio;
    vec4 quadOffset = vec4(0.0);
  `);
  const ee = t2`
  ${n3(W, t2`
    vec2 texSize = vec2(textureSize(tex, 0));
    vec2 uv = mix(uvi.xy, uvi.zw, bvec2(uv0)) / texSize;
    `, t2`
    vec2 uv = mix(vec2(0.), vec2(1.), bvec2(uv0));
    `)}

    quadOffset.xy = (uv0 - anchorPosition) * 2.0 * combinedSize;

    ${n3(E, t2`
        float angle = radians(materialRotation${n3(Q, " + rotation")});
        float cosAngle = cos(angle);
        float sinAngle = sin(angle);
        mat2 rotate = mat2(cosAngle, -sinAngle, sinAngle,  cosAngle);

        quadOffset.xy = rotate * quadOffset.xy;
      `)}

    quadOffset.xy = (quadOffset.xy + screenOffsetScaled) / viewport.zw * posProj.w;
  `, oe = _2 ? T3 ? t2`posProj = alignToPixelOrigin(posProj, viewport.zw) + quadOffset;` : t2`posProj += quadOffset;
if (inputSize.x == vertexSize.x) {
posProj = alignToPixelOrigin(posProj, viewport.zw);
}` : t2`posProj += quadOffset;`;
  X.include(a3), X.main.add(t2`
    ${ee}
    ${I ? "vcolor = interpolateVVColor(featureAttribute.y) * materialColor;" : J ? "vcolor = color * materialColor;" : "vcolor = materialColor;"}

    ${n3(11 === A, t2`vcolor.a = 1.0;`)}

    bool alphaDiscard = vcolor.a < alphaCutoff;
    ${n3(T3, "alphaDiscard = alphaDiscard && outlineColor.a < alphaCutoff;")}
    if (alphaDiscard) {
      // "early discard" if both symbol color (= fill) and outline color (if applicable) are transparent
      gl_Position = vec4(1e38, 1e38, 1e38, 1.0);
      return;
    } else {
      ${oe}
      gl_Position = posProj;
    }

    vtc = uv;

    ${n3(k, t2`debugBorderCoords = vec4(uv0, 1.5 / combinedSize);`)}
    vsize = inputSize;
  `);
  const re = p(A) && e7.hasFocusAreaStyle && !e7.draped;
  switch (Y.uniforms.add(new e("tex", (e8) => e8.texture)), re && Y.uniforms.add(new o2("focusAreaStyle", (e8) => B(e8.focusAreaStyle))), G && !Z && (Y.include(a4), Y.uniforms.add(new e6("depthMap", (e8) => e8.mainDepth), new r("occludedOpacity", (e8) => {
    var _a;
    return ((_a = e8.occludedFragmentOpacity) == null ? void 0 : _a.value) ?? 1;
  }))), $2 && Y.uniforms.add(new e6("texOcclusion", (e8) => {
    var _a;
    return (_a = e8.hudOcclusion) == null ? void 0 : _a.attachment;
  })), k ? Y.main.add("\n        float isBorder = float(any(lessThan(debugBorderCoords.xy, debugBorderCoords.zw)) || any(greaterThan(debugBorderCoords.xy, 1.0 - debugBorderCoords.zw)));\n        // don't discard fragments on debug border\n        float textureAlphaCutoff = isBorder > 0.0 ? 0.0 : alphaCutoff;\n      ") : Y.main.add("float textureAlphaCutoff = alphaCutoff;"), Y.main.add("vec2 samplePos = vtc;"), N && Y.main.add(t2`float txSize = float(textureSize(tex, 0).x);
float texelSize = 1.0 / txSize;
vec2 scaleFactor = (vsize - txSize) * texelSize;
samplePos += (vec2(1.0, -1.0) * texelSize) * scaleFactor;`), T3 ? Y.main.add(t2`
      vec4 fillPixelColor = vcolor;

      // Get distance in output units (i.e. pixels)

      float sdf = texture(tex, samplePos).r;
      float pixelDistance = sdf * vsize.x;

      // Create smooth transition from the icon into its outline
      float fillAlphaFactor = clamp(0.5 - pixelDistance, 0.0, 1.0);
      fillPixelColor.a *= fillAlphaFactor;

      if (outlineSize > 0.25) {
        vec4 outlinePixelColor = outlineColor;
        float clampedOutlineSize = min(outlineSize, 0.5*vsize.x);

        // Create smooth transition around outline
        float outlineAlphaFactor = clamp(0.5 - (abs(pixelDistance) - 0.5*clampedOutlineSize), 0.0, 1.0);
        outlinePixelColor.a *= outlineAlphaFactor;

        if (
          outlineAlphaFactor + fillAlphaFactor < textureAlphaCutoff ||
          fillPixelColor.a + outlinePixelColor.a < alphaCutoff
        ) {
          discard;
        }

        // perform un-premultiplied over operator (see https://en.wikipedia.org/wiki/Alpha_compositing#Description)
        float compositeAlpha = outlinePixelColor.a + fillPixelColor.a * (1.0 - outlinePixelColor.a);
        vec3 compositeColor = vec3(outlinePixelColor) * outlinePixelColor.a +
                              vec3(fillPixelColor) * fillPixelColor.a * (1.0 - outlinePixelColor.a);

        ${n3(!Z, t2`fragColor = vec4(compositeColor, compositeAlpha);`)}
      } else {
        if (fillAlphaFactor < textureAlphaCutoff) {
          discard;
        }

        ${n3(!Z, t2`fragColor = premultiplyAlpha(fillPixelColor);`)}
      }

      // visualize SDF:
      // fragColor = vec4(clamp(-pixelDistance/vsize.x*2.0, 0.0, 1.0), clamp(pixelDistance/vsize.x*2.0, 0.0, 1.0), 0.0, 1.0);
      `) : Y.main.add(t2`
        vec4 texColor = texture(tex, samplePos, -0.5);
        if (texColor.a < textureAlphaCutoff) {
          discard;
        }
        ${n3(!Z, t2`fragColor = texColor * premultiplyAlpha(vcolor);`)}
      `), G && !Z && Y.main.add(t2`
        float zSample = -linearizeDepth(texelFetch(depthMap, ivec2(gl_FragCoord.xy), 0).x);
        float zFragment = -linearizeDepth(gl_FragCoord.z);
        if (zSample < ${t2.float(1 - U)} * zFragment) {
          fragColor *= occludedOpacity;
        }
      `), $2 && Y.main.add("fragColor *= texelFetch(texOcclusion, ivec2(gl_FragCoord.xy), 0).r;"), !Z && k && Y.main.add("fragColor = mix(fragColor, vec4(1.0, 0.0, 1.0, 1.0), isBorder * 0.5);"), 2 === A && Y.main.add(t2`if (fragColor.a < alphaCutoff) {
discard;
}`), re && Y.main.add(t2`fragColor = applyFocusAreaStyle(fragColor, focusAreaStyle);`), p(A) && q && Y.main.add("fragEmission = vec4(0.0);"), A) {
    case 1:
      Y.main.add("\n        fragColor = vec4(fragColor.rgb * floatBlendOutputScale, fragColor.a);\n        fragAlpha = fragColor.a * floatBlendOutputScale;\n      ");
      break;
    case 2:
      Y.main.add("fragColor.rgb /= fragColor.a;");
      break;
    case 11:
      Y.main.add("outputObjectAndLayerIdColor();");
      break;
    case 10:
      r2.include(d3, e7), Y.main.add("outputHighlight(false);");
  }
  return r2;
}
function L(e7) {
  return e7.outlineColor[3] > 0 && e7.outlineSize > 0;
}
function V(o3) {
  return o3.textureIsSignedDistanceField ? T2(o3.anchorPosition, o3.distanceFieldBoundingBox, M) : o(M, o3.anchorPosition), M;
}
var M = n();
function T2(e7, r2, i2) {
  u(i2, e7[0] * (r2[2] - r2[0]) + r2[0], e7[1] * (r2[3] - r2[1]) + r2[1]);
}
var U = 0.08;
var _ = Object.freeze(Object.defineProperty({ __proto__: null, anchorPosition: V, build: $ }, Symbol.toStringTag, { value: "Module" }));

export {
  $,
  V,
  _
};
//# sourceMappingURL=chunk-UIB6F7AO.js.map
