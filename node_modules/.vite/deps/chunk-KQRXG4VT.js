import {
  c,
  e2,
  f,
  g2 as g,
  p
} from "./chunk-ZQF3TCDV.js";
import {
  o
} from "./chunk-XOZ3Q4UG.js";
import {
  s
} from "./chunk-RXTXR6K7.js";
import {
  e as e3
} from "./chunk-QL7J445R.js";
import {
  t as t2
} from "./chunk-NTM3SHYX.js";
import {
  e as e4
} from "./chunk-ODKNVHZK.js";
import {
  r
} from "./chunk-7BJKTGZL.js";
import {
  l
} from "./chunk-3ZZVJAOX.js";
import {
  i as i2,
  n as n2,
  t
} from "./chunk-NYA5CJQN.js";
import {
  i
} from "./chunk-IUMZVY6E.js";
import {
  e
} from "./chunk-FPD37LBO.js";
import {
  u
} from "./chunk-FTY3ENIR.js";
import {
  a,
  n
} from "./chunk-WT4NB7ZZ.js";

// node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderModules/Matrix4DrawUniform.js
var t3 = class extends i2 {
  constructor(r2, t5, o4) {
    super(r2, "mat4", 2, (s2, e8, m) => s2.setUniformMatrix4fv(r2, t5(e8, m), o4));
  }
};

// node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderLibrary/util/View.glsl.js
function d(r2, i3) {
  i3.instancedDoublePrecision ? r2.constants.add("cameraPosition", "vec3", a) : r2.uniforms.add(new e2("cameraPosition", (r3, i4) => u(v, i4.camera.viewInverseTransposeMatrix[3] - r3.origin[0], i4.camera.viewInverseTransposeMatrix[7] - r3.origin[1], i4.camera.viewInverseTransposeMatrix[11] - r3.origin[2])));
}
function f2(i3, o4) {
  if (!o4.instancedDoublePrecision) return void i3.uniforms.add(new t2("proj", (r2) => r2.camera.projectionMatrix), new t3("view", (i4, e8) => i(l2, e8.camera.viewMatrix, i4.origin)), new e2("localOrigin", (r2) => r2.origin));
  const a3 = ({ camera: r2 }) => u(v, r2.viewInverseTransposeMatrix[3], r2.viewInverseTransposeMatrix[7], r2.viewInverseTransposeMatrix[11]);
  i3.uniforms.add(new t2("proj", (r2) => r2.camera.projectionMatrix), new t2("view", (i4) => i(l2, i4.camera.viewMatrix, a3(i4))), new e3("localOrigin", (r2) => a3(r2)));
}
var l2 = e();
var v = n();
function p2(r2) {
  r2.uniforms.add(new t2("viewNormal", (r3) => r3.camera.viewInverseTransposeMatrix));
}
function w(r2) {
  r2.uniforms.add(new r("pixelRatio", (r3) => r3.camera.pixelRatio / r3.overlayStretch));
}

// node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderLibrary/util/ColorConversion.glsl.js
function e5(e8) {
  e8.code.add(t`vec4 premultiplyAlpha(vec4 v) {
return vec4(v.rgb * v.a, v.a);
}
vec3 rgb2hsv(vec3 c) {
vec4 K = vec4(0.0, -1.0 / 3.0, 2.0 / 3.0, -1.0);
vec4 p = c.g < c.b ? vec4(c.bg, K.wz) : vec4(c.gb, K.xy);
vec4 q = c.r < p.x ? vec4(p.xyw, c.r) : vec4(c.r, p.yzx);
float d = q.x - min(q.w, q.y);
float e = 1.0e-10;
return vec3(abs(q.z + (q.w - q.y) / (6.0 * d + e)), min(d / (q.x + e), 1.0), q.x);
}
vec3 hsv2rgb(vec3 c) {
vec4 K = vec4(1.0, 2.0 / 3.0, 1.0 / 3.0, 3.0);
vec3 p = abs(fract(c.xxx + K.xyz) * 6.0 - K.www);
return c.z * mix(K.xxx, clamp(p - K.xxx, 0.0, 1.0), c.y);
}
float rgb2v(vec3 c) {
return max(c.x, max(c.y, c.z));
}`);
}

// node_modules/@arcgis/core/webscene/support/AlphaCutoff.js
var o2 = 1 / 255.5;
var t4 = 1 - o2;

// node_modules/@arcgis/core/views/3d/webgl-engine/shaders/alphaCutoff.glsl.js
function a2(a3) {
  a3.constants.add("alphaCutoff", "float", o2), a3.constants.add("opacityCutoff", "float", t4);
}

// node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderModules/Integer2BindUniform.js
var e6 = class extends i2 {
  constructor(r2, e8) {
    super(r2, "ivec2", 0, (o4, s2) => o4.setUniform2iv(r2, e8(s2)));
  }
};

// node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderModules/IntegerBindUniform.js
var o3 = class extends i2 {
  constructor(r2, o4) {
    super(r2, "int", 0, (s2, t5) => s2.setUniform1i(r2, o4(t5)));
  }
};

// node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderModules/Texture2DUintBindUniform.js
var e7 = class extends i2 {
  constructor(r2, e8) {
    super(r2, "usampler2D", 0, (s2, o4) => s2.bindTexture(r2, e8(o4)));
  }
};

// node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderLibrary/output/OutputHighlight.glsl.js
function d2(d3, o4) {
  const { fragment: u3 } = d3, { output: r2, draped: c2, hasHighlightMixTexture: n3 } = o4;
  10 === r2 ? (u3.uniforms.add(new o3("highlightLevel", (i3) => i3.highlightLevel ?? 0), new e6("highlightMixOrigin", (i3) => i3.highlightMixOrigin)), d3.outputs.add("fragHighlight", "uvec2", 0), d3.include(l), n3 ? u3.uniforms.add(new e7("highlightMixTexture", (i3) => i3.highlightMixTexture)).code.add(t`uvec2 getAccumulatedHighlight() {
return texelFetch(highlightMixTexture, ivec2(gl_FragCoord.xy) - highlightMixOrigin, 0).rg;
}
void outputHighlight(bool occluded) {
if (highlightLevel == 0) {
uint bits = occluded ? 3u : 1u;
fragHighlight = uvec2(bits, 0);
} else {
int ll = (highlightLevel & 3) << 1;
int li = (highlightLevel >> 2) & 3;
uint bits;
if (occluded) {
bits = 3u << ll;
} else {
bits = 1u << ll;
}
uvec2 combinedHighlight = getAccumulatedHighlight();
combinedHighlight[li] |= bits;
fragHighlight = combinedHighlight;
}
}`) : u3.code.add(t`void outputHighlight(bool occluded) {
uint bits = occluded ? 3u : 1u;
fragHighlight = uvec2(bits, 0);
}`), c2 ? u3.code.add(t`bool isHighlightOccluded() {
return false;
}`) : u3.uniforms.add(new e4("depthTexture", (i3) => i3.mainDepth)).code.add(t`bool isHighlightOccluded() {
float sceneDepth = texelFetch(depthTexture, ivec2(gl_FragCoord.xy), 0).x;
return gl_FragCoord.z > sceneDepth + 5e-7;
}`), u3.code.add(t`void calculateOcclusionAndOutputHighlight() {
outputHighlight(isHighlightOccluded());
}`)) : u3.code.add(t`void calculateOcclusionAndOutputHighlight() {}`);
}

// node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderLibrary/util/EmissionDimming.glsl.js
function l3(l4, c2) {
  l4.include(e5), l4.include(s, c2), l4.code.add("\n    vec3 emissionDimming(in vec3 srcColor, float srcAlpha) {\n      vec3 color = min(srcColor + vec3(1.0 - srcAlpha), 1.0);\n      float m = max(color.r, max(color.g, color.b));\n      return m > 0.0 ? color / m : vec3(1.0);\n    }\n  ");
}

// node_modules/@arcgis/core/views/3d/webgl-engine/shaders/OutputColorHighlightOLID.glsl.js
function u2(u3, p3) {
  u3.include(d2, p3), u3.include(g, p3);
  const { fragment: d3, outputs: c2 } = u3, { output: h, hasEmission: C, discardInvisibleFragments: b, oitPremultipliedAlpha: v2, snowCover: A, useFloatBlend: E, emissionDimmingPass: y } = p3, $ = 11 === h, j = c(h), O = f(h), L = p(h) && !j;
  let S = 0;
  (L || j) && c2.add("fragColor", "vec4", S++), j && c2.add("fragAlpha", "float", S++), C && c2.add("fragEmission", "vec4", S++), d3.include(e5), d3.include(a2), d3.include(s, p3), d3.include(o, p3), y && d3.include(l3, p3), d3.code.add(t`
    void outputColorHighlightOLID(vec4 finalColor, vec3 emissiveSymbolColor ${n2(A, ", float snow")}) {
      ${n2($, "finalColor.a = 1.0;")}
      ${n2(b, "if (finalColor.a < alphaCutoff) { discard; }")}

      ${n2(j, `float noise = ditherNoise(finalColor);
         fragColor = ${n2(v2, "finalColor", "premultiplyAlpha(finalColor)")};
         fragColor = vec4(fragColor.rgb * floatBlendOutputScale + noise, fragColor.a);
         float scaledAlpha = finalColor.a * floatBlendOutputScale;
         fragAlpha = scaledAlpha + noise;
         ${n2(!E, "fragAlpha = fragAlpha < alphaCutoff ? scaledAlpha : fragAlpha;")}`)}
      ${n2(O && v2 && b, "finalColor.rgb /= finalColor.a;")}
      ${n2(L, "fragColor = finalColor;")}
      ${n2(C, y ? "fragEmission = vec4(emissionDimming(premultiplyAlpha(finalColor).rgb, finalColor.a), 0.0);" : `fragEmission = ${n2(A, "mix(finalColor.a * getEmissions(emissiveSymbolColor), vec4(0.0), snow);", "finalColor.a * getEmissions(emissiveSymbolColor);")}
            float emissionNoise = ditherNoise(fragEmission);
            fragEmission.rgb = fragEmission.rgb * floatBlendOutputScale + emissionNoise;
            fragEmission.a = finalColor.a;
            fragEmission = premultiplyAlpha(fragEmission);
            `)}
      calculateOcclusionAndOutputHighlight();
      ${n2($, "outputObjectAndLayerIdColor();")}
    }
  `);
}

export {
  t3 as t,
  d,
  f2 as f,
  p2 as p,
  w,
  e5 as e,
  o2 as o,
  a2 as a,
  o3 as o2,
  d2,
  u2 as u
};
//# sourceMappingURL=chunk-KQRXG4VT.js.map
