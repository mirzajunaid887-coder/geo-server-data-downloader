import {
  a,
  e as e2,
  f,
  u
} from "./chunk-BUQYRXN7.js";
import {
  e
} from "./chunk-B6MIGAJF.js";
import {
  r
} from "./chunk-URRK3ZOJ.js";
import {
  s
} from "./chunk-BCEJLVA4.js";
import {
  n,
  t
} from "./chunk-NYA5CJQN.js";

// node_modules/@arcgis/core/chunks/SlicePlaneMaterial.glsl.js
function t2(t3) {
  const n3 = new s(), { vertex: u2, fragment: p, attributes: c, varyings: v } = n3;
  return f(u2, t3), n3.include(u, t3), p.include(a), p.include(e2), c.add("position", "vec3"), c.add("uv0", "vec2"), v.add("vUV", "vec2"), u2.main.add(t`vUV = uv0;
gl_Position = proj * view * vec4(position, 1.0);`), p.uniforms.add(new e("backgroundColor", (e3) => e3.backgroundColor), new e("gridColor", (e3) => e3.gridColor), new r("gridWidth", (e3) => e3.gridWidth)).main.add(t`
    const float LINE_WIDTH = 1.0;

    vec2 uvScaled = vUV * gridWidth;
    vec2 gridUV = (fract(uvScaled + 0.5) - 0.5) / (LINE_WIDTH * fwidth(uvScaled));
    vec2 grid = (1.0 - step(0.5, gridUV)) * step(-0.5, gridUV);

    // mask aliasing along edges
    grid.x *= step(0.5, uvScaled.x) * step(uvScaled.x, gridWidth - 0.5);
    grid.y *= step(0.5, uvScaled.y) * step(uvScaled.y, gridWidth - 0.5);

    float gridFade = max(grid.x, grid.y);
    float gridAlpha = gridColor.a * gridFade;

    // premultiply alpha in output
    vec4 finalColor =
      premultiplyAlpha(backgroundColor) * (1.0 - gridAlpha) +
      premultiplyAlpha(vec4(gridColor.rgb, gridAlpha));
    ${n(t3.emissionDimmingPass, "if (finalColor.a > alphaCutoff) { finalColor.rgb /= finalColor.a; }")}
    outputColorHighlightOLID(finalColor, finalColor.rgb);`), n3;
}
var n2 = Object.freeze(Object.defineProperty({ __proto__: null, build: t2 }, Symbol.toStringTag, { value: "Module" }));

export {
  t2 as t,
  n2 as n
};
//# sourceMappingURL=chunk-VJKXXJEH.js.map
