import {
  d as d2,
  f as f2,
  h,
  i,
  m as m3,
  x
} from "./chunk-OLMW2GYA.js";
import {
  C as C2,
  n as n3,
  r as r2,
  s as s2,
  y
} from "./chunk-BSEEEXSD.js";
import {
  n as n4,
  t
} from "./chunk-QBRKNMMU.js";
import {
  a as a2
} from "./chunk-PDGSFAUL.js";
import {
  D,
  I,
  N,
  Z,
  ce,
  j as j2,
  le,
  ne,
  oe,
  te
} from "./chunk-WJ3ES3UM.js";
import {
  R,
  p
} from "./chunk-Z7XDJRFF.js";
import {
  S as S2,
  j,
  l2 as l3,
  s,
  u,
  z as z3
} from "./chunk-YAIHEU4N.js";
import {
  g
} from "./chunk-F4XFGC2I.js";
import {
  l
} from "./chunk-NUA3KVZI.js";
import {
  l as l2,
  z as z2
} from "./chunk-62CFPIEE.js";
import {
  d2 as d
} from "./chunk-3PC2GXGN.js";
import {
  z2 as z
} from "./chunk-DU2DNIJI.js";
import {
  _
} from "./chunk-NA7PETQC.js";
import {
  S
} from "./chunk-7A5FEBIH.js";
import {
  n as n2
} from "./chunk-TX64TBOR.js";
import {
  a3 as a,
  c,
  m
} from "./chunk-Q7QWHSGF.js";
import {
  __decorate
} from "./chunk-SS5CLIUY.js";
import {
  C,
  f2 as f
} from "./chunk-BRD4RK5L.js";
import {
  L,
  m as m2
} from "./chunk-M3TUHHNI.js";
import {
  n2 as n,
  r3 as r
} from "./chunk-NXFNHCI4.js";

// node_modules/@arcgis/core/layers/raster/datasets/BaseRaster.js
var K = 8;
var Y = 256;
var Z2 = 0;
var ee = class extends n2 {
  constructor() {
    super(...arguments), this._tileFetchQueue = new l({ concurrency: 32, process: (e2, t3) => this.fetchRawTile(e2.pyramidLevel, e2.row, e2.col, { ...e2.options, signal: t3 }) }), this.datasetName = null, this.datasetFormat = null, this.hasUniqueSourceStorageInfo = true, this.rasterInfo = null, this.ioConfig = { sampling: "closest" }, this.rawBlockRegistryIds = [], this.refresh = L(async (e2) => {
      await this._openPromise;
      const t3 = await this._refresh(e2);
      return t3.dataChanged && f2(this.rasterId, this.rawBlockRegistryIds), t3;
    });
  }
  normalizeCtorArgs(e2) {
    return (e2 == null ? void 0 : e2.ioConfig) && (e2 = { ...e2, ioConfig: { resolution: null, bandIds: null, sampling: "closest", tileInfo: z2.create(), ...e2.ioConfig } }), e2;
  }
  get _isGlobalWrappableSource() {
    const { rasterInfo: e2 } = this, t3 = Z(e2.spatialReference);
    return null != t3 && e2.extent.width >= t3 / 2;
  }
  get _hasNoneOrGCSShiftTransform() {
    const { transform: e2 } = this.rasterInfo;
    return null == e2 || "gcs-shift" === e2.type;
  }
  set rasterJobHandler(e2) {
    var _a, _b;
    this._set("rasterJobHandler", e2), t(this) && ((_b = (_a = this.primaryRasters) == null ? void 0 : _a.rasters) == null ? void 0 : _b.forEach((t3) => t3.rasterJobHandler = e2));
  }
  get rasterId() {
    return this.url || "rasterId-" + Z2++;
  }
  set url(e2) {
    this._set("url", C(e2, n.getLogger(this)));
  }
  async open(e2) {
    return this._openPromise ?? (this._openPromise = N().then(() => this._open(e2))), this._openPromise;
  }
  async fetchTile(e2, t3, r4, o2 = {}) {
    const n6 = o2.tileInfo || this.rasterInfo.storageInfo.tileInfo, s3 = this.getTileExtentFromTileInfo(e2, t3, r4, n6);
    if (!s3) throw new r("imagery-tile:out-of-bounds", "Level for fetch tile out of range");
    return o2 = { noClip: true, ...o2 }, this.fetchPixels(s3, n6.size[0], n6.size[1], o2);
  }
  async identify(e2, t3 = {}) {
    var _a;
    e2 = m(_, e2).clone().normalize();
    const { multidimensionalDefinition: i3, timeExtent: r4 } = t3, { rasterInfo: o2 } = this, { hasMultidimensionalTranspose: n6, multidimensionalInfo: s3 } = o2;
    let { transposedVariableName: l5 } = t3;
    const a3 = null != s3 && n6 && (null != r4 || y(i3));
    if (a3 && !l5) {
      l5 = null != i3 && i3.length > 0 ? i3[0].variableName ?? void 0 : s3.variables[0].name, t3 = { ...t3, transposedVariableName: l5 };
    }
    t3 = this._getRequestOptionsWithSliceId(t3);
    const { spatialReference: c3, extent: h2 } = o2, { datumTransformation: d3 } = t3;
    let y2 = I(e2, c3, d3);
    if (!h2.intersects(y2)) return { location: y2, value: null };
    if (null != o2.transform) {
      const e3 = o2.transform.inverseTransform(y2);
      if (!o2.nativeExtent.intersects(e3)) return { location: e3, value: null };
      y2 = e3;
    }
    const g2 = null != l5 && null != s3 && o2.hasMultidimensionalTranspose;
    if (t(this)) {
      const e3 = this.primaryRasters.rasters[0];
      if (g2) return e3.identify(y2, t3);
      const { pixelSize: r5 } = o2, n7 = 3, s4 = r5.x * n7 / 2, l6 = r5.y * n7 / 2, a4 = new z({ xmin: y2.x - s4, xmax: y2.x + s4, ymin: y2.y - l6, ymax: y2.y + l6, spatialReference: c3 }), f3 = { interpolation: "nearest", multidimensionalDefinition: i3, sliceId: t3.sliceId, bandIds: t3.bandIds }, { pixelBlock: m4 } = await e3.fetchPixels(a4, n7, n7, f3), { pixelBlock: h3 } = await this.fetchPixels(a4, n7, n7, f3);
      if (null == m4) return { location: y2, value: null };
      const p2 = Math.floor(n7 * n7 * 0.5), d4 = !m4.mask || m4.mask[p2] ? m4.pixels.map((e4) => e4[p2]) : null;
      let x2;
      return null != h3 && (x2 = !h3.mask || h3.mask[p2] ? h3.pixels.map((e4) => e4[p2]) : void 0), { location: y2, value: d4, processedValue: x2, pyramidLevel: 0 };
    }
    let I2 = 0;
    if (!g2) {
      let { srcResolution: i4 } = t3;
      if (i4) {
        i4.spatialReference && (i4 = a2(new _(i4), c3));
        I2 = ce(i4, o2, this.ioConfig.sampling).pyramidLevel;
      } else if (I2 = await this.computeBestPyramidLevelForLocation(e2, t3), null == I2) return { location: y2, value: null };
    }
    const w = this.identifyPixelLocation(y2, I2, null, g2);
    if (null === w) return { location: y2, value: null };
    const { row: R2, col: S3, rowOffset: k, colOffset: b, blockWidth: T } = w, v = await this._fetchRawTile(I2, R2, S3, t3);
    if (!((_a = v == null ? void 0 : v.pixels) == null ? void 0 : _a.length)) return { location: y2, value: null };
    const P = k * T + b;
    return this._processIdentifyResult(v, { srcLocation: y2, position: P, pyramidLevel: I2, useTransposedTile: !!g2, requestSomeSlices: a3, identifyOptions: t3 });
  }
  async fetchPixels(e2, t3, i3, r4 = {}) {
    e2 = le(e2), r4 = this._getRequestOptionsWithSliceId(r4);
    const { _hasNoneOrGCSShiftTransform: o2 } = this;
    if (r4.requestRawData && o2) return this._fetchPixels(e2, t3, i3, r4);
    const n6 = Z(e2.spatialReference), s3 = te(e2);
    if (null == n6 || 0 === s3 || 1 === s3 && this._isGlobalWrappableSource && o2) return this._fetchPixels(e2, t3, i3, r4);
    if (s3 >= 3) return { extent: e2, pixelBlock: null };
    const l5 = [], { xmin: a3, xmax: c3 } = e2, f3 = Math.round(n6 / (c3 - a3) * t3), m4 = f3 - Math.round((n6 / 2 - a3) / (c3 - a3) * t3);
    let h2 = 0;
    const p2 = [];
    for (let g2 = 0; g2 <= s3; g2++) {
      const o3 = new z({ xmin: 0 === g2 ? a3 : -n6 / 2, xmax: g2 === s3 ? c3 - n6 * g2 : n6 / 2, ymin: e2.ymin, ymax: e2.ymax, spatialReference: e2.spatialReference }), d4 = 0 === g2 ? f3 - m4 : g2 === s3 ? t3 - h2 : f3;
      h2 += d4, p2.push(d4);
      const x3 = r4.disableWrapAround && g2 > 0 ? null : this._fetchPixels(o3, d4, i3, r4);
      l5.push(x3);
    }
    const d3 = (await Promise.all(l5)).map((e3) => e3 == null ? void 0 : e3.pixelBlock);
    let x2 = null;
    const y2 = { width: t3, height: i3 };
    if (this.rasterJobHandler) {
      x2 = (await this.rasterJobHandler.mosaicAndTransform({ srcPixelBlocks: d3, srcMosaicSize: y2, destDimension: null, coefs: null, sampleSpacing: null, interpolation: "nearest", alignmentInfo: null, blockWidths: p2 }, r4)).pixelBlock;
    } else x2 = S2(d3, y2, { blockWidths: p2 });
    return { extent: e2, srcExtent: D(e2, this.rasterInfo.spatialReference, r4.datumTransformation), pixelBlock: x2 };
  }
  async fetchRawPixels(e2, t3, i3, r4 = {}) {
    t3 = { x: Math.floor(t3.x), y: Math.floor(t3.y) };
    const o2 = await this._fetchRawTiles(e2, t3, i3, r4), { nativeExtent: n6, nativePixelSize: s3, storageInfo: l5 } = this.rasterInfo, a3 = 2 ** e2, c3 = s3.x * a3, f3 = s3.y * a3, m4 = new z({ xmin: n6.xmin + c3 * t3.x, xmax: n6.xmin + c3 * (t3.x + i3.width - 1), ymin: n6.ymax - f3 * (t3.y + i3.height - 1), ymax: n6.ymax - f3 * t3.y, spatialReference: n6.spatialReference });
    if (!o2) return { extent: m4, srcExtent: m4, pixelBlock: null };
    const { pixelBlocks: h2, mosaicSize: p2 } = o2;
    if (1 === h2.length && null != h2[0] && h2[0].width === i3.width && h2[0].height === i3.height) return { extent: m4, srcExtent: m4, pixelBlock: o2.pixelBlocks[0] };
    const d3 = e2 > 0 ? l5.pyramidBlockWidth : l5.blockWidth, x2 = e2 > 0 ? l5.pyramidBlockHeight : l5.blockHeight, y2 = { x: t3.x % d3, y: t3.y % x2 };
    let g2;
    if (this.rasterJobHandler) {
      g2 = (await this.rasterJobHandler.mosaicAndTransform({ srcPixelBlocks: h2, srcMosaicSize: p2, destDimension: i3, clipOffset: y2, clipSize: i3, coefs: null, sampleSpacing: null, interpolation: r4.interpolation, alignmentInfo: null, blockWidths: null }, r4)).pixelBlock;
    } else g2 = S2(h2, p2, { clipOffset: y2, clipSize: i3 });
    return { extent: m4, srcExtent: m4, pixelBlock: g2 };
  }
  fetchRawTile(e2, t3, r4, o2) {
    throw new r("BaseRaster:read-not-implemented", "fetchRawTile() is not implemented");
  }
  computeExtent(e2) {
    return D(this.rasterInfo.extent, e2);
  }
  decodePixelBlock(e2, t3) {
    return this.rasterJobHandler ? this.rasterJobHandler.decode({ data: e2, options: t3 }) : R(e2, t3);
  }
  async request(e2, i3, r4 = 0) {
    const { customFetchParameters: o2 } = this.ioConfig, { range: n6, query: s3, headers: l5 } = i3;
    r4 = r4 ?? i3.retryCount ?? this.ioConfig.retryCount;
    const a3 = n6 ? { Range: `bytes=${n6.from}-${n6.to}` } : null;
    try {
      return await f(e2, { ...i3, query: { ...s3, ...o2 }, headers: { ...l5, ...a3 } });
    } catch (c3) {
      if (r4 > 0) return r4--, this.request(e2, i3, r4);
      throw c3;
    }
  }
  getSliceIndex(e2) {
    const { multidimensionalInfo: t3 } = this.rasterInfo;
    return null == t3 || null == e2 || 0 === e2.length ? null : C2(e2, t3);
  }
  getTileExtentFromTileInfo(e2, t3, i3, r4) {
    const o2 = r4.lodAt(e2);
    return o2 ? this.getTileExtent({ x: o2.resolution, y: o2.resolution }, t3, i3, r4.origin, r4.spatialReference, r4.size) : null;
  }
  updateTileInfo() {
    const { storageInfo: e2, spatialReference: t3, extent: i3, pixelSize: r4 } = this.rasterInfo, { pyramidResolutions: o2 } = e2;
    if (!e2.tileInfo) {
      const n6 = [], s3 = e2.maximumPyramidLevel || 0;
      let l5 = (r4.x + r4.y) / 2, a3 = 1 / 0.0254 * 96 * l5;
      for (let e3 = 0; e3 <= s3 && (n6.unshift(new l2({ level: s3 - e3, resolution: l5, scale: a3 })), e3 !== s3); e3++) if (o2) {
        const t4 = (o2[e3].x + o2[e3].y) / 2;
        a3 *= t4 / l5, l5 = t4;
      } else l5 *= 2, a3 *= 2;
      const c3 = new _({ x: i3.xmin, y: i3.ymax, spatialReference: t3 });
      e2.tileInfo = new z2({ origin: c3, size: [e2.blockWidth, e2.blockHeight], spatialReference: t3, lods: n6 }), e2.isVirtualTileInfo = true;
    }
  }
  createRemoteDatasetStorageInfo(e2, t3 = 512, i3 = 512, r4) {
    const { width: o2, height: n6, nativeExtent: s3, pixelSize: l5, spatialReference: a3 } = e2, c3 = new _({ x: s3.xmin, y: s3.ymax, spatialReference: a3 });
    null == r4 && (r4 = Math.max(0, Math.round(Math.log(Math.max(o2, n6)) / Math.LN2 - 8)));
    const f3 = this.computeBlockBoundary(s3, 512, 512, { x: s3.xmin, y: s3.ymax }, [l5], r4);
    e2.storageInfo = new p({ blockWidth: t3, blockHeight: i3, pyramidBlockWidth: t3, pyramidBlockHeight: i3, origin: c3, firstPyramidLevel: 1, maximumPyramidLevel: r4, blockBoundary: f3 });
  }
  async computeBestPyramidLevelForLocation(e2, t3 = {}) {
    return 0;
  }
  computeBlockBoundary(e2, t3, i3, r4, o2, n6 = 0, s3 = 2) {
    if (1 === o2.length && n6 > 0) {
      o2 = [...o2];
      let { x: e3, y: t4 } = o2[0];
      for (let i4 = 0; i4 < n6; i4++) e3 *= s3, t4 *= s3, o2.push({ x: e3, y: t4 });
    }
    const l5 = [], { x: a3, y: c3 } = r4;
    for (let f3 = 0; f3 < o2.length; f3++) {
      const { x: r5, y: n7 } = o2[f3];
      l5.push({ minCol: Math.floor((e2.xmin - a3 + 0.1 * r5) / t3 / r5), maxCol: Math.floor((e2.xmax - a3 - 0.1 * r5) / t3 / r5), minRow: Math.floor((c3 - e2.ymax + 0.1 * n7) / i3 / n7), maxRow: Math.floor((c3 - e2.ymin - 0.1 * n7) / i3 / n7) });
    }
    return l5;
  }
  getPyramidPixelSize(e2) {
    const { nativePixelSize: t3 } = this.rasterInfo, { pyramidResolutions: i3, pyramidScalingFactor: r4 } = this.rasterInfo.storageInfo;
    if (0 === e2) return t3;
    if (null != i3 && i3.length) return i3[e2 - 1];
    const o2 = r4 ** e2;
    return { x: t3.x * o2, y: t3.y * o2 };
  }
  identifyPixelLocation(e2, t3, i3, r4) {
    const { spatialReference: o2, nativeExtent: n6, storageInfo: s3 } = this.rasterInfo, { maximumPyramidLevel: l5, origin: a3, transposeInfo: c3 } = s3, f3 = r4 && null != c3 ? c3.tileSize[0] : s3.blockWidth, u3 = r4 && null != c3 ? c3.tileSize[1] : s3.blockHeight, m4 = I(e2, o2, i3);
    if (!n6.intersects(m4)) return null;
    if (t3 < 0 || t3 > l5) return null;
    const h2 = this.getPyramidPixelSize(t3), { x: p2, y: d3 } = h2, x2 = (a3.y - m4.y) / d3 / u3, y2 = (m4.x - a3.x) / p2 / f3, g2 = Math.min(u3 - 1, Math.floor((x2 - Math.floor(x2)) * u3)), I2 = Math.min(f3 - 1, Math.floor((y2 - Math.floor(y2)) * f3));
    return { pyramidLevel: t3, row: Math.floor(x2), col: Math.floor(y2), rowOffset: g2, colOffset: I2, blockWidth: f3, srcLocation: m4 };
  }
  getTileExtent(e2, t3, i3, r4, o2, n6) {
    const [s3, l5] = n6, a3 = r4.x + i3 * s3 * e2.x, c3 = a3 + s3 * e2.x, f3 = r4.y - t3 * l5 * e2.y, m4 = f3 - l5 * e2.y;
    return new z({ xmin: a3, xmax: c3, ymin: m4, ymax: f3, spatialReference: o2 });
  }
  getBlockWidthHeight(e2) {
    return { blockWidth: e2 > 0 ? this.rasterInfo.storageInfo.pyramidBlockWidth : this.rasterInfo.storageInfo.blockWidth, blockHeight: e2 > 0 ? this.rasterInfo.storageInfo.pyramidBlockHeight : this.rasterInfo.storageInfo.blockHeight };
  }
  isBlockOutside(e2, t3, i3) {
    const r4 = this.rasterInfo.storageInfo.blockBoundary[e2];
    return !r4 || r4.maxRow < t3 || r4.maxCol < i3 || r4.minRow > t3 || r4.minCol > i3;
  }
  updateImageSpaceRasterInfo(e2) {
    const { pixelSize: t3 } = e2, { width: i3, height: r4 } = e2, o2 = S.WebMercator;
    e2.spatialReference = o2, e2.extent = e2.nativeExtent = new z({ xmin: -0.5, ymax: 0.5, xmax: i3 - 0.5, ymin: 0.5 - r4, spatialReference: o2 }), e2.isPseudoSpatialReference = true, e2.transform = null, e2.pixelSize = new _({ x: 1, y: 1, spatialReference: o2 });
    const { extent: n6, storageInfo: s3 } = e2;
    if (s3) {
      s3.origin = new _({ x: n6.xmin, y: n6.ymax, spatialReference: o2 });
      const { pyramidResolutions: i4, tileInfo: r5 } = s3;
      if (i4 && i4.forEach((e3) => {
        e3.x /= t3.x, e3.y /= t3.y;
      }), r5) {
        r5.origin = s3.origin;
        const t4 = (e2.nativePixelSize.x + e2.nativePixelSize.y) / 2;
        r5.lods.forEach((e3, i5) => {
          e3.resolution = t4 * 2 ** i5, e3.scale = 96 * e3.resolution / 0.0254;
        });
      }
    }
  }
  async _refresh(e2) {
    return { dataChanged: true };
  }
  async _fetchPixels(e2, t3, i3, r4 = {}) {
    let o2 = te(e2);
    if (o2 >= 2) return { extent: e2, pixelBlock: null };
    const n6 = this._getSourceDataInfo(e2, t3, i3, r4), { pyramidLevel: s3, srcResolution: l5, srcWidth: a3, srcHeight: c3, ul: f3 } = n6;
    let { srcExtent: u3 } = n6;
    if (0 === a3 || 0 === c3) return { extent: e2, srcExtent: u3, pixelBlock: null };
    const { rasterInfo: h2 } = this, p2 = h2.transform, d3 = "gcs-shift" === (p2 == null ? void 0 : p2.type), x2 = null != Z(e2.spatialReference);
    !d3 && x2 || (o2 = te(u3, d3));
    const y2 = await this._fetchRawTiles(s3, f3, { width: a3, height: c3, wrapCount: o2 }, r4);
    if (!y2) return { extent: e2, srcExtent: u3, pixelBlock: null };
    u3 = y2.extent;
    const g2 = h2.storageInfo, I2 = s3 > 0 ? g2.pyramidBlockWidth : g2.blockWidth, w = s3 > 0 ? g2.pyramidBlockHeight : g2.blockHeight;
    let { x: R2, y: S3 } = h2.pixelSize;
    if (s3 > 0) {
      const { pyramidResolutions: e3, pyramidScalingFactor: t4 } = g2;
      if (null != e3 && e3[s3 - 1]) ({ x: R2, y: S3 } = e3[s3 - 1]);
      else {
        const e4 = t4 ** s3;
        R2 *= e4, S3 *= e4;
      }
    }
    const k = h2.spatialReference, b = new _({ x: R2, y: S3, spatialReference: k }), T = I2 === a3 && w === c3 && f3.x % I2 === 0 && f3.y % w === 0, v = new _({ x: (e2.xmax - e2.xmin) / t3, y: (e2.ymax - e2.ymin) / i3, spatialReference: e2.spatialReference }), P = !e2.spatialReference.equals(k), B = k.isGeographic ? 1e-9 : 1e-4, { datumTransformation: C3 } = r4;
    if (!P && T && 1 === y2.pixelBlocks.length && I2 === t3 && w === i3 && te2(l5, v, B)) return { extent: e2, srcExtent: u3, srcTilePixelSize: b, pixelBlock: y2.pixelBlocks[0] };
    const L2 = x2 && null != Z(u3.spatialReference) && this._hasNoneOrGCSShiftTransform, j3 = r4.requestProjectedLocalDirections && this.rasterInfo.dataType.startsWith("vector");
    j3 && !this.rasterJobHandler && await N();
    const E = this.rasterJobHandler ? await this.rasterJobHandler.getProjectionOffsetGrid({ projectedExtent: e2, srcBufferExtent: u3, pixelSize: v.toJSON(), datumTransformation: C3, rasterTransform: p2, hasWrapAround: o2 > 0 || L2, isAdaptive: false !== this.ioConfig.optimizeProjectionAccuracy, includeGCSGrid: j3 }, r4) : oe({ projectedExtent: e2, srcBufferExtent: u3, pixelSize: v, datumTransformation: C3, rasterTransform: p2, hasWrapAround: o2 > 0 || L2, isAdaptive: false, includeGCSGrid: j3 });
    let O;
    const q = !r4.requestRawData, F = { rows: E.spacing[0], cols: E.spacing[1] }, J = this._hasNoneOrGCSShiftTransform ? this._getRasterTileAlignmentInfo(s3, u3.xmin) : void 0, { pixelBlocks: N2, mosaicSize: V, isPartiallyFilled: U } = y2;
    let Q = null;
    if (this.rasterJobHandler) {
      const e3 = await this.rasterJobHandler.mosaicAndTransform({ srcPixelBlocks: N2, srcMosaicSize: V, destDimension: q ? { width: t3, height: i3 } : null, coefs: q ? E.coefficients : null, sampleSpacing: q ? F : null, projectDirections: j3, gcsGrid: j3 ? E.gcsGrid : null, isUV: "vector-uv" === this.rasterInfo.dataType, interpolation: r4.interpolation, alignmentInfo: J, blockWidths: null }, r4);
      ({ pixelBlock: O, localNorthDirections: Q } = e3);
    } else {
      const e3 = S2(N2, V, { alignmentInfo: J });
      O = q ? z3(e3, { width: t3, height: i3 }, E.coefficients, F, r4.interpolation) : e3, j3 && E.gcsGrid && (Q = j({ width: t3, height: i3 }, E.gcsGrid), O = u(O, this.rasterInfo.dataType, Q));
    }
    return r4.requestRawData || j3 ? { extent: e2, srcExtent: u3, srcTilePixelSize: b, pixelBlock: O, transformGrid: E, localNorthDirections: Q, isPartiallyFilled: U } : { extent: e2, srcExtent: e2, srcTilePixelSize: b, pixelBlock: O };
  }
  async _fetchRawTiles(e2, t3, i3, r4) {
    const { origin: o2, blockBoundary: n6 } = this.rasterInfo.storageInfo, { blockWidth: s3, blockHeight: l5 } = this.getBlockWidthHeight(e2);
    let { x: a3, y: c3 } = t3, { width: f3, height: m4, wrapCount: h2 } = i3;
    const p2 = this._getRasterTileAlignmentInfo(e2, 0);
    r4.buffer && (a3 -= r4.buffer.cols, c3 -= r4.buffer.rows, f3 += 2 * r4.buffer.cols, m4 += 2 * r4.buffer.rows);
    let d3 = 0, x2 = 0, y2 = 0;
    if (h2 && null != p2) {
      ({ worldColumnCountFromOrigin: x2, originColumnOffset: y2, rightPadding: d3 } = p2);
      x2 * p2.blockWidth - d3 >= a3 + f3 && (d3 = 0);
    }
    const g2 = Math.floor(a3 / s3), I2 = Math.floor(c3 / l5), w = Math.floor((a3 + f3 + d3 - 1) / s3), R2 = Math.floor((c3 + m4 + d3 - 1) / l5), S3 = n6[e2];
    if (!S3) return null;
    const { minRow: k, minCol: b, maxCol: T, maxRow: v } = S3;
    if (0 === h2 && (R2 < k || w < b || I2 > v || g2 > T)) return null;
    const P = new Array();
    let B = false;
    const _2 = null == this.ioConfig.allowPartialFill ? r4.allowPartialFill : this.ioConfig.allowPartialFill;
    for (let u3 = I2; u3 <= R2; u3++) for (let t4 = g2; t4 <= w; t4++) {
      let i4 = t4;
      if (!r4.disableWrapAround && h2 && null != p2 && x2 <= t4 && (i4 = t4 - x2 - y2), u3 >= k && i4 >= b && v >= u3 && T >= i4) {
        const t5 = this._fetchRawTile(e2, u3, i4, r4);
        _2 ? P.push(new Promise((e3) => {
          t5.then((t6) => e3(t6)).catch(() => {
            B = true, e3(null);
          });
        })) : P.push(t5);
      } else P.push(Promise.resolve(null));
    }
    if (0 === P.length) return null;
    const M = await Promise.all(P), W = { height: (R2 - I2 + 1) * l5, width: (w - g2 + 1) * s3 }, { spatialReference: C3 } = this.rasterInfo, H = this.getPyramidPixelSize(e2), { x: z4, y: L2 } = H;
    return { extent: new z({ xmin: o2.x + g2 * s3 * z4, xmax: o2.x + (w + 1) * s3 * z4, ymin: o2.y - (R2 + 1) * l5 * L2, ymax: o2.y - I2 * l5 * L2, spatialReference: C3 }), pixelBlocks: M, mosaicSize: W, isPartiallyFilled: B };
  }
  _fetchRawTile(e2, t3, i3, r4) {
    var _a;
    const { storageInfo: o2 } = this.rasterInfo, n6 = null != o2.transposeInfo && !!r4.transposedVariableName;
    if (!n6) {
      const r5 = o2.blockBoundary[e2];
      if (!r5) return Promise.resolve(null);
      const { minRow: n7, minCol: s3, maxCol: l6, maxRow: a4 } = r5;
      if (t3 < n7 || i3 < s3 || t3 > a4 || i3 > l6) return Promise.resolve(null);
    }
    const l5 = n6 ? r4.transposeVariableName : r4.sliceId, a3 = o2.isBsqTile ? r4.bandIds : null, c3 = i(this.rasterId, l5, a3), f3 = `${e2}/${t3}/${i3}`;
    let u3 = h(c3, r4.registryId, f3);
    if (null == u3) {
      const n7 = new AbortController(), s3 = (_a = r4.bandIds) == null ? void 0 : _a.slice();
      if ((s3 == null ? void 0 : s3.length) && o2.isBsqTile) {
        const o3 = new Set(s3), l6 = [], a4 = Array.from(o3);
        for (const s4 of a4) l6.push(this._tileFetchQueue.push({ pyramidLevel: e2, row: t3, col: i3, options: { ...r4, bandIds: [s4] } }, { signal: n7.signal }));
        u3 = Promise.all(l6).then((e3) => {
          if (e3.some((e4) => null == e4)) return null;
          if (a4.length !== s3.length) {
            const t4 = [];
            for (const i4 of s3) {
              let r5 = e3[a4.indexOf(i4)];
              t4.includes(r5) && (r5 = r5.clone()), t4.push(r5);
            }
            e3 = t4;
          }
          return this.rasterJobHandler ? this.rasterJobHandler.compositeBands({ pixelBlocks: e3 }, { signal: n7.signal, transferPixelsToWorker: true }) : s(e3);
        });
      } else u3 = this._tileFetchQueue.push({ pyramidLevel: e2, row: t3, col: i3, options: r4 }, { signal: n7.signal });
      x(c3, r4.registryId, f3, u3, n7), u3.catch(() => d2(c3, r4.registryId, f3));
    }
    return r4.signal && m2(r4, () => {
      m3(c3, r4.registryId, f3);
    }), u3;
  }
  _computeMagDirValues(e2) {
    var _a;
    const { bandCount: t3, dataType: i3 } = this.rasterInfo;
    if (!(2 === t3 && "vector-magdir" === i3 || "vector-uv" === i3) || 2 !== (e2 == null ? void 0 : e2.length) || !((_a = e2[0]) == null ? void 0 : _a.length)) return null;
    const r4 = e2[0].length;
    if ("vector-magdir" === i3) {
      const t4 = e2[1].map((e3) => (e3 + 360) % 360);
      return [e2[0], t4];
    }
    const [o2, n6] = e2, s3 = [], l5 = [];
    for (let a3 = 0; a3 < r4; a3++) {
      const [e3, t4] = l3([o2[a3], n6[a3]]);
      s3.push(e3), l5.push(t4);
    }
    return [s3, l5];
  }
  _getRasterTileAlignmentInfo(e2, t3) {
    return null == this._rasterTileAlignmentInfo && (this._rasterTileAlignmentInfo = ne(this.rasterInfo)), null == this._rasterTileAlignmentInfo.pyramidsInfo ? null : { startX: t3, halfWorldWidth: this._rasterTileAlignmentInfo.halfWorldWidth, hasGCSSShiftTransform: this._rasterTileAlignmentInfo.hasGCSSShiftTransform, ...this._rasterTileAlignmentInfo.pyramidsInfo[e2] };
  }
  _getSourceDataInfo(e2, t3, i3, r4 = {}) {
    const o2 = { datumTransformation: r4.datumTransformation, pyramidLevel: 0, pyramidResolution: null, srcExtent: null, srcHeight: 0, srcResolution: null, srcWidth: 0, ul: { x: 0, y: 0 } };
    r4.srcResolution && (o2.srcResolution = r4.srcResolution, this._updateSourceDataInfo(e2, o2));
    const n6 = this.rasterInfo.storageInfo.maximumPyramidLevel || 0, { srcWidth: s3, srcHeight: l5, pyramidLevel: a3 } = o2, c3 = s3 / t3, f3 = l5 / i3, u3 = a3 < n6 && c3 * f3 >= 16, h2 = a3 === n6 && this._requireTooManySrcTiles(s3, l5, t3, i3);
    if (u3 || h2 || (0 === s3 || 0 === l5)) {
      const s4 = new _({ x: (e2.xmax - e2.xmin) / t3, y: (e2.ymax - e2.ymin) / i3, spatialReference: e2.spatialReference });
      let l6 = j2(s4, this.rasterInfo.spatialReference, e2, o2.datumTransformation);
      const h3 = !l6 || r4.srcResolution && l6.x + l6.y < r4.srcResolution.x + r4.srcResolution.y;
      if (u3 && r4.srcResolution && h3) {
        const e3 = Math.round(Math.log(Math.max(c3, f3)) / Math.LN2) - 1;
        if (n6 - a3 + 3 >= e3) {
          const t4 = 2 ** e3;
          l6 = { x: r4.srcResolution.x * t4, y: r4.srcResolution.y * t4 };
        }
      }
      l6 && (o2.srcResolution = l6, this._updateSourceDataInfo(e2, o2));
    }
    return this._requireTooManySrcTiles(o2.srcWidth, o2.srcHeight, t3, i3) && (o2.srcWidth = 0, o2.srcHeight = 0), o2;
  }
  _requireTooManySrcTiles(e2, t3, i3, r4) {
    const { tileInfo: o2 } = this.rasterInfo.storageInfo, n6 = Math.ceil(e2 / o2.size[0]) * Math.ceil(t3 / o2.size[1]), s3 = e2 / i3, l5 = t3 / r4, a3 = Math.max(1, (i3 + r4) / 1024);
    return n6 >= Y * a3 || s3 > K || l5 > K;
  }
  _updateSourceDataInfo(e2, t3) {
    t3.srcWidth = 0, t3.srcHeight = 0;
    const { rasterInfo: i3 } = this, r4 = i3.spatialReference, { srcResolution: o2, datumTransformation: n6 } = t3, { pyramidLevel: s3, pyramidResolution: l5, excessiveReading: a3 } = ce(o2, i3, this.ioConfig.sampling);
    if (a3) return;
    let c3 = t3.srcExtent || D(e2, r4, n6);
    if (null == c3) return;
    const f3 = i3.transform;
    f3 && (c3 = f3.inverseTransform(c3)), t3.srcExtent = c3;
    const { origin: u3 } = i3.storageInfo, { width: m4, height: h2, ul: p2 } = n4(c3, u3, l5, s3);
    t3.pyramidLevel = s3, t3.pyramidResolution = l5, t3.srcWidth = m4, t3.srcHeight = h2, t3.ul = p2;
  }
  _getRequestOptionsWithSliceId(e2) {
    return null != this.rasterInfo.multidimensionalInfo && null == e2.sliceId && (e2 = { ...e2, sliceId: this.getSliceIndex(e2.multidimensionalDefinition) }), e2;
  }
  _processIdentifyResult(e2, t3) {
    const { srcLocation: i3, position: r4, pyramidLevel: o2, useTransposedTile: n6 } = t3, s3 = e2.pixels[0].length / e2.width / e2.height;
    if (!(!e2.mask || e2.mask[r4])) return { location: i3, value: null };
    const { multidimensionalInfo: l5 } = this.rasterInfo;
    if (null == l5 || !n6) {
      const t4 = e2.pixels.map((e3) => e3[r4]), n7 = { location: i3, value: t4, pyramidLevel: o2 }, s4 = this._computeMagDirValues(t4.map((e3) => [e3]));
      return (s4 == null ? void 0 : s4.length) && (n7.magdirValue = s4.map((e3) => e3[0])), n7;
    }
    let a3 = e2.pixels.map((e3) => e3.slice(r4 * s3, r4 * s3 + s3)), c3 = this._computeMagDirValues(a3);
    const { requestSomeSlices: f3, identifyOptions: u3 } = t3;
    let m4 = s2(l5, u3.transposedVariableName);
    if (f3) {
      const e3 = r2(m4, u3.multidimensionalDefinition, u3.timeExtent);
      a3 = a3.map((t4) => e3.map((e4) => t4[e4])), c3 = c3 == null ? void 0 : c3.map((t4) => e3.map((e4) => t4[e4])), m4 = e3.map((e4) => m4[e4]);
    }
    const h2 = e2.noDataValues || this.rasterInfo.noDataValue, p2 = { pixels: a3, pixelType: e2.pixelType };
    let d3;
    null != h2 && (g(p2, h2), d3 = p2.mask);
    return { location: i3, value: null, dataSeries: m4.map((e3, t4) => {
      const i4 = { value: 0 === (d3 == null ? void 0 : d3[t4]) ? null : a3.map((e4) => e4[t4]), multidimensionalDefinition: e3.multidimensionalDefinition.map((e4) => new n3({ ...e4, isSlice: true })) };
      return (c3 == null ? void 0 : c3.length) && (i4.magdirValue = [c3[0][t4], c3[1][t4]]), i4;
    }), pyramidLevel: o2 };
  }
};
function te2(e2, t3, i3) {
  return Math.abs(e2.x - t3.x) < i3 && Math.abs(e2.y - t3.y) < i3;
}
__decorate([a()], ee.prototype, "_rasterTileAlignmentInfo", void 0), __decorate([a()], ee.prototype, "_tileFetchQueue", void 0), __decorate([a({ readOnly: true })], ee.prototype, "_isGlobalWrappableSource", null), __decorate([a({ readOnly: true })], ee.prototype, "_hasNoneOrGCSShiftTransform", null), __decorate([a()], ee.prototype, "_openPromise", void 0), __decorate([a()], ee.prototype, "rasterJobHandler", null), __decorate([a({ readOnly: true })], ee.prototype, "rasterId", null), __decorate([a(d)], ee.prototype, "url", null), __decorate([a({ type: String, json: { write: true } })], ee.prototype, "datasetName", void 0), __decorate([a({ type: String, json: { write: true } })], ee.prototype, "datasetFormat", void 0), __decorate([a()], ee.prototype, "hasUniqueSourceStorageInfo", void 0), __decorate([a()], ee.prototype, "rasterInfo", void 0), __decorate([a()], ee.prototype, "ioConfig", void 0), __decorate([a()], ee.prototype, "sourceJSON", void 0), __decorate([a()], ee.prototype, "rawBlockRegistryIds", void 0), ee = __decorate([c("esri.layers.raster.datasets.BaseRaster")], ee);

// node_modules/@arcgis/core/layers/raster/datasets/xmlUtilities.js
function n5(e2, t3) {
  if (!e2 || !t3) return [];
  let l5 = t3;
  t3.includes("/") ? (l5 = t3.slice(0, t3.indexOf("/")), t3 = t3.slice(t3.indexOf("/") + 1)) : t3 = "";
  const r4 = [];
  if (t3) {
    const u4 = n5(e2, l5);
    for (let e3 = 0; e3 < u4.length; e3++) {
      n5(u4[e3], t3).forEach((n6) => r4.push(n6));
    }
    return r4;
  }
  const u3 = e2.getElementsByTagNameNS("*", l5);
  if (!u3 || 0 === u3.length) return [];
  for (let n6 = 0; n6 < u3.length; n6++) r4.push(u3[n6] || u3.item(n6));
  return r4;
}
function e(t3, l5) {
  if (!t3 || !l5) return null;
  let r4 = l5;
  l5.includes("/") ? (r4 = l5.slice(0, l5.indexOf("/")), l5 = l5.slice(l5.indexOf("/") + 1)) : l5 = "";
  const u3 = n5(t3, r4);
  return u3.length > 0 ? l5 ? e(u3[0], l5) : u3[0] : null;
}
function t2(n6, t3 = null) {
  const l5 = t3 ? e(n6, t3) : n6;
  let r4;
  return l5 ? (r4 = l5.textContent || l5.nodeValue, r4 ? r4.trim() : null) : null;
}
function l4(e2, t3) {
  const l5 = n5(e2, t3), r4 = [];
  let u3;
  for (let n6 = 0; n6 < l5.length; n6++) u3 = l5[n6].textContent || l5[n6].nodeValue, u3 && (u3 = u3.trim(), "" !== u3 && r4.push(u3));
  return r4;
}
function r3(n6, e2 = null) {
  const l5 = t2(n6, e2);
  return (l5 == null ? void 0 : l5.split(" ").map((n7) => Number(n7))) ?? [];
}
function u2(n6, e2) {
  return l4(n6, e2).map((n7) => Number(n7));
}
function o(n6, e2) {
  const l5 = t2(n6, e2);
  return Number(l5);
}
function i2(n6, e2) {
  var _a;
  const t3 = (_a = n6 == null ? void 0 : n6.nodeName) == null ? void 0 : _a.toLowerCase(), l5 = e2.toLowerCase();
  return t3.slice(t3.lastIndexOf(":") + 1) === l5;
}
function c2(n6) {
  return n6.nodeName.slice(n6.nodeName.lastIndexOf(":") + 1);
}

export {
  ee,
  n5 as n,
  e,
  t2 as t,
  l4 as l,
  r3 as r,
  u2 as u,
  o,
  i2 as i,
  c2 as c
};
//# sourceMappingURL=chunk-SRJ2IHTG.js.map
