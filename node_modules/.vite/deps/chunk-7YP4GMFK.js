import {
  r as r5
} from "./chunk-FH5SMI7B.js";
import {
  c as c2,
  t as t6
} from "./chunk-EVXQFK6M.js";
import {
  e as e10,
  f as f3
} from "./chunk-XWHOUQ7P.js";
import {
  r as r4
} from "./chunk-FLXJ4GV5.js";
import {
  a,
  b,
  c,
  e as e9,
  f as f2,
  n as n2,
  t2 as t5,
  v
} from "./chunk-R5OG4C43.js";
import {
  m
} from "./chunk-3ZV7F6M4.js";
import {
  i as i5,
  o as o2
} from "./chunk-APFTN4C3.js";
import {
  o as o3
} from "./chunk-R2SEOAN7.js";
import {
  t as t4
} from "./chunk-XJEXS547.js";
import {
  e as e2,
  e2 as e3,
  e3 as e6,
  r as r3,
  t2 as t3
} from "./chunk-ETP7JXHI.js";
import {
  d
} from "./chunk-F6VYKY5P.js";
import {
  h
} from "./chunk-72575BJC.js";
import {
  d as d2,
  d2 as d3,
  e as e7,
  f,
  p as p2,
  u
} from "./chunk-KQRXG4VT.js";
import {
  e as e5
} from "./chunk-B6MIGAJF.js";
import {
  p
} from "./chunk-ZQF3TCDV.js";
import {
  e as e8
} from "./chunk-YVHZBCEM.js";
import {
  e as e4
} from "./chunk-SGFXS57V.js";
import {
  r as r2
} from "./chunk-URRK3ZOJ.js";
import {
  o
} from "./chunk-727R2UNB.js";
import {
  s
} from "./chunk-X7QEPCUU.js";
import {
  i as i4,
  n,
  t as t2
} from "./chunk-NYA5CJQN.js";
import {
  i as i3
} from "./chunk-F4DT2IMP.js";
import {
  Q
} from "./chunk-LY4SN26E.js";
import {
  i as i2
} from "./chunk-GAE5ZZMT.js";
import {
  i
} from "./chunk-R4QJMN2M.js";
import {
  e,
  r,
  t
} from "./chunk-S2HMSOEZ.js";

// node_modules/@arcgis/core/views/3d/webgl-engine/lib/PathVertexInfo.js
var n3 = 0;
var e11 = 15;
var o4 = 4;
var s2 = 7;
var d4 = o4 + 3;
var r6 = 7;
var t7 = d4 + 3;
var u2 = 1;
function c3({ sourceIndex: c6, subdivision: p5, type: v2, capSide: a3 }) {
  return i2(c6 >= 0 && c6 <= e11, "invalid sourceIndex"), i2(p5 >= 0 && p5 <= s2, "invalid subdivision"), (c6 & e11) << n3 | (p5 & s2) << o4 | (v2 & r6) << d4 | (a3 & u2) << t7;
}
function p3(i7) {
  return { sourceIndex: i7 >> n3 & e11, subdivision: i7 >> o4 & s2, type: i7 >> d4 & r6, capSide: i7 >> t7 & u2 };
}

// node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderLibrary/attributes/PathVertexInfo.glsl.js
function p4(p5) {
  const { attributes: f4, vertex: h4 } = p5;
  f4.add("pathVertexInfo", "uint"), h4.constants.add("pathVertexInfoSourceIndexShift", "uint", n3), h4.constants.add("pathVertexInfoSourceIndexMask", "uint", e11), h4.constants.add("pathVertexInfoSubdivisionShift", "uint", o4), h4.constants.add("pathVertexInfoSubdivisionMask", "uint", s2), h4.constants.add("pathVertexInfoTypeShift", "uint", d4), h4.constants.add("pathVertexInfoTypeMask", "uint", r6), h4.constants.add("pathVertexInfoCapSideShift", "uint", t7), h4.constants.add("pathVertexInfoCapSideMask", "uint", u2), h4.constants.add("pathVertexCapSideEnd", "uint", 1), h4.code.add(t2`struct PathVertexInfo {
uint sourceIndex;
float subdivision;
uint type;
bool isEnd;
};
PathVertexInfo decodePathVertexInfo() {
uint sourceIndex = (pathVertexInfo >> pathVertexInfoSourceIndexShift) & pathVertexInfoSourceIndexMask;
uint subdivision = (pathVertexInfo >> pathVertexInfoSubdivisionShift) & pathVertexInfoSubdivisionMask;
uint type = (pathVertexInfo >> pathVertexInfoTypeShift) & pathVertexInfoTypeMask;
uint capSide = (pathVertexInfo >> pathVertexInfoCapSideShift) & pathVertexInfoCapSideMask;
return PathVertexInfo(
sourceIndex,
float(subdivision),
type,
capSide == pathVertexCapSideEnd
);
}`);
}

// node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderModules/Float2sPassUniform.js
var e12 = class extends i4 {
  constructor(r8, e13, o7, s3) {
    super(r8, "vec2", 1, (e14, t8, c6) => e14.setUniform2fv(r8, o7(t8, c6), s3), e13);
  }
};

// node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderModules/IntegersPassUniform.js
var o5 = class extends i4 {
  constructor(r8, o7, s3) {
    super(r8, "int", 1, (o8, t8, e13) => o8.setUniform1iv(r8, s3(t8, e13)), o7);
  }
};

// node_modules/@arcgis/core/views/3d/webgl-engine/lib/PathProfile.js
var n4 = { 0: { indexCount: 2 * c2, poleCount: 1, vertexCount: c2 }, 1: { indexCount: 8, poleCount: 4, vertexCount: 8 } };
var r7 = class {
  constructor(t8) {
    this.type = t8, this.vertices = new Array(), this.normals = new Array(), this.indices = new Array(), this.poles = new Array(), this.poleIndices = new Array();
  }
  addVertex(t8, e13) {
    return this.vertices.push(t(t8)), this.normals.push(t(e13)), this.vertices.length - 1;
  }
  addPole(t8, e13 = null) {
    return this.poles.push({ position: t(t8), normal: e13 ? t(e13) : null }), this.poles.length - 1;
  }
  addSegment(t8, e13 = null) {
    this.indices.push(t8.v0), this.indices.push(t8.v1), e13 && (this.poleIndices.push(e13.v0), this.poleIndices.push(e13.v1));
  }
  get numSegments() {
    return this.indices.length / 2;
  }
  translate(t8, e13) {
    for (const s3 of this.vertices) s3[0] += t8, s3[1] += e13;
    for (const s3 of this.poles) s3.position[0] += t8, s3.position[1] += e13;
  }
  get usedMemory() {
    return this.vertices.length * i(this.vertices[0]) * 2 + i(this.indices);
  }
};
var i6 = { top: [0, -0.5], bottom: [0, 0.5] };
function d5(t8) {
  const s3 = 0.5, n6 = c2, d6 = new r7(0), a3 = { v0: 0, v1: 0 };
  d6.addPole(e(0, 0));
  for (let o7 = 0; o7 < n6; ++o7) {
    const t9 = 2 * o7 * Math.PI / n6, r8 = Math.cos(t9), i7 = Math.sin(t9), a4 = e(r8 * s3, i7 * s3), c7 = e(r8, i7);
    d6.addVertex(a4, c7);
  }
  for (let e13 = 0; e13 < n6 - 1; ++e13) {
    const t9 = { v0: e13, v1: e13 + 1 };
    d6.addSegment(t9, a3);
  }
  const c6 = { v0: n6 - 1, v1: 0 };
  if (d6.addSegment(c6, a3), "center" !== t8) {
    const e13 = i6[t8];
    d6.translate(e13[0], e13[1]);
  }
  return d6;
}
var a2 = { center: d5("center"), top: d5("top"), bottom: d5("bottom") };
var c4 = { center: h2("center"), top: h2("top"), bottom: h2("bottom") };
function h2(t8) {
  const s3 = 1, o7 = 1, n6 = new r7(1), d6 = e(0.5 * -s3, 0.5 * -o7), a3 = e(0.5 * s3, 0.5 * -o7), c6 = e(0.5 * s3, 0.5 * o7), h4 = e(0.5 * -s3, 0.5 * o7), l = e(0, -1), v2 = e(1, 0), p5 = e(0, 1), m2 = e(-1, 0);
  if (n6.addPole(e(0, 0.5 * o7), p5), n6.addPole(e(0, 0.5 * o7)), n6.addPole(e(0, 0.5 * -o7)), n6.addPole(e(0, 0.5 * -o7), l), n6.addVertex(d6, l), n6.addVertex(a3, l), n6.addSegment({ v0: 0, v1: 1 }, { v0: 3, v1: 3 }), n6.addVertex(a3, v2), n6.addVertex(c6, v2), n6.addSegment({ v0: 2, v1: 3 }, { v0: 2, v1: 1 }), n6.addVertex(c6, p5), n6.addVertex(h4, p5), n6.addSegment({ v0: 4, v1: 5 }, { v0: 0, v1: 0 }), n6.addVertex(h4, m2), n6.addVertex(d6, m2), n6.addSegment({ v0: 6, v1: 7 }, { v0: 1, v1: 2 }), "center" !== t8) {
    const e13 = i6[t8];
    n6.translate(e13[0], e13[1]);
  }
  return n6;
}

// node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderLibrary/attributes/PathEvaluate.glsl.js
function x(e13, x2) {
  const { vertex: m2 } = e13;
  e13.include(p4);
  const u3 = 1 === x2.upVectorAlignment;
  m2.uniforms.add(new r2("angleCutoff", (e14) => e14.cutoffAngle)), m2.code.add(t2`float reciprocalClamped(float value) {
float signValue = value < 0.0 ? -1.0 : 1.0;
return signValue / max(abs(value), 1e-6);
}`), u3 ? m2.code.add(t2`vec2 applyMiterStretch(vec2 vertex, float rotationAngle, vec2 rotationRight) {
if (rotationAngle == 0.0) {
return vertex;
}
float k = reciprocalClamped(cos(0.5 * rotationAngle));
mat2 miterStretch = mat2(
1. + (k - 1.) * rotationRight.x * rotationRight.x,
(k - 1.) * rotationRight.x * rotationRight.y,
(k - 1.) * rotationRight.x * rotationRight.y,
1. + (k - 1.) * rotationRight.y * rotationRight.y
);
return miterStretch * vertex;
}`) : m2.code.add(t2`vec2 applyMiterStretch(vec2 vertex, float rotationAngle) {
if (rotationAngle == 0.0) {
return vertex;
}
float k = reciprocalClamped(cos(0.5 * rotationAngle));
return vec2(k, 1.) * vertex;
}`);
  const { vertexCount: v2, indexCount: h4, poleCount: g2 } = n4[x2.pathProfileType];
  switch (m2.uniforms.add(new e12("pathProfileVertices", v2, (e14) => e14.profile.vertices.flat()), new e12("pathProfileNormals", v2, (e14) => e14.profile.normals.flat())), m2.code.add(t2`mat3 mat3FromRotation(float angle, vec3 axis) {
float x = axis.x;
float y = axis.y;
float z = axis.z;
float s = sin(angle);
float c = cos(angle);
float t = 1.0 - c;
return mat3(
x * x * t + c,      y * x * t + z * s,  z * x * t - y * s,
x * y * t - z * s,  y * y * t + c,      z * y * t + x * s,
x * z * t + y * s,  y * z * t - x * s,  z * z * t + c
);
}`), m2.code.add(t2`struct ExtrusionFrame {
vec3 up;
vec3 right;
};
struct ExtrudedVertex {
ExtrusionFrame frame;
vec2 profileVertex;
vec2 profileNormal;
vec2 rotationRight;
float maxDistance;
float capPositionOffset;
float capNormalOffset;
bool isCap;
};`), u3 ? m2.code.add(t2`vec2 getPathRotationRight(ExtrusionFrame frame) {
vec3 rotationUp = getFrameRotationUp();
float a = dot(rotationUp, frame.up);
float b = dot(rotationUp, frame.right);
vec3 vertex = normalize(frame.up * -b + frame.right * a);
return vec2(dot(vertex, frame.right), dot(vertex, frame.up));
}`) : m2.code.add(t2`vec2 getPathRotationRight() {
return vec2(1., 0.);
}`), m2.constants.add("pathVertexTypeJoin", "uint", 0), m2.uniforms.add(new o("numJoinSubdivisions", (e14) => e14.numJoinSubdivisions)), m2.code.add(t2`
      ExtrudedVertex evaluateJoinVertex(PathVertexInfo vertexInfo, ExtrusionFrame frame) {
        vec2 profileVertex = pathProfileVertices[vertexInfo.sourceIndex];
        vec2 profileNormal = pathProfileNormals[vertexInfo.sourceIndex];
        float profileRotation = getProfileRotation();
        vec2 rotationRight = getPathRotationRight(${n(u3, t2`frame`)});
        bool isBevel = abs(profileRotation) >= angleCutoff;

        // determine if the current profile vertex is on the inside or outside of the rotationAxis
        // this determines if the geometry folds inwards or is bend outwards
        float b = dot(profileVertex, rotationRight);
        bool isBend = b * profileRotation >= 0.;

        bool isBevelBend = isBevel && isBend;

        if (isBevelBend) {
          float k = vertexInfo.subdivision;
          // rotate half rotation angle backwards to where the rotation starts
          // and then rotate a couple of times depending on the current subdivision segment
          float bendRotation = -profileRotation * 0.5 + (k * profileRotation) / float(numJoinSubdivisions);
          if (bendRotation != 0.) {
            vec3 rotationUp = getFrameRotationUp();
            mat3 transform  = mat3FromRotation(bendRotation, rotationUp);
            ${n(u3, t2`frame.up = normalize(transform * frame.up);`)}
            frame.right = normalize(transform * frame.right);
          }
        } else {
          profileVertex = applyMiterStretch(
            profileVertex,
            profileRotation${n(u3, t2`,
              rotationRight`)}
          );
        }

        rotationRight = isBend ? vec2(0.) : rotationRight;
        float maxDistance = isBend
        ? 0.
        : getMaxStretchDistance();

        return ExtrudedVertex(
          frame,
          profileVertex,
          profileNormal,
          rotationRight,
          maxDistance,
          0.,
          0.,
          false
        );
      }
    `), m2.constants.add("pathVertexTypeCapConnectingProfile", "uint", 1), m2.code.add(t2`
        ExtrudedVertex evaluateConnectingVertex(PathVertexInfo vertexInfo, ExtrusionFrame frame) {
          vec2 profileVertex = pathProfileVertices[vertexInfo.sourceIndex];
          vec2 profileNormal = pathProfileNormals[vertexInfo.sourceIndex];

          float profilePlaneVertexOffset = ${n(2 === x2.pathCapType, t2`vertexInfo.isEnd ? 0.5 : -0.5`, t2`0.`)};

          return ExtrudedVertex(
            frame,
            profileVertex,
            profileNormal,
            vec2(0.),
            0.,
            profilePlaneVertexOffset,
            0.,
            true
          );
        }
    `), x2.pathCapType) {
    case 1:
    case 2:
      m2.constants.add("pathVertexTypeFlatCapProfile", "uint", 2), m2.code.add(t2`
          ExtrudedVertex evaluateFlatCapVertex(PathVertexInfo vertexInfo, ExtrusionFrame frame) {
            vec2 profileVertex = pathProfileVertices[vertexInfo.sourceIndex];
            bool isEnd = vertexInfo.isEnd;
            float normalOffset = isEnd ? 1. : -1.;
            float profilePlaneOffset = ${n(2 === x2.pathCapType, t2`isEnd ? 0.5 : -0.5`, t2`0.0`)};
            vec2 normal = vec2(0.);

            return ExtrudedVertex(
              frame,
              profileVertex,
              normal,
              vec2(0.),
              0.,
              profilePlaneOffset,
              normalOffset,
              true
            );
          }
        `);
      break;
    case 3:
      m2.uniforms.add(new e6("pathProfilePoles", g2, (e14) => e14.profile.poles.flatMap(({ position: e15, normal: t8 }) => [...e15, ...t8 ?? c5]), { supportsNaN: true }), new o5("pathProfilePoleIndices", h4, (e14) => e14.profile.poleIndices)), m2.include(t4), m2.constants.add("pathVertexTypeRoundCapPole", "uint", 3), m2.constants.add("pathVertexTypeRoundCapInnerProfile", "uint", 4), m2.constants.add("pathNumRoundCapExtrusionSubdivisions", "float", t6), m2.code.add(t2`ExtrudedVertex evaluateRoundCapPoleVertex(PathVertexInfo vertexInfo, ExtrusionFrame frame) {
bool isEnd = vertexInfo.isEnd;
float capSign = isEnd ? 1. : -1.;
float offsetScale = capSign * 0.5;
vec4 pole = pathProfilePoles[vertexInfo.sourceIndex];
vec2 polePosition = pole.xy;
bool hasPoleNormal = !isnan(pole.z);
vec2 poleNormal = hasPoleNormal ? pole.zw : vec2(0.);
float normalOffset = hasPoleNormal ? 0. : capSign;
return ExtrudedVertex(
frame,
polePosition,
poleNormal,
vec2(0.),
0.,
offsetScale,
normalOffset,
true
);
}
ExtrudedVertex evaluateRoundCapInnerVertex(PathVertexInfo vertexInfo, ExtrusionFrame frame) {
bool isEnd = vertexInfo.isEnd;
float capSign = isEnd ? 1. : -1.;
float offsetScale = capSign * 0.5;
float subdivision = vertexInfo.subdivision;
float t = 1. - (subdivision + 1.) / pathNumRoundCapExtrusionSubdivisions;
float theta = t * HALF_PI;
float t1 = sin(theta);
float t2 = cos(theta);
int poleIndex = pathProfilePoleIndices[vertexInfo.sourceIndex];
vec4 pole = pathProfilePoles[poleIndex];
vec2 polePosition = pole.xy;
bool hasPoleNormal = !isnan(pole.z);
vec2 profileVertex = pathProfileVertices[vertexInfo.sourceIndex];
vec2 poleOffsetScaled = (profileVertex - polePosition) * t1;
vec2 poleVertex = poleOffsetScaled + polePosition;
vec2 profileNormal = hasPoleNormal
? pole.zw
: normalize(poleOffsetScaled) * t1;
float normalOffset = hasPoleNormal ? 0. : capSign * t2;
return ExtrudedVertex(
frame,
poleVertex,
profileNormal,
vec2(0.),
0.,
offsetScale * t2,
normalOffset,
true
);
}`);
  }
  m2.code.add(t2`
      ExtrudedVertex evaluateVertex() {
        PathVertexInfo vertexInfo = decodePathVertexInfo();
        ExtrusionFrame frame = ExtrusionFrame(
          getFrameUp(),
          getFrameRight()
        );

        switch (vertexInfo.type) {
          case pathVertexTypeJoin:
            return evaluateJoinVertex(vertexInfo, frame);

          case pathVertexTypeCapConnectingProfile:
            return evaluateConnectingVertex(vertexInfo, frame);

          ${n(1 === x2.pathCapType || 2 === x2.pathCapType, t2`
          case pathVertexTypeFlatCapProfile:
            return evaluateFlatCapVertex(vertexInfo, frame);
          `)}

          ${n(3 === x2.pathCapType, t2`
          case pathVertexTypeRoundCapPole:
            return evaluateRoundCapPoleVertex(vertexInfo, frame);
          case pathVertexTypeRoundCapInnerProfile:
            return evaluateRoundCapInnerVertex(vertexInfo, frame);
          `)}

          default:
            return ExtrudedVertex(
              frame,
              vec2(0.),
              vec2(0.),
              vec2(0.),
              0.,
              0.,
              0.,
              false
            );
        }
      }
    `);
}
var c5 = r(NaN, NaN);

// node_modules/@arcgis/core/views/3d/webgl-engine/materials/PathLayouts.js
function o6() {
  const r8 = Q().u32("pathVertexInfo", { integer: true }).u32("textureElementIndex", { integer: true });
  return e2() && r8.vec4u8("olidColor"), r8.freeze();
}
function n5(e13) {
  const t8 = [{ type: "vec3f32", name: "position" }, { type: "vec2snorm16", name: "profileRight" }, { type: "vec2snorm16", name: "profileUp" }];
  return 1 === e13.upVectorAlignment && t8.push({ type: "vec2snorm16", name: "pathRotationUp" }), e13.hasVVSize && t8.push({ type: "f32", name: "sizeFeatureAttribute" }), e13.hasVVColor && t8.push({ type: "f32", name: "colorFeatureAttribute" }), e13.hasVVOpacity && t8.push({ type: "f32", name: "opacityFeatureAttribute" }), t8.push({ type: "f16", name: "pathMaxStretchDistance" }, { type: "snorm16", name: "profileRotation" }), new i3(t8);
}

// node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderLibrary/attributes/PathVertexPosition.glsl.js
var g = 8;
function h3(e13, d6) {
  const { attributes: m2, vertex: h4 } = e13, y2 = new e10("componentTextureBuffer", (e14) => e14.textureBuffer), V = new f3({ layout: n5(d6), itemIndexAttribute: "textureElementIndex", bufferUniform: y2 });
  e13.include(V.TextureBackedBufferModule, d6), m2.add("textureElementIndex", "uint"), h4.uniforms.add(new e8("size", (e14) => e14.size));
  const { hasVVSize: b2, hasVVColor: z, hasVVOpacity: O } = d6;
  b2 ? (h4.uniforms.add(new e4("vvSizeMinSize", (e14) => e14.vvSize.minSize), new e4("vvSizeMaxSize", (e14) => e14.vvSize.maxSize), new e4("vvSizeOffset", (e14) => e14.vvSize.offset), new e4("vvSizeFactor", (e14) => e14.vvSize.factor), new e4("vvSizeFallback", (e14) => e14.vvSize.fallback)), h4.code.add(t2`
    vec2 getSize() {
      float value = ${V.getTextureAttribute("sizeFeatureAttribute")};
      if (isnan(value)) {
        return vvSizeFallback.xz;
      }
      return size * clamp(vvSizeOffset + value * vvSizeFactor, vvSizeMinSize, vvSizeMaxSize).xz;
    }
    `)) : h4.code.add(t2`vec2 getSize(){
return size;
}`), O ? (h4.constants.add("vvOpacityNumber", "int", g), h4.uniforms.add(new r3("vvOpacityValues", g, (e14) => e14.vvOpacity.values), new r3("vvOpacityOpacities", g, (e14) => e14.vvOpacity.opacityValues), new r2("vvOpacityFallback", (e14) => e14.vvOpacity.fallback, { supportsNaN: true })), h4.code.add(t2`
    vec4 applyOpacity(vec4 color) {
      // if we encounter NaN in the color it means the color is in the fallback case where the symbol color
      // is not defined and there is no valid color visual variable override. In this case just return a fully
      // transparent color
      if (isnan(color.r)) {
        return vec4(0);
      }

      float value = ${V.getTextureAttribute("opacityFeatureAttribute")};

      if (isnan(value)) {
        // If there is a color vv then it will already have taken care of applying the fallback
        return ${n(z, "color", "vec4(color.rgb, vvOpacityFallback)")};
      }

      if (value <= vvOpacityValues[0]) {
        return vec4(color.rgb, vvOpacityOpacities[0]);
      }

      for (int i = 1; i < vvOpacityNumber; ++i) {
        if (vvOpacityValues[i] >= value) {
          float f = (value - vvOpacityValues[i-1]) / (vvOpacityValues[i] - vvOpacityValues[i-1]);
          return vec4(color.rgb, mix(vvOpacityOpacities[i-1], vvOpacityOpacities[i], f));
        }
      }

      return vec4( color.rgb, vvOpacityOpacities[vvOpacityNumber - 1]);
    }
    `)) : h4.code.add(t2`vec4 applyOpacity(vec4 color){
return color;
}`), z ? (h4.constants.add("vvColorNumber", "int", t3), h4.uniforms.add(new r3("vvColorValues", t3, (e14) => e14.vvColor.values), new e6("vvColorColors", t3, (e14) => e14.vvColor.colors), new e5("vvColorFallback", (e14) => e14.vvColor.fallback)), h4.code.add(t2`
    vec4 getColor() {
      float value = ${V.getTextureAttribute("colorFeatureAttribute")};
      if (isnan(value)) {
        return applyOpacity(vvColorFallback);
      }

      if (value <= vvColorValues[0]) {
        return applyOpacity(vvColorColors[0]);
      }

      for (int i = 1; i < vvColorNumber; ++i) {
        if (vvColorValues[i] >= value) {
          float f = (value - vvColorValues[i-1]) / (vvColorValues[i] - vvColorValues[i-1]);
          return applyOpacity(mix(vvColorColors[i-1], vvColorColors[i], f));
        }
      }

      return applyOpacity(vvColorColors[vvColorNumber - 1]);
    }
    `)) : h4.code.add(t2`vec4 getColor(){
return applyOpacity(vec4(1, 1, 1, 1));
}`), h4.include(t4), h4.code.add(t2`
    vec3 decompressAxis(vec2 axis) {
      float z = 1.0 - abs(axis.x) - abs(axis.y);
      return normalize(vec3(axis + sign(axis) * min(z, 0.0), z));
    }

    float getProfileRotation() {
      return PI * ${V.getTextureAttribute("profileRotation")};
    }

    float getMaxStretchDistance() {
      return ${V.getTextureAttribute("pathMaxStretchDistance")};
    }

    vec3 getFrameUp() {
      return decompressAxis(${V.getTextureAttribute("profileUp")});
    }

    vec3 getFrameRight() {
      return decompressAxis(${V.getTextureAttribute("profileRight")});
    }
  `), h4.code.add(t2`
    vec3 getFrameRotationUp() {
      return ${1 === d6.upVectorAlignment ? t2`decompressAxis(${V.getTextureAttribute("pathRotationUp")})` : t2`getFrameUp()`};
    }
  `), e13.include(x, d6), h4.code.add(t2`
  vec3 calculateVPos(ExtrudedVertex extrudedVertex) {
    vec2 size = getSize();
    vec3 origin = ${V.getTextureAttribute("position")};
    vec3 right = extrudedVertex.frame.right;
    vec3 up = extrudedVertex.frame.up;
    vec2 profileVertex = extrudedVertex.profileVertex * size;
    `), h4.code.add(t2`if(extrudedVertex.isCap) {
float positionOffsetAlongProfilePlaneNormal = extrudedVertex.capPositionOffset * size[0];
vec3 forward = cross(up, right);
vec3 offset = right * profileVertex.x + up * profileVertex.y + forward * positionOffsetAlongProfilePlaneNormal;
return origin + offset;
}
vec2 rotationRight = extrudedVertex.rotationRight;
float maxDistance = extrudedVertex.maxDistance;`), h4.code.add(t2`rotationRight *= size;
rotationRight = length(rotationRight) > 0.0 ? normalize(rotationRight) : vec2(0, 0);
float rx = dot(profileVertex, rotationRight);
if (abs(rx) > maxDistance) {
vec2 rotationUp = vec2(-rotationRight.y, rotationRight.x);
float ry = dot(profileVertex, rotationUp);
profileVertex = rotationRight * maxDistance * sign(rx) + rotationUp * ry;
}
vec3 offset = right * profileVertex.x + up * profileVertex.y;
return origin + offset;
}`), h4.code.add(t2`vec3 localNormal(ExtrudedVertex extrudedVertex) {
vec3 right = extrudedVertex.frame.right;
vec3 up = extrudedVertex.frame.up;
vec2 profileNormal = extrudedVertex.profileNormal;
vec3 normal = right * profileNormal.x + up * profileNormal.y;
if(extrudedVertex.isCap) {
vec3 forward = cross(up, right);
normal += forward * extrudedVertex.capNormalOffset;
}
return normal;
}`);
}
var y = class extends e3 {
  constructor() {
    super(...arguments), this.numJoinSubdivisions = 1, this.size = e(1, 1), this.cutoffAngle = 0, this.profile = a2.center;
  }
};

// node_modules/@arcgis/core/chunks/Path.glsl.js
function A(A2) {
  const B2 = new s(), { vertex: _, fragment: I, varyings: D } = B2;
  f(_, A2), D.add("vpos", "vec3", { invariant: true }), B2.include(h3, A2);
  const { output: M, spherical: F, pbrMode: k, snowCover: z, offsetBackfaces: E } = A2;
  switch ((p(M) || 11 === M) && (B2.include(o2), B2.include(m, A2), B2.include(d, A2), E && (d2(_, A2), B2.include(c)), D.add("vnormal", "vec3"), D.add("vcolor", "vec4"), _.main.add(t2`
      ExtrudedVertex extrudedVertex = evaluateVertex();
      vpos = calculateVPos(extrudedVertex);
      vnormal = normalize(localNormal(extrudedVertex));
      gl_Position = transformPosition(proj, view, vpos);
      ${n(E, "gl_Position = offsetBackfacingClipPosition(gl_Position, vpos, vnormal, cameraPosition);")}

      forwardObjectAndLayerIdColor();
      vcolor = getColor();
      forwardLinearDepthToReadShadowMap();`)), M) {
    case 0:
    case 1:
    case 2:
      B2.include(n2, A2), I.include(v, A2), I.include(t5, A2), B2.include(r4, A2), I.include(h, A2), B2.include(u, A2), d2(I, A2), f2(I), b(I), I.uniforms.add(_.uniforms.get("localOrigin"), new e4("ambient", (e13) => e13.ambient), new e4("diffuse", (e13) => e13.diffuse), new r2("opacity", (e13) => e13.opacity)), I.include(e7), I.include(e9, A2), o3(I), I.main.add(t2`
        discardBySlice(vpos);

        shadingParams.viewDirection = normalize(vpos - cameraPosition);
        shadingParams.normalView = vnormal;
        vec3 normal = shadingNormal(shadingParams);
        float ssao = evaluateAmbientOcclusionInverse();

        vec3 posWorld = vpos + localOrigin;
        vec3 normalGround = ${F ? "normalize(posWorld);" : "vec3(0.0, 0.0, 1.0);"}

        vec3 albedo = vcolor.rgb * max(ambient, diffuse); // combine the old material parameters into a single one
        float combinedOpacity = vcolor.a * opacity;

        ${n(z, t2`float snow = getSnow(normal, normalGround);
                 albedo = mix(albedo, vec3(1), snow);
                 ssao = mix(ssao, 1.0, snow);`)}

        float additionalAmbientScale = additionalDirectedAmbientLight(posWorld);
        vec3 additionalLight = ssao * mainLightIntensity * additionalAmbientScale * ambientBoostFactor * lightingGlobalFactor;
        float shadow = readShadow(additionalAmbientScale, vpos);

        ${n(2 === k, `float additionalAmbientIrradiance = additionalAmbientIrradianceFactor * mainLightIntensity[2];
           ${n(z, "mrr = applySnowToMRR(mrr, snow);")}`)}

        vec3 shadedColor = ${2 === k ? "evaluateSceneLightingPBR(normal, albedo, shadow, 1.0 - ssao, additionalLight, shadingParams.viewDirection, normalGround, mrr, additionalAmbientIrradiance);" : "evaluateSceneLighting(normal, albedo, shadow, 1.0 - ssao, additionalLight);"}
        vec4 finalColor = vec4(shadedColor, combinedOpacity);
        outputColorHighlightOLID(applySlice(finalColor, vpos), albedo ${n(z, ", snow")});`);
      break;
    case 3:
      B2.include(o2), _.main.add(t2`ExtrudedVertex extrudedVertex = evaluateVertex();
vpos = calculateVPos(extrudedVertex);
gl_Position = transformPosition(proj, view, vpos);`), B2.fragment.include(h, A2), I.main.add(t2`discardBySlice(vpos);`);
      break;
    case 5:
    case 6:
    case 7:
    case 8:
      B2.include(o2), i5(B2), D.add("depth", "float"), _.main.add(t2`ExtrudedVertex extrudedVertex = evaluateVertex();
vpos = calculateVPos(extrudedVertex);
gl_Position = transformPositionWithDepth(proj, view, vpos, nearFar, depth);`), B2.fragment.include(h, A2), B2.include(a, A2), I.main.add(t2`discardBySlice(vpos);
outputDepth(depth);`);
      break;
    case 11:
      B2.fragment.include(h, A2), I.main.add(t2`discardBySlice(vpos);
outputObjectAndLayerIdColor();`);
      break;
    case 4:
      B2.include(o2), B2.include(r5, A2), p2(_), D.add("vnormal", "vec3"), _.main.add(t2`ExtrudedVertex extrudedVertex = evaluateVertex();
vpos = calculateVPos(extrudedVertex);
vnormal = normalize((viewNormal * vec4(localNormal(extrudedVertex), 1.0)).xyz);
gl_Position = transformPosition(proj, view, vpos);`), B2.fragment.include(h, A2), I.main.add(t2`discardBySlice(vpos);
vec3 normal = normalize(vnormal);
if (gl_FrontFacing == false) normal = -normal;
fragColor = vec4(vec3(0.5) + 0.5 * normal, 1.0);`);
      break;
    case 10:
      B2.include(o2), B2.include(r5, A2), D.add("vnormal", "vec3"), _.main.add(t2`ExtrudedVertex extrudedVertex = evaluateVertex();
vpos = calculateVPos(extrudedVertex);
gl_Position = transformPosition(proj, view, vpos);`), B2.fragment.include(h, A2), B2.include(d3, A2), I.main.add(t2`discardBySlice(vpos);
calculateOcclusionAndOutputHighlight();`);
  }
  return B2;
}
var B = Object.freeze(Object.defineProperty({ __proto__: null, build: A }, Symbol.toStringTag, { value: "Module" }));

export {
  c3 as c,
  p3 as p,
  a2 as a,
  c4 as c2,
  o6 as o,
  n5 as n,
  y,
  A,
  B
};
//# sourceMappingURL=chunk-7YP4GMFK.js.map
