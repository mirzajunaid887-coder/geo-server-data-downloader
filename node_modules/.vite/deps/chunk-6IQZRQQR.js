import {
  f as f2,
  g
} from "./chunk-OCDKMBYA.js";
import {
  p,
  y
} from "./chunk-GGR3VIJJ.js";
import {
  l as l3
} from "./chunk-WAJX2EKK.js";
import {
  r as r2
} from "./chunk-BSH5FPFO.js";
import {
  b
} from "./chunk-Z3S576MN.js";
import {
  u
} from "./chunk-KHUEF5LA.js";
import {
  l as l2
} from "./chunk-MKCGKLGO.js";
import {
  S
} from "./chunk-7A5FEBIH.js";
import {
  a3 as a,
  c,
  l
} from "./chunk-Q7QWHSGF.js";
import {
  __decorate
} from "./chunk-SS5CLIUY.js";
import {
  Bt,
  f2 as f
} from "./chunk-S6ISPWGV.js";
import {
  A
} from "./chunk-M3TUHHNI.js";
import {
  n2 as n,
  r3 as r
} from "./chunk-NXFNHCI4.js";

// node_modules/@arcgis/core/layers/support/StreamConnection.js
var n2 = class extends l2 {
  destroy() {
    this.emit("destroy");
  }
  get connectionError() {
    return this.errorString ? new r("stream-connection", this.errorString) : null;
  }
  onFeature(r4) {
    this.emit("data-received", r4);
  }
  onMessage(r4) {
    this.emit("message-received", r4);
  }
};
__decorate([a({ readOnly: true })], n2.prototype, "connectionError", null), n2 = __decorate([c("esri.layers.support.StreamConnection")], n2);

// node_modules/@arcgis/core/layers/graphics/sources/connections/FabricKustoConnection.js
var a2 = class extends n2 {
  constructor(e, t) {
    super(), this._config = e, this._outSpatialReference = t, this.connectionStatus = "connected", this.errorString = null, this._isPolling = false, this._lastIngestedAt = null, this._pollTimer = null, this._pollRecentData = async () => {
      if (!this.destroyed && !this._isPolling) {
        this._isPolling = true;
        try {
          const e2 = await this._queryChanges(this._lastIngestedAt);
          if (this.destroyed) return;
          this._handleResult(e2);
        } catch (e2) {
          this.errorString = e2 instanceof Error ? e2.message : "Failed to fetch";
        } finally {
          this._isPolling = false;
        }
      }
    }, this._initialize();
  }
  destroy() {
    super.destroy(), this._pollTimer && (clearInterval(this._pollTimer), this._pollTimer = null);
  }
  sendMessageToSocket(e) {
    throw new r("kusto:unsupported", "sendMessageToSocket not supported for Kusto connections");
  }
  sendMessageToClient(e) {
    throw new r("kusto:unsupported", "sendMessageToClient not supported for Kusto connections");
  }
  updateCustomParameters(e) {
    throw new r("kusto:unsupported", "updateCustomParameters not supported for Kusto connections");
  }
  async _initialize() {
    await p(this._config.sourceSpatialReference, this._outSpatialReference);
    try {
      const e = await this._queryInitial();
      this._handleResult(e), this._pollTimer = setInterval(this._pollRecentData, 6e4 * this._config.refreshInterval);
    } catch {
      throw new r("kusto:connection", "Failed to query KQL database");
    }
  }
  async _queryInitial() {
    const { trackIdField: e } = this._config, n3 = l4(await this._executeQuery(`${f2(this._config.table)} | extend IngestedAt = ingestion_time() | summarize arg_max(IngestedAt, *) by ${f2(e)}`));
    if (!n3) throw new r("kusto:connection", "Unable to find primaryTable");
    return { ingestedAt: u2(n3), table: n3 };
  }
  async _queryChanges(e = null) {
    const n3 = e ?? "1970-01-01T00:00:00Z", s = l4(await this._executeQuery(`${f2(this._config.table)} | extend IngestedAt = ingestion_time() | where IngestedAt > todatetime('${g(n3)}') | order by IngestedAt asc`));
    if (!s) throw new r("kusto:connection", "Unable to find primaryTable");
    return { ingestedAt: u2(s), table: s };
  }
  async _executeQuery(t) {
    const n3 = this._config.database, s = n3.properties.queryServiceUri, { data: o } = await f(`${s}/v2/rest/query`, { responseType: "json", method: "post", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ db: n3.displayName, csl: t }) });
    return o;
  }
  _handleResult(e) {
    this._lastIngestedAt = e.ingestedAt;
    const t = this._createFeatures(e.table);
    if (null != t) {
      this.onMessage(t);
      for (const e2 of t) this.onFeature(e2);
    }
  }
  _createFeatures(e) {
    if (null == e || 0 === e.Rows.length) return null;
    const { xField: t, yField: n3 } = this._config.geometryEncoding, s = e.Columns.findIndex((e2) => e2.ColumnName === t), o = e.Columns.findIndex((e2) => e2.ColumnName === n3);
    return e.Rows.map((t2) => this._createFeature(e.Columns, s, o, t2));
  }
  _createFeature(e, t, n3, o) {
    const { xField: i, yField: r4 } = this._config.geometryEncoding, a4 = {};
    for (let s = 0; s < e.length; s++) {
      const t2 = e[s], n4 = o[s] ?? null;
      t2.ColumnName !== r4 && t2.ColumnName !== i && (a4[t2.ColumnName] = n4);
    }
    let l6 = null;
    const u3 = o[t], c2 = o[n3];
    return null != c2 && null != u3 && (l6 = y({ x: u3, y: c2 }, this._config.sourceSpatialReference, this._outSpatialReference)), { attributes: a4, geometry: l6 };
  }
};
function l4(e) {
  return e.find((e2) => "DataTable" === e2.FrameType && "PrimaryResult" === e2.TableKind) ?? null;
}
function u2(e) {
  const t = e.Columns.findIndex((e2) => "IngestedAt" === e2.ColumnName);
  if (t < 0) return null;
  let n3 = null;
  for (const s of e.Rows) {
    const e2 = s[t];
    "string" == typeof e2 && (null == n3 || e2 > n3) && (n3 = e2);
  }
  return n3;
}

// node_modules/@arcgis/core/layers/graphics/sources/connections/websocketUils.js
function l5(l6, n3) {
  if (null == l6 && null == n3) return null;
  const u3 = {};
  return null != n3 && (u3.geometry = n3), null != l6 && (u3.where = l6), u3;
}

// node_modules/@arcgis/core/layers/graphics/sources/connections/WebSocketConnection.js
var h = class extends n2 {
  constructor(e, t) {
    super({}), this._outSpatialReference = t, this._outstandingMessages = [], this.errorString = null;
    const { geometryType: o, sourceSpatialReference: s } = e;
    this._config = e, this._featureZScaler = r2(o, s, this._outSpatialReference), this._open();
  }
  normalizeCtorArgs() {
    return {};
  }
  async _open() {
    await this._tryCreateWebSocket(), this.destroyed || await this._handshake();
  }
  destroy() {
    super.destroy(), null != this._websocket && (this._websocket.onopen = null, this._websocket.onclose = null, this._websocket.onerror = null, this._websocket.onmessage = null, this._websocket.close()), this._websocket = null;
  }
  get connectionStatus() {
    if (null == this._websocket) return "disconnected";
    switch (this._websocket.readyState) {
      case 0:
      case 1:
        return "connected";
      case 2:
      case 3:
        return "disconnected";
    }
  }
  sendMessageToSocket(e) {
    null != this._websocket ? this._websocket.send(JSON.stringify(e)) : this._outstandingMessages.push(e);
  }
  sendMessageToClient(e) {
    this._onMessage(e);
  }
  updateCustomParameters(e) {
    this._config.customParameters = e, null != this._websocket && this._websocket.close();
  }
  async _tryCreateWebSocket(e = this._config.url.path, n3 = 1e3, i = 0) {
    try {
      if (this.destroyed) return;
      const t = Bt(e, this._config.customParameters ?? {});
      this._websocket = await this._createWebSocket(t), this.notifyChange("connectionStatus");
    } catch (c2) {
      const r4 = n3 / 1e3;
      return this._config.maxReconnectionAttempts && i >= this._config.maxReconnectionAttempts ? (n.getLogger(this).error(new r("websocket-connection", "Exceeded maxReconnectionAttempts attempts. No further attempts will be made")), void this.destroy()) : (n.getLogger(this).error(new r("websocket-connection", `Failed to connect. Attempting to reconnect in ${r4}s`, c2)), await A(n3), this._tryCreateWebSocket(e, Math.min(1.5 * n3, 1e3 * this._config.maxReconnectionInterval), i + 1));
    }
  }
  _setWebSocketJSONParseHandler(e) {
    e.onmessage = (e2) => {
      try {
        const t = JSON.parse(e2.data);
        this._onMessage(t);
      } catch (s) {
        return void n.getLogger(this).error(new r("websocket-connection", "Failed to parse message, invalid JSON", { error: s }));
      }
    };
  }
  _createWebSocket(e) {
    return new Promise((t, o) => {
      const s = new WebSocket(e);
      s.onopen = () => {
        if (s.onopen = null, this.destroyed) return s.onclose = null, void s.close();
        s.onclose = (e2) => this._onClose(e2), s.onerror = (e2) => this._onError(e2), this._setWebSocketJSONParseHandler(s), t(s);
      }, s.onclose = (e2) => {
        s.onopen = s.onclose = null, o(e2);
      };
    });
  }
  async _handshake(e = 1e4) {
    const s = this._websocket;
    if (null == s) return;
    const r4 = Promise.withResolvers(), n3 = s.onmessage, i = l5(this._config.definitionExpression, this._config.geometryDefinition), { outFields: c2 } = this._config, l6 = setTimeout(() => r4.reject(), e), h3 = () => {
      clearTimeout(l6), r4.reject();
    };
    return s.onmessage = (e2) => {
      var _a, _b;
      let a4 = null;
      try {
        a4 = JSON.parse(e2.data);
      } catch (d2) {
      }
      a4 && "object" == typeof a4 || (n.getLogger(this).error(new r("websocket-connection", "Protocol violation. Handshake failed - malformed message", e2.data)), h3(), this.destroy()), ((_a = a4.spatialReference) == null ? void 0 : _a.wkid) !== ((_b = this._outSpatialReference) == null ? void 0 : _b.wkid) && (n.getLogger(this).error(new r("websocket-connection", `Protocol violation. Handshake failed - expected wkid of ${this._outSpatialReference.wkid}`, e2.data)), h3(), this.destroy()), "json" !== a4.format && (n.getLogger(this).error(new r("websocket-connection", "Protocol violation. Handshake failed - format is not set", e2.data)), h3(), this.destroy()), i && a4.filter !== i && n.getLogger(this).error(new r("websocket-connection", "Tried to set filter, but server doesn't support it")), c2 && a4.outFields !== c2 && n.getLogger(this).error(new r("websocket-connection", "Tried to set outFields, but server doesn't support it")), s.onmessage = n3;
      for (const t of this._outstandingMessages) s.send(JSON.stringify(t));
      this._outstandingMessages = [], clearTimeout(l6), r4.resolve();
    }, s.send(JSON.stringify({ filter: i, outFields: c2, format: "json", spatialReference: { wkid: this._outSpatialReference.wkid } })), r4.promise;
  }
  _onMessage(e) {
    if (this.onMessage(e), "type" in e) switch (e.type) {
      case "features":
      case "featureResult":
        for (const t of e.features) null != this._featureZScaler && this._featureZScaler(t.geometry), this.onFeature(t);
    }
  }
  _onError(e) {
    const t = "Encountered an error over WebSocket connection";
    this._set("errorString", t), n.getLogger(this).error("websocket-connection", t);
  }
  _onClose(e) {
    this._websocket = null, this.notifyChange("connectionStatus"), 1e3 !== e.code && n.getLogger(this).error("websocket-connection", `WebSocket closed unexpectedly with error code ${e.code}`), this.destroyed || this._open();
  }
};
__decorate([a()], h.prototype, "connectionStatus", null), __decorate([a()], h.prototype, "errorString", void 0), h = __decorate([c("esri.layers.graphics.sources.connections.WebSocketConnection")], h);

// node_modules/@arcgis/core/layers/graphics/sources/connections/GeoEventConnection.js
var h2 = 1e4;
var d = { maxQueryDepth: 5, maxRecordCountFactor: 3 };
var g2 = class extends h {
  constructor(e, t) {
    super({ ...d, ...e }, t), this._buddyServicesQuery = null, this._relatedFeatures = null;
  }
  async _open() {
    const e = await this._fetchServiceDefinition(this._config.url);
    e.timeInfo.trackIdField || n.getLogger(this).warn("GeoEvent service was configured without a TrackIdField. This may result in certain functionality being disabled. The purgeOptions.maxObservations property will have no effect.");
    const t = this._fetchWebSocketUrl(e.streamUrls, this._outSpatialReference);
    this._buddyServicesQuery || (this._buddyServicesQuery = this._queryBuddyServices()), await this._buddyServicesQuery, await this._tryCreateWebSocket(t);
    const r4 = l5(this._config.definitionExpression, this._config.geometryDefinition), { outFields: o } = this._config;
    this.destroyed || this._setFilter(r4, o);
  }
  _onMessage(e) {
    if ("attributes" in e) {
      let o;
      try {
        o = this._enrich(e), null != this._featureZScaler && this._featureZScaler(o.geometry);
      } catch (t) {
        return void n.getLogger(this).error(new r("geoevent-connection", "Failed to parse message", t));
      }
      this.onFeature(o);
    } else this.onMessage(e);
  }
  async _fetchServiceDefinition(e) {
    const r4 = { f: "json", ...this._config.customParameters }, i = f(e.path, { query: r4, responseType: "json" }), o = (await i).data;
    return this._serviceDefinition = o, o;
  }
  _fetchWebSocketUrl(e, t) {
    const r4 = e[0], { urls: i, token: s } = r4, n3 = this._inferWebSocketBaseUrl(i);
    return Bt(`${n3}/subscribe`, { outSR: "" + t.wkid, token: s });
  }
  _inferWebSocketBaseUrl(e) {
    if (1 === e.length) return e[0];
    for (const t of e) if (t.includes("wss")) return t;
    return n.getLogger(this).error(new r("geoevent-connection", "Unable to infer WebSocket url", e)), null;
  }
  async _setFilter(e, t) {
    const o = this._websocket;
    if (null == o || null == e && null == t) return;
    const s = JSON.stringify({ filter: this._serializeFilter(e, t) });
    let n3 = false;
    const a4 = Promise.withResolvers(), c2 = () => {
      n3 || (this.destroyed || this._websocket !== o || n.getLogger(this).error(new r("geoevent-connection", "Server timed out when setting filter")), a4.reject());
    }, u3 = (e2) => {
      const t2 = JSON.parse(e2.data);
      t2.filter && (t2.error && (n.getLogger(this).error(new r("geoevent-connection", "Failed to set service filter", t2.error)), this._set("errorString", `Could not set service filter - ${t2.error}`), a4.reject(t2.error)), this._setWebSocketJSONParseHandler(o), n3 = true, a4.resolve());
    };
    return o.onmessage = u3, o.send(s), setTimeout(c2, h2), a4.promise;
  }
  _serializeFilter(e, t) {
    const o = {};
    if (null == e && null == t) return o;
    if (e == null ? void 0 : e.geometry) try {
      const t2 = u(e.geometry);
      if ("extent" !== t2.type) throw new r("geoevent-connection", `Expected extent but found type ${t2.type}`);
      o.geometry = JSON.stringify(t2.shiftCentralMeridian());
    } catch (s) {
      n.getLogger(this).error(new r("geoevent-connection", "Encountered an error when setting connection geometryDefinition", s));
    }
    return (e == null ? void 0 : e.where) && "1 = 1" !== e.where && "1=1" !== e.where && (o.where = e.where), null != t && (o.outFields = t.join(",")), o;
  }
  _enrich(e) {
    if (!this._relatedFeatures) return e;
    const t = this._serviceDefinition.relatedFeatures.joinField, o = e.attributes[t], s = this._relatedFeatures.get(o);
    if (!s) return n.getLogger(this).warn("geoevent-connection", "Feature join failed. Is the join field configured correctly?", e), e;
    const { attributes: n3, geometry: a4 } = s;
    for (const r4 in n3) e.attributes[r4] = n3[r4];
    return a4 && (e.geometry = a4), e.geometry || e.centroid || n.getLogger(this).error(new r("geoevent-connection", "Found malformed feature - no geometry found", e)), e;
  }
  async _queryBuddyServices() {
    try {
      const { relatedFeatures: e, keepLatestArchive: t } = this._serviceDefinition, r4 = this._queryRelatedFeatures(e), i = this._queryArchive(t);
      await r4;
      const o = await i;
      if (!o) return;
      for (const s of o.features) this.onFeature(this._enrich(s));
    } catch (e) {
      n.getLogger(this).error(new r("geoevent-connection", "Encountered an error when querying buddy services", { error: e }));
    }
  }
  async _queryRelatedFeatures(e) {
    if (!e) return;
    const t = await this._queryBuddy(e.featuresUrl);
    this._addRelatedFeatures(t);
  }
  async _queryArchive(e) {
    if (e) return this._queryBuddy(e.featuresUrl);
  }
  async _queryBuddy(e) {
    const t = new (await import("./@arcgis_core_layers_FeatureLayer.js")).default({ url: e }), { capabilities: r4 } = await t.load(), i = r4.query.supportsMaxRecordCountFactor, o = r4.query.supportsPagination, s = r4.query.supportsCentroid, a4 = this._config.maxRecordCountFactor, c2 = t.capabilities.query.maxRecordCount, u3 = i ? c2 * a4 : c2, h3 = new b();
    if (h3.outFields = this._config.outFields ?? ["*"], h3.where = this._config.definitionExpression ?? "1=1", h3.returnGeometry = true, h3.returnExceededLimitFeatures = true, h3.outSpatialReference = S.fromJSON(this._outSpatialReference), s && (h3.returnCentroid = true), i && (h3.maxRecordCountFactor = a4), o) return h3.num = u3, t.destroy(), this._queryPages(e, h3);
    const d2 = await l3(e, h3, this._config.sourceSpatialReference);
    return t.destroy(), d2;
  }
  async _queryPages(e, t, r4 = [], i = 0) {
    t.start = null != t.num ? i * t.num : null;
    const o = await l3(e, t, this._config.sourceSpatialReference);
    return o.exceededTransferLimit && i < (this._config.maxQueryDepth ?? 0) ? (o.features.forEach((e2) => r4.push(e2)), this._queryPages(e, t, r4, i + 1)) : (r4.forEach((e2) => o.features.push(e2)), o);
  }
  _addRelatedFeatures(e) {
    const t = /* @__PURE__ */ new Map(), r4 = e.features, i = this._serviceDefinition.relatedFeatures.joinField;
    for (const o of r4) {
      const e2 = o.attributes[i];
      t.set(e2, o);
    }
    this._relatedFeatures = t;
  }
};
g2 = __decorate([c("esri.layers.graphics.sources.connections.GeoEventConnection")], g2);

// node_modules/@arcgis/core/layers/support/ClientSideConnection.js
var a3 = class extends n2 {
  constructor(e, t) {
    super({}), this.connectionStatus = "connected", this.errorString = null;
    const { geometryType: r4, sourceSpatialReference: s } = e;
    this._featureZScaler = r2(r4, s, t);
  }
  normalizeCtorArgs() {
    return {};
  }
  updateCustomParameters(e) {
  }
  sendMessageToSocket(e) {
  }
  sendMessageToClient(e) {
    if ("type" in e) switch (e.type) {
      case "features":
      case "featureResult":
        for (const t of e.features) null != this._featureZScaler && this._featureZScaler(t.geometry), this.onFeature(t);
    }
    this.onMessage(e);
  }
};
__decorate([a()], a3.prototype, "connectionStatus", void 0), __decorate([a()], a3.prototype, "errorString", void 0), a3 = __decorate([l("esri.layers.support.ClientSideConnection")], a3);

// node_modules/@arcgis/core/layers/graphics/sources/connections/createConnection.js
function r3(r4, c2) {
  const i = c2.toJSON();
  switch (r4.type) {
    case "client":
      return new a3(r4, i);
    case "custom":
      return new h(r4, i);
    case "geoevent":
      return new g2(r4, i);
    case "kusto":
      return new a2(r4, i);
  }
}

export {
  r3 as r
};
//# sourceMappingURL=chunk-6IQZRQQR.js.map
