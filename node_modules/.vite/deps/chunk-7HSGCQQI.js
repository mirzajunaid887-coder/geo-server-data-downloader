import {
  m,
  y
} from "./chunk-DX52QORO.js";

// node_modules/@arcgis/core/geometry/support/Indices.js
function t(r) {
  return e(r, true);
}
function e(t2, e2) {
  if (Array.isArray(t2)) {
    if (t2.length < y) return t2;
  } else if (t2.length < y) return Array.from(t2);
  let i = true, u2 = true;
  return t2.some((r, n) => (i = i && 0 === r, u2 = u2 && r === n, !i && !u2)), i ? w(t2.length) : u2 ? h(t2.length) : m(t2) && t2.BYTES_PER_ELEMENT === Uint16Array.BYTES_PER_ELEMENT ? t2 : f(t2, !e2);
}
function f(r, t2) {
  for (const e2 of r) {
    if (e2 >= 65536) return m(r) ? r : new Uint32Array(r);
    e2 >= 256 && (t2 = false);
  }
  return t2 ? new Uint8Array(r) : new Uint16Array(r);
}
function u(n, t2 = n) {
  return n <= y ? new Array(n) : t2 <= 65536 ? new Uint16Array(n) : new Uint32Array(n);
}
function l(r, n, t2) {
  if (0 === n.length) return r.slice();
  if (0 === r.length) return n.slice();
  const e2 = u(r.length + n.length, t2);
  for (let i = 0; i < r.length; ++i) e2[i] = r[i];
  for (let i = 0; i < n.length; ++i) e2[r.length + i] = n[i];
  return e2;
}
var o = c(131072);
var y2 = [0];
var A = (() => {
  const r = new Uint16Array(65536);
  for (let n = 0; n < r.length; ++n) r[n] = n;
  return r;
})();
function h(n) {
  return 1 === n ? y2 : n < y ? Array.from(new Uint16Array(A.buffer, 0, n)) : n < A.length ? new Uint16Array(A.buffer, 0, n) : (n > o.length && (o = c(Math.max(2 * o.length, n))), new Uint32Array(o.buffer, 0, n));
}
function c(r) {
  const n = new Uint32Array(r);
  for (let t2 = 0; t2 < n.length; t2++) n[t2] = t2;
  return n;
}
var U = new Uint8Array(65536);
function w(n) {
  if (1 === n) return y2;
  if (n < y) return new Array(n).fill(0);
  if (n > U.length) {
    const r = Math.max(2 * U.length, n);
    U = new Uint8Array(r);
  }
  return new Uint8Array(U.buffer, 0, n);
}

export {
  t,
  e,
  u,
  l,
  h,
  w
};
//# sourceMappingURL=chunk-7HSGCQQI.js.map
