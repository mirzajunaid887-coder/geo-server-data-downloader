import {
  p
} from "./chunk-QE4GZQDJ.js";
import {
  tn
} from "./chunk-2TXEZV2I.js";
import {
  n as n2
} from "./chunk-WT4NB7ZZ.js";
import {
  r
} from "./chunk-ATF6IPXT.js";
import {
  n2 as n
} from "./chunk-NXFNHCI4.js";

// node_modules/@arcgis/core/views/3d/layers/SceneLayerWorkerHandle.js
var r2 = class {
  constructor(s, t, e, o, i, n3) {
    this.layout = s, this.interleavedVertexData = t, this.indices = e, this.hasColors = o, this.hasModifications = i, this.positionData = n3;
  }
};
var h = class {
  constructor(s, t, e, o, i, n3, r3) {
    this.componentOffsets = s, this.featureIds = t, this.anchorIds = e, this.anchors = o, this.transformedGeometry = i, this.globalTrafo = n3, this.obb = r3;
  }
};
var c = new r({ deallocator: null });
var p2 = n2();
function u(t, e, o) {
  c.clear();
  let r3 = 1 / 0, h2 = 1 / 0, a2 = -1 / 0, u2 = -1 / 0, l2 = false;
  for (const m of e) {
    const t2 = "clip" === m.type ? 2 : "mask" === m.type ? 1 : 0, e2 = m.geometry;
    let f2 = (s) => s;
    if (e2.spatialReference) {
      if (!tn(e2.spatialReference, o)) {
        n.getLogger("esri.views.3d.layers.SceneLayerWorkerHandle").warn("im-modification-projection-failed", "Can't project modification polygon into layer spatial reference, ignoring modification", { polygon: e2 });
        continue;
      }
      f2 = (s) => (p(s, e2.spatialReference, p2, o), p2);
    }
    l2 = l2 || 1 === t2;
    const g = e2.rings.length, d = e2.rings.some((s) => s.length < 3);
    if (0 === g || d) n.getLogger("esri.views.3d.layers.SceneLayerWorkerHandle").warn("im-modification-invalid-polygon", "Ignoring invalid modification polygon (no rings or rings with less than 3 vertices).", { polygon: e2 });
    else {
      c.push(t2), c.push(g);
      for (const s of e2.rings) {
        c.push(s.length);
        for (const t3 of s) {
          const s2 = f2(t3);
          c.push(s2[0]), c.push(s2[1]), c.push(s2[2] ?? 0), r3 = Math.min(r3, s2[0]), h2 = Math.min(h2, s2[1]), a2 = Math.max(a2, s2[0]), u2 = Math.max(u2, s2[1]);
        }
      }
    }
  }
  if (null != t) if (l2) {
    const s = 1e-4;
    c.push(2), c.push(2), c.push(4), c.push(r3 - s), c.push(h2 - s), c.push(0), c.push(a2 + s), c.push(h2 - s), c.push(0), c.push(a2 + s), c.push(u2 + s), c.push(0), c.push(r3 - s), c.push(u2 + s), c.push(0), c.push(4), c.push(t[0]), c.push(t[1]), c.push(0), c.push(t[2]), c.push(t[1]), c.push(0), c.push(t[2]), c.push(t[3]), c.push(0), c.push(t[0]), c.push(t[3]), c.push(0);
  } else c.push(1), c.push(1), c.push(4), c.push(t[0]), c.push(t[1]), c.push(0), c.push(t[2]), c.push(t[1]), c.push(0), c.push(t[2]), c.push(t[3]), c.push(0), c.push(t[0]), c.push(t[3]), c.push(0);
  c.push(3);
  const f = new Float64Array(c.length);
  for (let s = 0; s < c.length; ++s) f[s] = c.at(s);
  return f;
}
function l(s, t) {
  let e = 0;
  for (const o of t) {
    const t2 = o.geometry, i = t2.rings.length, n3 = t2.rings.some((s2) => s2.length < 3);
    if (0 !== i && !n3) {
      e += 1, e += 1;
      for (let o2 = 0; o2 < t2.rings.length; ++o2) {
        const i2 = t2.rings[o2];
        e += 1;
        for (let n4 = 0; n4 < i2.length; ++n4) {
          const i3 = [s[e + 0], s[e + 1], s[e + 2]];
          e += 3, t2.setPoint(o2, n4, i3);
        }
      }
    }
  }
}

export {
  r2 as r,
  h,
  u,
  l
};
//# sourceMappingURL=chunk-LAEWXV3J.js.map
