import {
  r as r7
} from "./chunk-FLXJ4GV5.js";
import {
  a as a2,
  d2 as d3,
  e as e7,
  f as f2,
  i2 as i,
  i3 as i2,
  p as p2,
  r as r5,
  s as s3,
  t as t3,
  t2 as t4,
  t3 as t6
} from "./chunk-ZB3O4R3Q.js";
import {
  b,
  c,
  e as e8,
  f as f3,
  n as n4,
  t2 as t5,
  v
} from "./chunk-7NL6FKO3.js";
import {
  a,
  f as f4,
  m
} from "./chunk-RL2NNJL6.js";
import {
  n as n3
} from "./chunk-NSRUD46G.js";
import {
  r as r6
} from "./chunk-DDSKY4JI.js";
import {
  o as o2
} from "./chunk-APFTN4C3.js";
import {
  o as o3
} from "./chunk-WQFSH25T.js";
import {
  l
} from "./chunk-BZNEURM7.js";
import {
  n as n2
} from "./chunk-CSZKF7UB.js";
import {
  l as l2,
  r as r3,
  u
} from "./chunk-CAZJRW52.js";
import {
  t as t2
} from "./chunk-I2ORUFDN.js";
import {
  h
} from "./chunk-RZ3N43WD.js";
import {
  d,
  f,
  o,
  u as u2
} from "./chunk-QWT5D5LM.js";
import {
  e as e5
} from "./chunk-B6MIGAJF.js";
import {
  d2,
  p,
  r2 as r4
} from "./chunk-DI4PQ4KF.js";
import {
  e as e6
} from "./chunk-YVHZBCEM.js";
import {
  e as e4
} from "./chunk-SGFXS57V.js";
import {
  r as r2
} from "./chunk-URRK3ZOJ.js";
import {
  e as e2
} from "./chunk-VKNLVFIM.js";
import {
  e as e3
} from "./chunk-FY3YFVEV.js";
import {
  e
} from "./chunk-5FRKFKJQ.js";
import {
  s as s2
} from "./chunk-BCEJLVA4.js";
import {
  n,
  t
} from "./chunk-NYA5CJQN.js";
import {
  s
} from "./chunk-S2HMSOEZ.js";
import {
  r
} from "./chunk-FRE56XDD.js";

// node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderLibrary/shading/ComputeNormalTexture.glsl.js
function l3(e9, t7) {
  return i3(e9, t7);
}
function i3(l4, c2) {
  const i4 = l4.fragment, { hasVertexTangents: u3, doubleSidedMode: x, hasNormalTexture: T, textureCoordinateType: g, bindType: f5, hasNormalTextureTransform: v2 } = c2;
  u3 ? (l4.attributes.add("tangent", "vec4"), l4.varyings.add("vTangent", "vec4"), 2 === x ? i4.code.add(t`mat3 computeTangentSpace(vec3 normal) {
float tangentHeadedness = gl_FrontFacing ? vTangent.w : -vTangent.w;
vec3 tangent = normalize(gl_FrontFacing ? vTangent.xyz : -vTangent.xyz);
vec3 bitangent = cross(normal, tangent) * tangentHeadedness;
return mat3(tangent, bitangent, normal);
}`) : i4.code.add(t`mat3 computeTangentSpace(vec3 normal) {
float tangentHeadedness = vTangent.w;
vec3 tangent = normalize(vTangent.xyz);
vec3 bitangent = cross(normal, tangent) * tangentHeadedness;
return mat3(tangent, bitangent, normal);
}`)) : i4.code.add(t`mat3 computeTangentSpace(vec3 normal, vec3 pos, vec2 st) {
vec3 Q1 = dFdx(pos);
vec3 Q2 = dFdy(pos);
vec2 stx = dFdx(st);
vec2 sty = dFdy(st);
float det = stx.t * sty.s - sty.t * stx.s;
vec3 T = stx.t * Q2 - sty.t * Q1;
T = T - normal * dot(normal, T);
T *= inversesqrt(max(dot(T,T), 1.e-10));
vec3 B = sign(det) * cross(normal, T);
return mat3(T, B, normal);
}`), T && 0 !== g && (l4.include(r4, c2), i4.uniforms.add(1 === f5 ? new e2("normalTexture", (e9) => e9.textureNormal) : new e3("normalTexture", (e9) => e9.textureNormal)), v2 && (i4.uniforms.add(1 === f5 ? new e6("scale", (e9) => e9.scale ?? s) : new e("scale", (e9) => e9.scale ?? s)), i4.uniforms.add(new t2("normalTextureTransformMatrix", (t7) => t7.normalTextureTransformMatrix ?? r))), i4.code.add(t`vec3 computeTextureNormal(mat3 tangentSpace, vec2 uv) {
vec3 rawNormal = textureLookup(normalTexture, uv).rgb * 2.0 - 1.0;`), v2 && i4.code.add(t`mat3 normalRotation = mat3(normalTextureTransformMatrix[0][0]/scale[0], normalTextureTransformMatrix[0][1]/scale[1], 0.0,
normalTextureTransformMatrix[1][0]/scale[0], normalTextureTransformMatrix[1][1]/scale[1], 0.0,
0.0, 0.0, 0.0 );
rawNormal.xy = (normalRotation * vec3(rawNormal.x, rawNormal.y, 1.0)).xy;`), i4.code.add(t`return tangentSpace * rawNormal;
}`));
}

// node_modules/@arcgis/core/chunks/DefaultMaterial.glsl.js
function q(q2) {
  const J2 = new s2(), { attributes: K, vertex: Q, fragment: X, varyings: Y } = J2, { output: Z, normalType: ee, offsetBackfaces: re, spherical: oe, snowCover: ie, pbrMode: ae, textureAlphaPremultiplied: le, instancedDoublePrecision: se, hasVertexColors: ne, hasVertexTangents: te, hasColorTexture: de, hasNormalTexture: ce, hasNormalTextureTransform: me, hasColorTextureTransform: ge } = q2;
  if (f(Q, q2), K.add("position", "vec3"), Q.inputs.add("position", () => "position"), Y.add("vpos", "vec3", { invariant: true }), J2.include(u, q2), J2.include(p2, q2), J2.include(n3, q2), J2.include(a2, q2), !p(Z)) return J2.include(f2, q2), J2;
  J2.include(s3, q2), J2.include(i2, q2), J2.include(d3, q2), J2.include(t6, q2), d(Q, q2), J2.include(r5, q2), J2.include(o2);
  const ue = 0 === ee || 1 === ee;
  return ue && re && J2.include(c), J2.include(l3, q2), J2.include(t3, q2), J2.include(t4, q2), Y.add("vPositionLocal", "vec3"), J2.include(d2, q2), J2.include(i, q2), J2.include(r6, q2), Q.uniforms.add(new e5("externalColor", (e9) => e9.externalColor, { supportsNaN: true })), Y.add("vcolorExt", "vec4"), Q.include(r3), Q.include(l2), J2.include(se ? f4 : m, q2), Q.main.add(t`
    forwardVertexColor();

    MaskedColor maskedColor =
      applySymbolColor(applyVVColor(applyInstanceColor(createMaskedFromNaNColor(externalColor))));

    vcolorExt = maskedColor.color;
    forwardColorMixMode(maskedColor.mask);

    vpos = getVertexInLocalOriginSpace();
    vPositionLocal = vpos - view[3].xyz;
    vpos = subtractOrigin(vpos);
    ${n(ue, "vNormalWorld = dpNormal(vvLocalNormal(normalModel()));")}
    vpos = addVerticalOffset(vpos, localOrigin);
    ${n(te, "vTangent = dpTransformVertexTangent(tangent);")}
    gl_Position = transformPosition(proj, view, vpos);
    ${n(ue && re, "gl_Position = offsetBackfacingClipPosition(gl_Position, vpos, vNormalWorld, cameraPosition);")}

    forwardTextureCoordinates();
    forwardColorUV();
    forwardNormalUV();
    forwardEmissiveUV();
    forwardOcclusionUV();
    forwardMetallicRoughnessUV();

    if (opacityMixMode != ${t.int(l.ignore)} && vcolorExt.a < ${t.float(o)}) {
      gl_Position = vec4(1e38, 1e38, 1e38, 1.0);
    }
    forwardLinearDepthToReadShadowMap();
  `), X.include(v, q2), X.include(t5, q2), J2.include(e7, q2), X.include(h, q2), J2.include(u2, q2), d(X, q2), X.uniforms.add(Q.uniforms.get("localOrigin"), new e4("ambient", (e9) => e9.ambient), new e4("diffuse", (e9) => e9.diffuse), new r2("opacity", (e9) => e9.opacity), new r2("layerOpacity", (e9) => e9.layerOpacity)), de && X.uniforms.add(new e2("tex", (e9) => e9.texture)), J2.include(n4, q2), X.include(a, q2), X.include(n2), J2.include(r7, q2), X.include(e8, q2), f3(X), b(X), o3(X), X.main.add(t`
    discardBySlice(vpos);
    ${de ? t`
            vec4 texColor = texture(tex, ${ge ? "colorUV" : "vuv0"});
            ${n(le, "texColor.rgb /= texColor.a;")}
            discardOrAdjustAlpha(texColor);` : t`vec4 texColor = vec4(1.0);`}
    shadingParams.viewDirection = normalize(vpos - cameraPosition);
    ${2 === ee ? t`vec3 normal = screenDerivativeNormal(vPositionLocal);` : t`shadingParams.normalView = vNormalWorld;
                vec3 normal = shadingNormal(shadingParams);`}
    applyPBRFactors();
    float ssao = evaluateAmbientOcclusionInverse() * getBakedOcclusion();

    vec3 posWorld = vpos + localOrigin;

    float additionalAmbientScale = additionalDirectedAmbientLight(posWorld);
    float shadow = readShadow(additionalAmbientScale, vpos);

    vec3 matColor = max(ambient, diffuse);
    vec3 albedo = mixExternalColor(${n(ne, "vColor.rgb *")} matColor, texColor.rgb, vcolorExt.rgb, colorMixMode);
    float opacity_ = layerOpacity * mixExternalOpacity(${n(ne, "vColor.a * ")} opacity, texColor.a, vcolorExt.a, opacityMixMode);

    ${ce ? `mat3 tangentSpace = computeTangentSpace(${te ? "normal" : "normal, vpos, vuv0"});
           vec3 shadingNormal = computeTextureNormal(tangentSpace, ${me ? "normalUV" : "vuv0"});` : "vec3 shadingNormal = normal;"}
    vec3 normalGround = ${oe ? "normalize(posWorld);" : "vec3(0.0, 0.0, 1.0);"}

    ${n(ie, t`
          float snow = getSnow(normal, normalGround);
          albedo = mix(albedo, vec3(1), snow);
          shadingNormal = mix(shadingNormal, normal, snow);
          ssao = mix(ssao, 1.0, snow);`)}

    vec3 additionalLight = ssao * mainLightIntensity * additionalAmbientScale * ambientBoostFactor * lightingGlobalFactor;

    ${1 === ae || 2 === ae ? t`
            float additionalAmbientIrradiance = additionalAmbientIrradianceFactor * mainLightIntensity[2];
            ${n(ie, "mrr = applySnowToMRR(mrr, snow);")}
            vec3 shadedColor = evaluateSceneLightingPBR(shadingNormal, albedo, shadow, 1.0 - ssao, additionalLight, shadingParams.viewDirection, normalGround, mrr, additionalAmbientIrradiance);` : t`vec3 shadedColor = evaluateSceneLighting(shadingNormal, albedo, shadow, 1.0 - ssao, additionalLight);`}
    vec4 finalColor = vec4(shadedColor, opacity_);
    outputColorHighlightOLID(applySlice(finalColor, vpos), albedo ${n(ie, ", snow")});
  `), J2;
}
var J = Object.freeze(Object.defineProperty({ __proto__: null, build: q }, Symbol.toStringTag, { value: "Module" }));

export {
  q,
  J
};
//# sourceMappingURL=chunk-SI4HPK6K.js.map
