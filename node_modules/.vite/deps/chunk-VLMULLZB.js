import {
  d as d3,
  e as e4,
  f as f2,
  i2 as i
} from "./chunk-RWQKQ2RJ.js";
import {
  s as s2
} from "./chunk-WQUJR66M.js";
import {
  n as n5
} from "./chunk-CSZKF7UB.js";
import {
  t as t4
} from "./chunk-XJEXS547.js";
import {
  d
} from "./chunk-F6VYKY5P.js";
import {
  h
} from "./chunk-RZ3N43WD.js";
import {
  a as a2,
  d as d2,
  e as e3,
  f,
  u as u2,
  w
} from "./chunk-QWT5D5LM.js";
import {
  e as e2
} from "./chunk-B6MIGAJF.js";
import {
  e as e7
} from "./chunk-YVHZBCEM.js";
import {
  t as t3
} from "./chunk-NTM3SHYX.js";
import {
  r as r2
} from "./chunk-7BJKTGZL.js";
import {
  e as e6
} from "./chunk-HYIKO77H.js";
import {
  e as e5
} from "./chunk-UEK4F7VK.js";
import {
  r
} from "./chunk-URRK3ZOJ.js";
import {
  e
} from "./chunk-VKNLVFIM.js";
import {
  s
} from "./chunk-BCEJLVA4.js";
import {
  n as n4,
  t as t2
} from "./chunk-NYA5CJQN.js";
import {
  n as n2
} from "./chunk-S2HMSOEZ.js";
import {
  a,
  n as n3
} from "./chunk-I5UAHQ4J.js";
import {
  o
} from "./chunk-5BY2LZNH.js";
import {
  n
} from "./chunk-GZLC55W4.js";
import {
  u
} from "./chunk-OSHDQB3V.js";
import {
  t
} from "./chunk-HGT5JKWU.js";

// node_modules/@arcgis/core/views/3d/webgl-engine/materials/lineStippleUtils.js
var o2 = { dash: [4, 3], dot: [1, 3], "long-dash": [8, 3], "short-dash": [4, 1], "short-dot": [1, 1] };
var s3 = { dash: o2.dash, "dash-dot": [...o2.dash, ...o2.dot], dot: o2.dot, "long-dash": o2["long-dash"], "long-dash-dot": [...o2["long-dash"], ...o2.dot], "long-dash-dot-dot": [...o2["long-dash"], ...o2.dot, ...o2.dot], none: null, "short-dash": o2["short-dash"], "short-dash-dot": [...o2["short-dash"], ...o2["short-dot"]], "short-dash-dot-dot": [...o2["short-dash"], ...o2["short-dot"], ...o2["short-dot"]], "short-dot": o2["short-dot"], solid: null };
var d4 = 8;
var n6 = class {
  constructor(o3, s4, d6) {
    this.image = o3, this.width = s4, this.length = d6, this.uuid = n();
  }
};
function h2(t7) {
  return null != t7 && "image" in t7;
}
function r3(t7, o3) {
  return null == t7 ? t7 : { pattern: t7.slice(), pixelRatio: o3 };
}
function l(t7) {
  return { pattern: [t7, t7], pixelRatio: 2 };
}
function a3(t7) {
  switch (t7 == null ? void 0 : t7.type) {
    case "style":
      return e8(t7.style);
    case "image":
      return new n6(t7.image, t7.width, t7.length);
    case void 0:
    case null:
      return null;
  }
  return null;
}
function e8(t7) {
  return null != t7 ? r3(s3[t7], d4) : null;
}

// node_modules/@arcgis/core/views/3d/webgl-engine/materials/stippleTextureRepository.js
function p(t7) {
  return t7.pattern.map((e11) => Math.round(e11 * t7.pixelRatio));
}
function l2(t7) {
  if (null == t7) return 1;
  const e11 = p(t7);
  return Math.floor(e11.reduce((t8, e12) => t8 + e12));
}

// node_modules/@arcgis/core/views/3d/webgl-engine/shaders/ensureColor4.js
function e10(o3) {
  return null == o3 ? a : 4 === o3.length ? o3 : o(i2, o3[0], o3[1], o3[2], 1);
}
var i2 = n3();

// node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderLibrary/shading/LineStipple.glsl.js
function m(d6, c) {
  if (!c.stippleEnabled) return void d6.fragment.code.add(t2`float getStippleAlpha(float lineWidth) { return 1.0; }
void discardByStippleAlpha(float stippleAlpha, float threshold) {}
vec4 blendStipple(vec4 color, float stippleAlpha) { return color; }`);
  const m2 = !(c.draped && c.stipplePreferContinuous), { vertex: u3, fragment: v } = d6;
  c.draped || (d2(u3, c), u3.uniforms.add(new r2("worldToScreenPerDistanceRatio", ({ camera: e11 }) => 1 / e11.perScreenPixelRatio)).code.add(t2`float computeWorldToScreenRatio(vec3 segmentCenter) {
float segmentDistanceToCamera = length(segmentCenter - cameraPosition);
return worldToScreenPerDistanceRatio / segmentDistanceToCamera;
}`)), d6.varyings.add("vStippleDistance", "float"), d6.varyings.add("vStippleDistanceLimits", "vec2"), d6.varyings.add("vStipplePatternStretch", "float"), u3.code.add(t2`
    float discretizeWorldToScreenRatio(float worldToScreenRatio) {
      float step = ${t2.float(g)};

      float discreteWorldToScreenRatio = log(worldToScreenRatio);
      discreteWorldToScreenRatio = ceil(discreteWorldToScreenRatio / step) * step;
      discreteWorldToScreenRatio = exp(discreteWorldToScreenRatio);
      return discreteWorldToScreenRatio;
    }
  `), w(u3), u3.code.add(t2`
    vec2 computeStippleDistanceLimits(float startPseudoScreen, float segmentLengthPseudoScreen, float segmentLengthScreen, float patternLength) {

      // First check if the segment is long enough to support fully screen space patterns.
      // Force sparse mode for segments that are very large in screen space even if it is not allowed,
      // to avoid imprecision from calculating with large floats.
      if (segmentLengthPseudoScreen >= ${m2 ? "patternLength" : "1e4"}) {
        // Round the screen length to get an integer number of pattern repetitions (minimum 1).
        float repetitions = segmentLengthScreen / (patternLength * pixelRatio);
        float flooredRepetitions = max(1.0, floor(repetitions + 0.5));
        float segmentLengthScreenRounded = flooredRepetitions * patternLength;

        float stretch = repetitions / flooredRepetitions;

        // We need to impose a lower bound on the stretch factor to prevent the dots from merging together when there is only 1 repetition.
        // 0.75 is the lowest possible stretch value for flooredRepetitions > 1, so it makes sense as lower bound.
        vStipplePatternStretch = max(0.75, stretch);

        return vec2(0.0, segmentLengthScreenRounded);
      }
      return vec2(startPseudoScreen, startPseudoScreen + segmentLengthPseudoScreen);
    }
  `), v.uniforms.add(new e("stipplePatternTexture", (e11) => e11.stippleTexture), new r("stipplePatternPixelSizeInv", (e11) => 1 / S(e11))), c.stippleOffColorEnabled && v.uniforms.add(new e2("stippleOffColor", (e11) => e10(e11.stippleOffColor))), d6.include(d3), c.worldSizedImagePattern ? (d6.varyings.add("vStippleV", "float"), d6.fragment.include(n5), v.code.add(t2`vec4 getStippleColor(out bool isClamped) {
vec2 aaCorrectedLimits = vStippleDistanceLimits + vec2(1.0, -1.0) / gl_FragCoord.w;
isClamped = vStippleDistance < aaCorrectedLimits.x || vStippleDistance > aaCorrectedLimits.y;
float u = vStippleDistance * stipplePatternPixelSizeInv;
float v = vStippleV == -1.0 ? 0.5 : vStippleV;
return texture(stipplePatternTexture, vec2(u, v));
}
vec4 getStippleColor() {
bool ignored;
return getStippleColor(ignored);
}
float getStippleSDF() {
vec4 color = getStippleColor();
return color.a == 0.0 ? -0.5 : 0.5;
}
float getStippleAlpha(float lineWidth) {
return getStippleColor().a;
}
vec4 blendStipple(vec4 color, float stippleAlpha) {
vec4 stippleColor = getStippleColor();
int mixMode  = 1;
vec3 col = mixExternalColor(color.rgb, vec3(1.0), stippleColor.rgb, mixMode);
float opacity = mixExternalOpacity(color.a, 1.0, stippleColor.a, mixMode);
return vec4(col, opacity);
}`)) : v.code.add(t2`
    float getStippleSDF(out bool isClamped) {
      float stippleDistanceClamped = noPerspectiveRead(clamp(vStippleDistance, vStippleDistanceLimits.x, vStippleDistanceLimits.y));
      float lineSizeInv = noPerspectiveRead(vLineSizeInv);

      vec2 aaCorrectedLimits = vStippleDistanceLimits + vec2(1.0, -1.0) / gl_FragCoord.w;
      isClamped = vStippleDistance < aaCorrectedLimits.x || vStippleDistance > aaCorrectedLimits.y;

      float u = stippleDistanceClamped * stipplePatternPixelSizeInv * lineSizeInv;
      u = fract(u);

      float sdf = texture(stipplePatternTexture, vec2(u, 0.5)).r;

      return (sdf - 0.5) * vStipplePatternStretch + 0.5;
    }

    float getStippleSDF() {
      bool ignored;
      return getStippleSDF(ignored);
    }

    float getStippleAlpha(float lineWidth) {
      bool isClamped;
      float stippleSDF = getStippleSDF(isClamped);
      float antiAliasedResult = clamp(stippleSDF * lineWidth + 0.5, 0.0, 1.0);
      return isClamped ? floor(antiAliasedResult + 0.5) : antiAliasedResult;
    }

    vec4 blendStipple(vec4 color, float stippleAlpha) {
      return ${c.stippleOffColorEnabled ? "mix(color, stippleOffColor, stippleAlpha)" : "vec4(color.rgb, color.a * stippleAlpha)"};
    }
  `), v.code.add(t2`
    void discardByStippleAlpha(float stippleAlpha, float threshold) {
     ${n4(!c.stippleOffColorEnabled, "if (stippleAlpha < threshold) { discard; }")}
    }
  `);
}
function S(e11) {
  const t7 = e11.stipplePattern;
  return h2(t7) ? t7.length : t7 ? l2(t7) / t7.pixelRatio : 1;
}
var g = 0.4;

// node_modules/@arcgis/core/views/3d/support/flow/constants.js
var r4 = t(1);
var t6 = t(1);

// node_modules/@arcgis/core/views/3d/webgl-engine/shaders/AnimatedLine.glsl.js
function n7(a4, n8) {
  const { hasAnimation: s4, animation: T } = n8;
  if (!s4) return;
  const { attributes: f3, varyings: p2, vertex: u3, fragment: c } = a4;
  f3.add("timeStamps", "vec4"), p2.add("vTimeStamp", "float"), p2.add("vFirstTime", "float"), p2.add("vLastTime", "float"), p2.add("vTransitionType", "float"), u3.main.add(t2`vTimeStamp = timeStamps.x;
vFirstTime = timeStamps.y;
vLastTime = timeStamps.z;
vTransitionType = timeStamps.w;`), 3 === T && c.constants.add("decayRate", "float", 2.3), c.code.add(t2`
    float getTrailOpacity(float x) {
      if (x < 0.0) {
        return 0.0;
      }

      ${d5(T)}
    }`), c.uniforms.add(new r("timeElapsed", (t7) => t7.timeElapsed), new r("trailLength", (t7) => t7.trailLength), new r("speed", (t7) => t7.animationSpeed), new e7("startEndTime", (a5) => u(r5, a5.startTime, a5.endTime))), c.constants.add("fadeInTime", "float", t6), c.constants.add("fadeOutTime", "float", r4), c.constants.add("incomingTransition", "int", 0), c.constants.add("outgoingTransition", "int", 2), c.code.add(t2`float fadeIn(float x) {
return smoothstep(0.0, fadeInTime, x);
}
float fadeOut(float x) {
return isinf(fadeOutTime) ? 1.0 : smoothstep(fadeOutTime, 0.0, x);
}
void updateAlphaIf(inout float alpha, bool condition, float newAlpha) {
alpha = condition ? min(alpha, newAlpha) : alpha;
}
vec4 animate(vec4 color) {
float startTime = startEndTime[0];
float endTime = startEndTime[1];
float totalTime = vLastTime - vFirstTime;
float actualFadeOutTime = min(fadeOutTime * speed, trailLength);
float longStreamlineThreshold = (fadeInTime + 1.0) * speed + actualFadeOutTime;
bool longStreamline = totalTime > longStreamlineThreshold;
float totalTimeWithFadeOut = longStreamline && actualFadeOutTime != trailLength ? totalTime : totalTime + actualFadeOutTime;
float fadeOutStartTime = longStreamline ? totalTime - actualFadeOutTime : totalTime;
float originTime =  -vFirstTime;
float actualEndTime = int(vTransitionType) == outgoingTransition ? min(endTime, startTime + vLastTime / speed) : endTime;
vec4 animatedColor = color;
if (speed == 0.0) {
float alpha = getTrailOpacity((totalTimeWithFadeOut - (vTimeStamp - vFirstTime)) / trailLength);
updateAlphaIf(alpha, !isinf(actualEndTime), fadeOut(timeElapsed - actualEndTime));
updateAlphaIf(alpha, true, fadeIn(timeElapsed - startTime));
animatedColor.a *= alpha;
return animatedColor;
}
float relativeStartTime = mod(startTime, totalTimeWithFadeOut);
float shiftedTimeElapsed = timeElapsed - relativeStartTime + originTime;
float headRelativeToFirst = mod(shiftedTimeElapsed * speed, totalTimeWithFadeOut);
float vRelativeToHead = headRelativeToFirst - originTime - vTimeStamp;
float vAbsoluteTime = timeElapsed - vRelativeToHead / speed;
if (startTime > timeElapsed) {
return vec4(0.0);
}
float alpha = getTrailOpacity(vRelativeToHead / trailLength);
updateAlphaIf(alpha, true, fadeIn(timeElapsed - startTime));
updateAlphaIf(alpha, !isinf(actualEndTime), fadeOut(timeElapsed - actualEndTime));
updateAlphaIf(alpha, int(vTransitionType) != incomingTransition, step(startTime, vAbsoluteTime));
updateAlphaIf(alpha, headRelativeToFirst > fadeOutStartTime, fadeOut((headRelativeToFirst - fadeOutStartTime) / speed));
alpha *= fadeIn(vTimeStamp - vFirstTime);
animatedColor.a *= alpha;
return animatedColor;
}`);
}
function d5(t7) {
  switch (t7) {
    case 2:
      return "return x >= 0.0 && x <= 1.0 ? 1.0 : 0.0;";
    case 3:
      return "float cutOff = exp(-decayRate);\n        return (exp(-decayRate * x) - cutOff) / (1.0 - cutOff);";
    default:
      return "return 1.0;";
  }
}
var r5 = n2();

// node_modules/@arcgis/core/chunks/RibbonLine.glsl.js
var P = 1;
function C(P2) {
  const C2 = new s(), { attributes: z2, varyings: F, vertex: j, fragment: W } = C2, { applyMarkerOffset: R, draped: I, output: V, capType: A, stippleEnabled: E2, falloffEnabled: N2, roundJoins: T, wireframe: O, innerColorEnabled: J, hasAnimation: _, hasScreenSizePerspective: M, worldSizedImagePattern: U } = P2;
  j.inputs.add("position", () => "position"), W.include(t4), C2.include(f2, P2), C2.include(m, P2), C2.include(d, P2), C2.include(n7, P2);
  const H = R && !I;
  H && (j.uniforms.add(new r("markerScale", (e11) => e11.markerScale)), C2.include(i, { space: 2, hasScreenSizePerspective: M })), f(j, P2), j.uniforms.add(new t3("inverseProjectionMatrix", (e11) => e11.camera.inverseProjectionMatrix), new e5("nearFar", (e11) => e11.camera.nearFar), new r("miterLimit", (e11) => "miter" !== e11.join ? 0 : e11.miterLimit), new e6("viewport", (e11) => e11.camera.fullViewport)), j.constants.add("LARGE_HALF_FLOAT", "float", 65500), j.constants.add("EPS", "float", 1e-3), j.constants.add("NUM_JOIN_SUBDIVISIONS", "float", P2.numJoinSubdivisions), z2.add("position", "vec3"), z2.add("previousDelta", "vec4"), z2.add("nextDelta", "vec4"), z2.add("lineParameters", "vec2"), z2.add("u0", "float"), F.add("vColor", "vec4"), F.add("vpos", "vec3", { invariant: true }), F.add("vLineDistance", "float"), F.add("vLineWidth", "float"), E2 || (F.add("vIsInsideJoin", "int"), F.add("vStretchFactor", "float"), F.add("vJoinCenterLineSDFs", "vec2"), F.add("vSubdivisionFactor", "float"));
  const $ = E2;
  $ && F.add("vLineSizeInv", "float");
  const B = 2 === A, k = E2 && B, G = N2 || k;
  G && F.add("vLineDistanceNorm", "float"), B && (F.add("vSegmentSDF", "float"), F.add("vReverseSegmentSDF", "float")), j.code.add(t2`vec3 perpendicular(vec3 v) {
return vec3(v.y, -v.x, 0.0);
}
float interp(float ncp, vec4 a, vec4 b) {
return (-ncp - a.z) / (b.z - a.z);
}
vec3 rotateZ(vec3 v, float a) {
float s = sin(a);
float c = cos(a);
mat2 m = mat2(c, -s, s, c);
return vec3(m * v.xy, v.z);
}`), j.code.add(t2`vec4 projectAndScale(vec4 pos) {
vec4 posNdc = proj * pos;
posNdc.xy *= viewport.zw / posNdc.w;
posNdc.z /= posNdc.w;
return posNdc;
}`), j.code.add(t2`void clip(
inout vec4 pos,
inout vec4 prev,
inout vec4 next,
bool isStartVertex
) {
float vnp = nearFar[0] * 0.99;
if (pos.z > -nearFar[0]) {
if (!isStartVertex) {
if (prev.z < -nearFar[0]) {
pos = mix(prev, pos, interp(vnp, prev, pos));
next = pos;
} else {
pos = vec4(0.0, 0.0, 0.0, 1.0);
}
} else {
if (next.z < -nearFar[0]) {
pos = mix(pos, next, interp(vnp, pos, next));
prev = pos;
} else {
pos = vec4(0.0, 0.0, 0.0, 1.0);
}
}
} else {
if (prev.z > -nearFar[0]) {
prev = mix(pos, prev, interp(vnp, pos, prev));
}
if (next.z > -nearFar[0]) {
next = mix(next, pos, interp(vnp, next, pos));
}
}
}`), w(j), j.constants.add("aaWidth", "float", E2 ? 0 : 1).main.add(t2`
    // unpack values from vertex type
    bool isStartVertex = abs(abs(lineParameters.y) - 3.0) == 1.0;
    vec3 prevPosition = position + previousDelta.xyz * previousDelta.w;
    vec3 nextPosition = position + nextDelta.xyz * nextDelta.w;

    float coverage = 1.0;

    // Check for special value of lineParameters.y which is used by the Renderer when graphics are removed before the
    // VBO is recompacted. If this is the case, then we just project outside of clip space.
    if (lineParameters.y == 0.0) {
      gl_Position = ${s2};
    }
    else {
      vec4 pos  = view * vec4(position, 1.0);
      vec4 prev = view * vec4(prevPosition, 1.0);
      vec4 next = view * vec4(nextPosition, 1.0);

      bool isJoin = abs(lineParameters.y) < 3.0;
  `), H && j.main.add(t2`vec4 other = isStartVertex ? next : prev;
bool markersHidden = areWorldMarkersHidden(pos.xyz, other.xyz);
if (!isJoin && !markersHidden) {
pos.xyz += normalize(other.xyz - pos.xyz) * getWorldMarkerSize(pos.xyz) * 0.5;
}`), C2.include(e4), j.main.add(t2`
      clip(pos, prev, next, isStartVertex);

      vec3 clippedPos = pos.xyz;
      vec3 clippedCenter = mix(pos.xyz, isStartVertex ? next.xyz : prev.xyz, 0.5);

      pos = projectAndScale(pos);
      next = projectAndScale(next);
      prev = projectAndScale(prev);

      vec3 left = (pos.xyz - prev.xyz);
      vec3 right = (next.xyz - pos.xyz);

      float leftLen = length(left);
      float rightLen = length(right);

      float lineSize = getSize(${n4(M, "clippedPos")});
      ${n4(E2 && M, "float patternLineSize = getSize(clippedCenter);")}
      ${n4(E2 && !M, "float patternLineSize = lineSize;")}

      ${n4(U, t2`
          lineSize += aaWidth;
          float lineWidth = lineSize * pixelRatio * worldToScreenRatio;
          if (lineWidth < 1.0) {
            coverage = lineWidth;
            lineWidth = 1.0;
          }
        `, t2`
          if (lineSize < 1.0) {
            coverage = lineSize; // convert sub-pixel coverage to alpha
            lineSize = 1.0;
          }

          lineSize += aaWidth;
          float lineWidth = lineSize * pixelRatio;
        `)}

      vLineWidth = noPerspectiveWrite(lineWidth, pos.w);
      ${$ ? t2`vLineSizeInv = noPerspectiveWrite(1.0 / lineSize, pos.w);` : ""}
  `);
  (E2 || B) && j.main.add(t2`
      float isEndVertex = float(!isStartVertex);
      vec3 segmentOrigin = mix(pos.xyz, prev.xyz, isEndVertex);
      vec3 segment = mix(right, left, isEndVertex);
      ${B ? t2`vec3 segmentEnd = mix(next.xyz, pos.xyz, isEndVertex);` : ""}
    `), j.main.add(t2`left = (leftLen > EPS) ? left/leftLen : vec3(0.0, 0.0, 0.0);
right = (rightLen > EPS) ? right/rightLen : vec3(0.0, 0.0, 0.0);
vec3 segmentDirection = isStartVertex ? right : left;
vec3 capDisplacementDir = vec3(0.0, 0.0, 0.0);
vec3 joinDisplacementDir = vec3(0.0, 0.0, 0.0);
float displacementLen = lineWidth;
float miterDisplacementLen = lineWidth;
float innerDisplacementLen = lineWidth;`), E2 || j.main.add(t2`vIsInsideJoin = 0;
vStretchFactor = 1.0;
vSubdivisionFactor = 0.0;
vJoinCenterLineSDFs = vec2(LARGE_HALF_FLOAT);`), j.main.add(t2`float subdivisionFactor = 0.0;
bool isOutside = false;
if (isJoin) {
isOutside = (left.x * right.y - left.y * right.x) * lineParameters.y > 0.0;
vec3 joinDirection = normalize(left + right);
joinDisplacementDir = perpendicular(joinDirection);
if (leftLen > EPS && rightLen > EPS) {
float nDotSeg = dot(joinDisplacementDir, left);
displacementLen /= length(nDotSeg * left - joinDisplacementDir);
miterDisplacementLen = displacementLen;
innerDisplacementLen = min(displacementLen, min(leftLen, rightLen)/abs(nDotSeg));
if (!isOutside) {
displacementLen = innerDisplacementLen;
}
}
subdivisionFactor = lineParameters.x;`), E2 || j.main.add(t2`if(subdivisionFactor > 0.0) {
vIsInsideJoin = 1;
}
vSubdivisionFactor = isOutside ? subdivisionFactor : 0.5;
if (miterDisplacementLen > miterLimit * lineWidth) {
vec2 leftScreenDir = left.xy;
vec2 rightScreenDir = right.xy;
float leftScreenLen = length(leftScreenDir);
float rightScreenLen = length(rightScreenDir);
if (leftScreenLen > EPS && rightScreenLen > EPS) {
leftScreenDir /= leftScreenLen;
rightScreenDir /= rightScreenLen;
float theta = acos(clamp(dot(leftScreenDir, rightScreenDir), -1.0, 1.0));
float subdividedTriangleHeight = (innerDisplacementLen + lineWidth) * cos(theta / (2.0 + 2.0 * NUM_JOIN_SUBDIVISIONS));
float bevelTriangleHeight = innerDisplacementLen + lineWidth * cos(theta * 0.5);
float triangleHeight = NUM_JOIN_SUBDIVISIONS > 0.0 ? subdividedTriangleHeight : bevelTriangleHeight;
vStretchFactor = noPerspectiveWrite(max(triangleHeight / (2.0 * lineWidth), 1.0), pos.w);
}
}`), j.main.add(t2`if (isOutside && (displacementLen > miterLimit * lineWidth)) {`), T ? j.main.add(t2`
        vec3 startDir = leftLen < EPS ? right : left;
        startDir = perpendicular(startDir);

        vec3 endDir = rightLen < EPS ? left : right;
        endDir = perpendicular(endDir);

        float factor = ${E2 ? t2`min(1.0, subdivisionFactor * ((NUM_JOIN_SUBDIVISIONS + 1.0) / NUM_JOIN_SUBDIVISIONS))` : t2`subdivisionFactor`};

        float rotationAngle = acos(clamp(dot(startDir.xy, endDir.xy), -1.0, 1.0));
        joinDisplacementDir = rotateZ(startDir, -sign(lineParameters.y) * factor * rotationAngle);
      `) : j.main.add(t2`
        vec3 startDir = perpendicular(leftLen < EPS ? right : left);
        vec3 endDir = perpendicular(rightLen < EPS ? left : right);

        ${n4(E2, t2`joinDisplacementDir = (isStartVertex || subdivisionFactor > 0.0) ? endDir : startDir;`, t2`joinDisplacementDir = mix(startDir, endDir, subdivisionFactor);`)}
  `);
  const X = 0 !== A;
  return j.main.add(t2`
        displacementLen = lineWidth;
      }
    } else {
      // CAP handling ---------------------------------------------------
      joinDisplacementDir = isStartVertex ? right : left;
      joinDisplacementDir = perpendicular(joinDisplacementDir);

      ${X ? t2`capDisplacementDir = vec3((isStartVertex ? -right : left).xy, 0.0);` : ""}
    }
  `), j.main.add(t2`
    // Displacement (in pixels) caused by join/or cap
    vec2 dposXY = (joinDisplacementDir.xy * sign(lineParameters.y) + capDisplacementDir.xy) * displacementLen;

    /**
     * To prevent z-fighting between layers, we also adjust the z value.
     * We want to ensure that the orientation of the final triangles is the same, regardless of the line width.
     * To do so, the below formula projects the xy displacement onto the original segment direction
     * to find the z-offset necessary so the triangle orientation is independent of the width.
     */
    float dposZ = dot(dposXY, segmentDirection.xy) / dot(segmentDirection.xy, segmentDirection.xy) * segmentDirection.z;
    vec3 dpos = vec3(dposXY, dposZ);

    float lineDistNorm = noPerspectiveWrite(sign(lineParameters.y), pos.w);

    vLineDistance = lineWidth * lineDistNorm;
    ${G ? t2`vLineDistanceNorm = lineDistNorm;` : ""}

    pos.xyz += dpos;
  `), E2 || j.main.add(t2`if (isJoin) {
vec2 joinCenterToVertex = dposXY;
vec2 leftCenterlineDir = left.xy;
vec2 rightCenterlineDir = right.xy;
float leftCenterlineLen = length(leftCenterlineDir);
float rightCenterlineLen = length(rightCenterlineDir);
leftCenterlineDir = leftCenterlineLen > EPS ? leftCenterlineDir / leftCenterlineLen : vec2(1.0, 0.0);
rightCenterlineDir = rightCenterlineLen > EPS ? rightCenterlineDir / rightCenterlineLen : leftCenterlineDir;
vJoinCenterLineSDFs = noPerspectiveWrite(
vec2(
dot(vec2(rightCenterlineDir.y, -rightCenterlineDir.x), joinCenterToVertex),
dot(vec2(leftCenterlineDir.y, -leftCenterlineDir.x), joinCenterToVertex)
),
pos.w
);
}`), B && j.main.add(t2`vec2 segmentDir = normalize(segment.xy);
vSegmentSDF = noPerspectiveWrite((isJoin && isStartVertex) ? LARGE_HALF_FLOAT : (dot(pos.xy - segmentOrigin.xy, segmentDir)), pos.w);
vReverseSegmentSDF = noPerspectiveWrite((isJoin && !isStartVertex) ? LARGE_HALF_FLOAT : (dot(pos.xy - segmentEnd.xy, -segmentDir)), pos.w);`), E2 && (I ? j.uniforms.add(new r2("worldToScreenRatio", (e11) => 1 / e11.screenToPCSRatio)) : j.main.add(t2`vec3 segmentCenter = mix((nextPosition + position) * 0.5, (position + prevPosition) * 0.5, isEndVertex);
float worldToScreenRatio = computeWorldToScreenRatio(segmentCenter);`), j.main.add(t2`float segmentLengthScreenDouble = length(segment.xy);
float segmentLengthScreen = segmentLengthScreenDouble * 0.5;
float discreteWorldToScreenRatio = discretizeWorldToScreenRatio(worldToScreenRatio);
float segmentLengthRender = length(mix(nextPosition - position, position - prevPosition, isEndVertex));
vStipplePatternStretch = worldToScreenRatio / discreteWorldToScreenRatio;`), I ? j.main.add(t2`float segmentLengthPseudoScreen = segmentLengthScreen / pixelRatio * discreteWorldToScreenRatio / worldToScreenRatio;
float startPseudoScreen = u0 * discreteWorldToScreenRatio - mix(0.0, segmentLengthPseudoScreen, isEndVertex);`) : j.main.add(t2`float startPseudoScreen = mix(u0, u0 - segmentLengthRender, isEndVertex) * discreteWorldToScreenRatio;
float segmentLengthPseudoScreen = segmentLengthRender * discreteWorldToScreenRatio;`), j.uniforms.add(new r("stipplePatternPixelSize", (e11) => S(e11))), j.main.add(t2`
      float patternLength = patternLineSize * stipplePatternPixelSize;

      ${n4(U, t2`
          float uu = mix(u0, u0 - segmentLengthRender, isEndVertex);
          vStippleDistanceLimits = vec2(uu, uu + segmentLengthRender);
          vStipplePatternStretch = 1.0;

          // The v-coordinate used in case of an image pattern.
          bool isLeft = sign(lineParameters.y) < 0.0;
          vStippleV = isLeft ? 0.0 : 1.0;
        `, t2`
          // Compute the coordinates at both start and end of the line segment, because we need both to clamp to in the
          // fragment shader
          vStippleDistanceLimits = computeStippleDistanceLimits(startPseudoScreen, segmentLengthPseudoScreen, segmentLengthScreen, patternLength);
        `)}

      vStippleDistance = mix(vStippleDistanceLimits.x, vStippleDistanceLimits.y, isEndVertex);

      // Adjust the coordinate to the displaced position (the pattern is shortened/overextended on the in/outside of
      // joins)
      if (segmentLengthScreenDouble >= EPS) {
        // Project the actual vertex position onto the line segment. Note that the resulting factor is within [0..1]
        // at the original vertex positions, and slightly outside of that range at the displaced positions
        vec3 stippleDisplacement = pos.xyz - segmentOrigin;
        float stippleDisplacementFactor = dot(segment.xy, stippleDisplacement.xy) / (segmentLengthScreenDouble * segmentLengthScreenDouble);

        // Apply this offset to the actual vertex coordinate (can be screen or pseudo-screen space)
        vStippleDistance += (stippleDisplacementFactor - isEndVertex) * (vStippleDistanceLimits.y - vStippleDistanceLimits.x);
      }

      // Cancel out perspective correct interpolation because we want this length the really represent the screen
      // distance
      vStippleDistanceLimits = noPerspectiveWrite(vStippleDistanceLimits, pos.w);
      vStippleDistance = noPerspectiveWrite(vStippleDistance, pos.w);

      // Disable stipple distance limits on caps
      vStippleDistanceLimits = isJoin ?
                                 vStippleDistanceLimits :
                                 isStartVertex ?
                                  vec2(-1e34, vStippleDistanceLimits.y) :
                                  vec2(vStippleDistanceLimits.x, 1e34);
    `)), j.main.add(t2`
      // Convert back into NDC
      pos.xy = (pos.xy / viewport.zw) * pos.w;
      pos.z = pos.z * pos.w;

      vColor = getColor();
      vColor.a = noPerspectiveWrite(vColor.a * coverage, pos.w);

      ${O && !I ? "pos.z -= EPS * pos.w;" : ""}

      // transform final position to camera space for slicing
      vpos = (inverseProjectionMatrix * pos).xyz;
      gl_Position = pos;
      forwardObjectAndLayerIdColor();
    }`), C2.fragment.include(h, P2), C2.include(u2, P2), W.include(e3), W.main.add(t2`discardBySlice(vpos);`), C2.include(d3), W.include(a2), W.main.add(t2`
    float lineWidth = noPerspectiveRead(vLineWidth);
    float lineDistance = noPerspectiveRead(vLineDistance);
    ${n4(G, t2`float lineDistanceNorm = noPerspectiveRead(vLineDistanceNorm);`)}
  `), O ? W.main.add(t2`vec4 finalColor = vec4(1.0, 0.0, 1.0, 1.0);`) : (B && W.main.add(t2`float sdf = noPerspectiveRead(min(vSegmentSDF, vReverseSegmentSDF));
vec2 fragmentPosition = vec2(min(sdf, 0.0), lineDistance);
float fragmentRadius = length(fragmentPosition);
float fragmentCapSDF = (fragmentRadius - lineWidth) * 0.5;
float capCoverage = clamp(0.5 - fragmentCapSDF, 0.0, 1.0);
if (capCoverage < alphaCutoff) {
discard;
}`), k ? W.main.add(t2`vec2 stipplePosition = vec2(
min(getStippleSDF() * 2.0 - 1.0, 0.0),
lineDistanceNorm
);
float stippleRadius = length(stipplePosition * lineWidth);
float stippleCapSDF = (stippleRadius - lineWidth) * 0.5;
float stippleCoverage = clamp(0.5 - stippleCapSDF, 0.0, 1.0);
float stippleAlpha = step(alphaCutoff, stippleCoverage);`) : W.main.add(t2`float stippleAlpha = getStippleAlpha(lineWidth);`), 11 !== V && W.main.add(t2`discardByStippleAlpha(stippleAlpha, alphaCutoff);`), C2.include(d3), W.uniforms.add(new e2("intrinsicColor", (e11) => e11.color)).main.add(t2`vec4 color = intrinsicColor * vColor;
color.a = noPerspectiveRead(color.a);`), J && W.uniforms.add(new e2("innerColor", (e11) => e11.innerColor ?? e11.color), new r("innerWidth", (e11, i3) => e11.innerWidth * i3.camera.pixelRatio)).main.add(t2`float distToInner = abs(lineDistance) - innerWidth;
float innerAA = clamp(0.5 - distToInner, 0.0, 1.0);
float innerAlpha = innerColor.a + color.a * (1.0 - innerColor.a);
color = mix(color, vec4(innerColor.rgb, innerAlpha), innerAA);`), W.main.add(t2`vec4 finalColor = blendStipple(color, stippleAlpha);`), N2 && (W.uniforms.add(new r("falloff", (e11) => e11.falloff)), W.main.add(t2`finalColor.a *= pow(max(0.0, 1.0 - abs(lineDistanceNorm)), falloff);`)), E2 || W.main.add(t2`float stretchFactor = vIsInsideJoin == 1 ? noPerspectiveRead(vStretchFactor) : 1.0;
float featherWidth = 2.0;
float featherStartDistance = max(lineWidth - featherWidth / stretchFactor, 0.0);
float straightFeatherStartDistance = max(lineWidth - featherWidth, 0.0);
float value = abs(lineDistance);
float feather = (value - featherStartDistance) / (lineWidth - featherStartDistance);
vec2 joinCenterSDFs = noPerspectiveRead(vJoinCenterLineSDFs);
float joinCenterDistance = abs(vSubdivisionFactor > 0.5 ? joinCenterSDFs.x : joinCenterSDFs.y);
float straightFeather = (joinCenterDistance - straightFeatherStartDistance) / (lineWidth - straightFeatherStartDistance);
feather = vIsInsideJoin == 1 ? max(feather, straightFeather) : feather;
finalColor.a *= 1.0 - clamp(feather, 0.0, 1.0);`), _ && W.main.add(t2`
        finalColor = animate(finalColor);

        ${n4(11 !== V, t2`
            if (finalColor.a <= alphaCutoff) {
              discard;
            }`)}
      `)), W.main.add(t2`outputColorHighlightOLID(applySlice(finalColor, vpos), finalColor.rgb);`), C2;
}
var z = Object.freeze(Object.defineProperty({ __proto__: null, build: C, ribbonlineNumRoundJoinSubdivisions: P }, Symbol.toStringTag, { value: "Module" }));

export {
  h2 as h,
  l,
  a3 as a,
  P,
  C,
  z
};
//# sourceMappingURL=chunk-VLMULLZB.js.map
