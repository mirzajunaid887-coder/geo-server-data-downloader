import {
  makeGenericController
} from "./chunk-BAWXBGCB.js";

// node_modules/@esri/calcite-components/dist/chunks/useFormTrigger.js
var useFormTrigger = (options) => makeGenericController((component) => {
  function clickHandler(event) {
    var _a;
    const { form } = component.elementInternals;
    if (event.defaultPrevented || component.disabled || ((_a = options == null ? void 0 : options.disabled) == null ? void 0 : _a.call(options)) || !form || component.type === "button") {
      return;
    }
    if (component.type === "submit") {
      form.requestSubmit();
      return;
    }
    form.reset();
  }
  component.listen("luminaFormAssociatedCallback", ({ detail: [form] }) => {
    if (form) {
      component.el.addEventListener("click", clickHandler);
    } else {
      component.el.removeEventListener("click", clickHandler);
    }
  });
});

export {
  useFormTrigger
};
//# sourceMappingURL=chunk-NRLP3LDU.js.map
