import {
  m
} from "./chunk-YNFAPEQE.js";
import {
  q
} from "./chunk-ZMOSP5E7.js";
import {
  t
} from "./chunk-XVUWZ5Z2.js";
import {
  Js,
  Pu,
  kh,
  lr,
  zt
} from "./chunk-6MN3AQXL.js";
import {
  s
} from "./chunk-KEKZ5PBQ.js";
import {
  K,
  Kt,
  M,
  Q,
  W,
  Wt,
  a,
  f,
  j,
  li,
  n,
  x
} from "./chunk-JZGFKTKC.js";
import {
  __publicField
} from "./chunk-SROXYCZ4.js";

// node_modules/@arcgis/core/geometry/operators/gx/operatorOffset.js
function M2(t2, s2, e, o, h, r, m2) {
  if (null === t2 && M(""), t2.getDimension() < 1 && M(""), h > 0 || M(""), j(t2), 0 === s2 || t2.isEmpty()) return t2;
  const a2 = new D(m2);
  return a2.m_bUseZ = t2.hasAttribute(1), a2.m_bUseM = t2.hasAttribute(2), a2.m_inputGeometry = t2, a2.m_distance = s2, a2.m_tolerance = h, a2.m_joins = e, a2.m_miterLimit = o > 1 ? o : 1, a2.m_miterLimit = q(a2.m_miterLimit), a2.m_progressCounter = 0, a2.m_options = r, a2.constructOffset();
}
var g = 1.4142135623730951;
var C = 0.017453292519943295;
var E = 256;
var v = 512;
var A = 1024;
var G = class _G {
  constructor() {
    __publicField(this, "x");
    __publicField(this, "y");
    __publicField(this, "m_next");
    __publicField(this, "m_prev");
    __publicField(this, "z");
    __publicField(this, "m");
    __publicField(this, "type");
  }
  static construct(t2, s2, e) {
    const i = new _G();
    return i.x = t2.x, i.y = t2.y, i.m_next = -1, i.m_prev = -1, i.z = s2, i.m = e, i.type = 0, i;
  }
  clone() {
    const t2 = new _G();
    return t2.x = this.x, t2.y = this.y, t2.m_next = this.m_next, t2.m_prev = this.m_prev, t2.z = this.z, t2.m = this.m, t2.type = this.type, t2;
  }
  asPoint2D() {
    return new li(this.x, this.y);
  }
};
function w() {
  return { pt: new G(), bAtExistingPt: false };
}
var D = class _D {
  constructor(t2) {
    __publicField(this, "m_progressTracker");
    __publicField(this, "m_inputGeometry");
    __publicField(this, "m_distance", -1);
    __publicField(this, "m_tolerance", -1);
    __publicField(this, "m_miterLimit", 10);
    __publicField(this, "m_joins", 0);
    __publicField(this, "m_progressCounter", 0);
    __publicField(this, "m_options", 0);
    __publicField(this, "m_bUseZ", false);
    __publicField(this, "m_bUseM", false);
    __publicField(this, "m_srcPts", []);
    __publicField(this, "m_srcPtCount", 0);
    __publicField(this, "m_offsetPts", []);
    __publicField(this, "m_offsetPtCount", 0);
    __publicField(this, "m_resultPath");
    __publicField(this, "m_a1", 0);
    __publicField(this, "m_a2", 0);
    this.m_progressTracker = t2;
  }
  constructOffset() {
    const s2 = this.m_inputGeometry.getGeometryType();
    if (s2 === a.enumLine) return this.offsetLine();
    if (s2 === a.enumEnvelope) return this.offsetEnvelope();
    if (f(s2)) {
      const t2 = new Js();
      return t2.addSegment(this.m_inputGeometry, true), this.m_inputGeometry = t2, this.constructOffset();
    }
    if (s2 === a.enumPolyline) {
      const t2 = new Js();
      return this.offsetMultiPath(t2), t2;
    }
    if (s2 === a.enumPolygon) {
      if (1 === this.m_options) {
        let s4;
        switch (this.m_joins) {
          case 1:
          case 3:
            s4 = 2;
            break;
          case 2:
            s4 = 1;
            break;
          case 0:
            s4 = 0;
            break;
          default:
            n(false, "Invalid join type");
        }
        const e = 1, i = [-1];
        return new m().executeMany(new t([this.m_inputGeometry]), null, i, s4, e, this.m_miterLimit, this.m_tolerance, 360, false, 0, null).next();
      }
      const s3 = new lr();
      return this.offsetMultiPath(s3), s3;
    }
    x("");
  }
  offsetLine() {
    const t2 = this.m_inputGeometry, s2 = t2.getStartXY(), e = t2.getEndXY(), i = new li();
    i.setSub(e, s2), i.normalize(), i.leftPerpendicularThis(), i.scale(this.m_distance), s2.addThis(i), e.addThis(i);
    const n2 = t2.clone();
    return n2.setStartXY(s2), n2.setEndXY(e), n2;
  }
  offsetEnvelope() {
    const t2 = this.m_inputGeometry;
    if (this.m_distance > 0 && 2 !== this.m_joins) {
      const s3 = new lr();
      return s3.addEnvelope(t2, false), this.m_inputGeometry = s3, this.constructOffset();
    }
    const s2 = t2.clone();
    return s2.inflateCoords(this.m_distance, this.m_distance), s2;
  }
  progress() {
  }
  static buildPoint(t2, s2, e, i) {
    i.x = t2.x + s2 * Math.cos(e), i.y = t2.y + s2 * Math.sin(e), i.type = t2.type, i.z = t2.z, i.m = t2.m, i.m_next = -1, i.m_prev = -1;
  }
  addPoint(t2) {
    this.m_offsetPts.push(t2.clone()), this.m_offsetPtCount++;
  }
  addPointEx(t2, s2) {
    if (0 === this.m_offsetPtCount) return void this.addPoint(t2);
    const e = this.m_srcPtCount, i = this.m_srcPts[0 === s2 ? e - 1 : s2 - 1], n2 = this.m_srcPts[s2], o = _D.dotSign(i, n2, this.m_offsetPts[this.m_offsetPtCount - 1], t2);
    if (o > 0) this.addPoint(t2);
    else if (o < 0) if (_D.dotSign(i, n2, n2, this.m_offsetPts[this.m_offsetPtCount - 1]) > 0) {
      const n3 = new G();
      let o2;
      o2 = 0 === s2 ? e - 2 : 1 === s2 ? e - 1 : s2 - 2;
      const h = this.m_srcPts[o2], r = Math.atan2(i.y - h.y, i.x - h.x);
      if (_D.buildPoint(i, this.m_distance, r - Wt, n3), this.m_offsetPts[this.m_offsetPtCount - 1] = n3.clone(), 1 === this.m_joins || 2 === this.m_joins) {
        n3.x = 0.5 * (n3.x + i.x), n3.y = 0.5 * (n3.y + i.y), this.addPoint(n3), _D.buildPoint(i, this.m_distance, this.m_a1 + Wt, n3);
        const t3 = n3.clone();
        t3.x = 0.5 * (t3.x + i.x), t3.y = 0.5 * (t3.y + i.y), t3.type |= E, this.addPoint(t3), this.addPoint(n3);
      } else _D.buildPoint(i, this.m_distance, this.m_a1 + Wt, n3), n3.type |= E, this.addPoint(n3);
      this.addPointEx(t2, s2);
    } else {
      const t3 = new G();
      if (_D.buildPoint(n2, this.m_distance, this.m_a1 + Wt, t3), this.addPoint(t3), 1 === this.m_joins || 2 === this.m_joins) {
        t3.x = 0.5 * (t3.x + n2.x), t3.y = 0.5 * (t3.y + n2.y), this.addPoint(t3), _D.buildPoint(n2, this.m_distance, this.m_a2 - Wt, t3);
        const s3 = t3.clone();
        s3.x = 0.5 * (s3.x + n2.x), s3.y = 0.5 * (s3.y + n2.y), s3.type |= E, this.addPoint(s3), this.addPoint(t3);
      } else _D.buildPoint(n2, this.m_distance, this.m_a2 - Wt, t3), t3.type |= E, this.addPoint(t3);
    }
  }
  buildOffset() {
    const t2 = new G(), s2 = this.m_srcPtCount;
    this.m_offsetPtCount = 0;
    const e = 0.5 * this.m_tolerance;
    let i = 0, n2 = 0;
    for (let o = 0; o < s2; o++) {
      const h = this.m_srcPts[o], r = 0 === o ? this.m_srcPts[s2 - 1] : this.m_srcPts[o - 1], m2 = o === s2 - 1 ? this.m_srcPts[0] : this.m_srcPts[o + 1];
      let a2, _, f2 = 0;
      {
        const t3 = r.x - h.x, s3 = r.y - h.y, e2 = m2.x - h.x, c2 = m2.y - h.y;
        a2 = Math.atan2(s3, t3), _ = Math.atan2(c2, e2), this.m_a1 = a2, this.m_a2 = _, 0 === o && (i = a2, n2 = _), f2 = li.orientationRobust(h.asPoint2D(), r.asPoint2D(), m2.asPoint2D());
      }
      const c = _;
      _ < a2 && (_ += Kt);
      const l = Math.abs(Math.abs(a2 - _) - Math.PI) < 1e-8;
      if (l || f2 * this.m_distance > 0) if (l || 1 !== this.m_joins && 2 !== this.m_joins) {
        const s3 = 0.5 * (_ - a2), e2 = 0 === s3 ? this.m_distance : this.m_distance / Math.abs(Math.sin(s3));
        _D.buildPoint(h, e2, 0.5 * (a2 + _), t2), this.addPointEx(t2, o);
      } else {
        _D.buildPoint(h, this.m_distance, a2 + Wt, t2), this.addPoint(t2);
        const s3 = 1e-3;
        t2.x = h.x + (t2.x - h.x) * s3, t2.y = h.y + (t2.y - h.y) * s3, this.addPoint(t2), _D.buildPoint(h, this.m_distance, _ - Wt, t2);
        const e2 = t2.clone();
        e2.x = h.x + (e2.x - h.x) * s3, e2.y = h.y + (e2.y - h.y) * s3, e2.type |= E, this.addPoint(e2), this.addPoint(t2);
      }
      else {
        if (h.type & v) {
          const s3 = 1 - e / Math.abs(this.m_distance);
          let i2 = 1, n3 = this.m_distance < 0 ? -Math.PI : Math.PI;
          if (s3 > -1 && s3 < 1) {
            let t3 = 2 * Math.acos(s3);
            t3 < C && (t3 = C), i2 = Math.trunc(Math.PI / t3 + 1.5), i2 > 1 && (n3 /= i2);
          }
          i2 <= 1 && (i2 = 2, n3 /= 2);
          let r2 = a2 + Wt;
          _D.buildPoint(h, this.m_distance, r2, t2), 0 === o && (t2.type |= A), this.addPointEx(t2, o);
          const m3 = this.m_distance / Math.cos(n3 / 2);
          for (r2 += n3 / 2, _D.buildPoint(h, m3, r2, t2), t2.type |= A, this.addPoint(t2); --i2 > 0; ) r2 += n3, _D.buildPoint(h, m3, r2, t2), t2.type |= A, this.addPoint(t2);
          _D.buildPoint(h, this.m_distance, _ - Wt, t2), t2.type |= A, this.addPoint(t2);
          continue;
        }
        if (1 !== this.m_joins) {
          if (0 === this.m_joins) {
            const s3 = 1 - e / Math.abs(this.m_distance);
            let i2 = 1, n3 = _ - Wt - (a2 + Wt);
            if (s3 > -1 && s3 < 1) {
              let t3 = 2 * Math.acos(s3);
              t3 < C && (t3 = C), i2 = Math.trunc(Math.abs(n3) / t3 + 1.5), i2 > 1 && (n3 /= i2);
            }
            const r2 = this.m_distance / Math.cos(0.5 * n3);
            let m3 = a2 + Wt + 0.5 * n3;
            for (_D.buildPoint(h, r2, m3, t2), this.addPointEx(t2, o); --i2 > 0; ) m3 += n3, _D.buildPoint(h, r2, m3, t2), this.addPoint(t2);
            continue;
          }
          if (2 === this.m_joins) {
            const s3 = r.x - h.x, e2 = r.y - h.y, i2 = m2.x - h.x, n3 = m2.y - h.y;
            let f3 = (s3 * i2 + e2 * n3) / Math.sqrt(s3 * s3 + e2 * e2) / Math.sqrt(i2 * i2 + n3 * n3);
            if (f3 = Math.max(f3, -1), f3 > 1 - 1e-8) {
              _D.buildPoint(h, g * this.m_distance, _ - 0.25 * Math.PI, t2), this.addPointEx(t2, o), _D.buildPoint(h, g * this.m_distance, _ + 0.25 * Math.PI, t2), this.addPoint(t2);
              continue;
            }
            const c2 = Math.abs(this.m_distance / Math.sin(0.5 * Math.acos(f3))), P = Math.abs(this.m_miterLimit * this.m_distance);
            if (c2 > P) {
              const s4 = 0.5 * (_ - a2), e3 = this.m_distance / Math.abs(Math.sin(s4));
              _D.buildPoint(h, e3, 0.5 * (a2 + _), t2);
              const i3 = li.construct(t2.x, t2.y), n4 = li.construct(h.x, h.y), r2 = new li();
              r2.setSub(i3, n4);
              const m3 = new li();
              m3.setScaleAdd(P / r2.length(), r2, n4);
              const f4 = Math.sqrt(c2 * c2 - this.m_distance * this.m_distance), d2 = (c2 - P) * Math.abs(this.m_distance) / f4;
              this.m_distance > 0 ? r2.rightPerpendicularThis() : r2.leftPerpendicularThis(), r2.scale(d2 / r2.length());
              const l3 = new li();
              l3.setAdd(m3, r2);
              const y = new li();
              y.setSub(m3, r2), t2.x = l3.x, t2.y = l3.y, this.addPointEx(t2, o), t2.x = y.x, t2.y = y.y, this.addPoint(t2);
              continue;
            }
            const d = 0.5 * (_ - a2), l2 = this.m_distance / Math.abs(Math.sin(d));
            _D.buildPoint(h, l2, 0.5 * (a2 + _), t2), this.addPointEx(t2, o);
            continue;
          }
          {
            let s3;
            if (_ = c, this.m_distance > 0 ? (_ > a2 && (_ -= Kt), s3 = a2 - _ < Wt) : (_ < a2 && (_ += Kt), s3 = _ - a2 < Wt), s3) {
              const s4 = this.m_distance * g;
              let e2;
              e2 = s4 < 0 ? a2 + 0.25 * Math.PI : a2 + 3 * Math.PI * 0.25, _D.buildPoint(h, s4, e2, t2), this.addPointEx(t2, o), e2 = s4 < 0 ? _ - 0.25 * Math.PI : _ - 3 * Math.PI * 0.25, _D.buildPoint(h, s4, e2, t2), this.addPoint(t2);
            } else {
              const s4 = 0.5 * (_ - a2), e2 = this.m_distance / Math.abs(Math.sin(s4));
              _ < a2 && (_ += Kt), _D.buildPoint(h, e2, (a2 + _) / 2, t2), this.addPointEx(t2, o);
            }
          }
        } else _D.buildPoint(h, this.m_distance, a2 + Wt, t2), this.addPointEx(t2, o), _D.buildPoint(h, this.m_distance, _ - Wt, t2), this.addPoint(t2);
      }
    }
    return this.m_a1 = i, this.m_a2 = n2, this.addPointEx(this.m_offsetPts[0], 0), this.m_offsetPts[0] = this.m_offsetPts[this.m_offsetPtCount - 1].clone(), this.removeBadSegsFast();
  }
  removeBadSegsFast() {
    for (let e = 0; e < this.m_offsetPtCount; e++) {
      const t3 = this.m_offsetPts[e];
      t3.m_next = e + 1, t3.m_prev = e - 1;
    }
    this.m_offsetPts[0].m_prev = this.m_offsetPtCount - 2, this.m_offsetPts[this.m_offsetPtCount - 2].m_next = 0;
    let t2 = this.m_offsetPtCount, s2 = 0;
    for (let e = 0; e < t2; e++) if (this.m_offsetPts[s2].type & E) {
      const e2 = this.m_offsetPts[s2].m_next, i = this.deleteClosedSeg(s2);
      if (-1 === i) {
        n(this.m_offsetPts[s2].m_next === e2);
        break;
      }
      if (s2 > i) {
        s2 = i, t2 = 0;
        do {
          t2++, s2 = this.m_offsetPts[s2].m_next;
        } while (s2 !== i);
      }
      s2 = i;
    } else s2 = this.m_offsetPts[s2].m_next;
    return this.compressOffsetArray(s2), true;
  }
  deleteClosedSeg(t2) {
    const s2 = this.m_offsetPtCount - 1;
    let e = t2;
    for (let i = 1; i <= s2 - 2 && (e = this.m_offsetPts[e].m_next, t2 !== e); i++) {
      let s3 = e, n2 = t2;
      for (let o = 1; o <= i && (n2 = this.m_offsetPts[n2].m_prev, n2 !== t2); o++) {
        if (0 === (this.m_offsetPts[n2].type & E) && 0 === (this.m_offsetPts[s3].type & E)) {
          const t3 = this.handleClosedIntersection(n2, s3);
          if (-1 !== t3) return t3;
        }
        if (s3 = this.m_offsetPts[s3].m_prev, s3 === e) break;
      }
    }
    return -1;
  }
  handleClosedIntersection(t2, s2) {
    const e = this.m_offsetPts[this.m_offsetPts[t2].m_prev], i = this.m_offsetPts[t2], n2 = this.m_offsetPts[this.m_offsetPts[s2].m_prev], o = this.m_offsetPts[s2];
    if (!this.sectGraphicRect(e, i, n2, o)) return -1;
    if (li.orientationRobustEx(e.asPoint2D(), i.asPoint2D(), n2.asPoint2D(), o.asPoint2D()) * this.m_distance < 0) {
      const h = this.findIntersection(e, i, n2, o);
      let r = false;
      if (h[0] >= 0) {
        const m2 = w();
        if (h[0] >= 0 && h[0] <= 1 && (m2.pt.x = Q(e.x, i.x, h[0]), m2.pt.y = Q(e.y, i.y, h[0]), this.m_bUseZ && (m2.pt.z = Q(n2.z, o.z, h[1])), this.m_bUseM && (m2.pt.m = Q(n2.m, o.m, h[1])), 0 !== h[1] && 1 !== h[1] || 0 !== h[0] && 1 !== h[0] || (m2.bAtExistingPt = true), r = !((0 === h[1] || 1 === h[1]) && h[0] > 0 && h[0] < 1 || (0 === h[0] || 1 === h[0]) && h[1] > 0 && h[1] < 1)), r && !m2.bAtExistingPt) {
          let e2 = false;
          const o2 = m2.pt.asPoint2D(), h2 = n2.asPoint2D(), r2 = m2.pt.asPoint2D();
          let a2 = this.m_offsetPts[t2].m_prev;
          for (; ; ) {
            if (li.getClosestCoordinate(h2, r2, o2, false) < 1e-8) {
              e2 = false;
              break;
            }
            if (r2.y > o2.y && h2.y <= o2.y || r2.y < o2.y && r2.y <= o2.y) {
              li.orientationRobustEx(r2, o2, r2, h2) < 0 && (e2 = !e2);
            }
            if (h2.x = r2.x, h2.y = r2.y, a2 = this.m_offsetPts[a2].m_next, a2 === s2) break;
            r2.x = this.m_offsetPts[a2].x, r2.y = this.m_offsetPts[a2].y;
          }
          if (e2) return -1;
          const _ = this.m_offsetPts[t2].m_prev;
          return m2.pt.type = i.type, m2.pt.m_next = s2, m2.pt.m_prev = _, this.m_offsetPts[t2] = m2.pt, this.m_offsetPts[s2].m_prev = t2, s2;
        }
      }
    }
    return -1;
  }
  sectGraphicRect(t2, s2, e, i) {
    return Math.max(t2.x, s2.x) >= Math.min(e.x, i.x) && Math.max(e.x, i.x) >= Math.min(t2.x, s2.x) && Math.max(t2.y, s2.y) >= Math.min(e.y, i.y) && Math.max(e.y, i.y) >= Math.min(t2.y, s2.y);
  }
  findIntersection(t2, s2, e, i) {
    const [n2, o, h, r] = [t2, s2, e, i].map((t3) => t3.asPoint2D());
    if (n2.equals(h)) return [0, 0];
    if (n2.equals(r)) return [0, 1];
    if (o.equals(h)) return [1, 0];
    if (o.equals(r)) return [1, 1];
    const m2 = [];
    return 1 === Pu.segmentIntersectRobust(n2, o, h, r, 1e-15, m2) ? m2 : [-1, -1];
  }
  compressOffsetArray(t2) {
    for (; this.m_offsetPts[t2].m_prev < t2; ) t2 = this.m_offsetPts[t2].m_prev;
    let s2 = 0, e = t2;
    do {
      const t3 = this.m_offsetPts[e].clone();
      this.m_offsetPts[s2] = t3, e = t3.m_next, s2++;
    } while (e !== t2);
    this.m_offsetPts[s2] = this.m_offsetPts[0].clone(), this.m_offsetPtCount = s2 + 1;
  }
  addPart(t2, s2, e) {
    if (!(s2 < 2)) {
      for (let e2 = 0; e2 < s2; e2++) {
        const s3 = this.m_offsetPts[t2 + e2];
        if (e2 ? this.m_bUseZ ? this.m_resultPath.lineTo3DCoords(s3.x, s3.y, s3.z) : this.m_resultPath.lineToCoords(s3.x, s3.y) : this.m_bUseZ ? this.m_resultPath.startPath3DCoords(s3.x, s3.y, s3.z) : this.m_resultPath.startPathCoords(s3.x, s3.y), this.m_bUseM) {
          const t3 = this.m_resultPath.getPointCount() - 1;
          this.m_resultPath.setAttribute(2, t3, 0, s3.m);
        }
      }
      e && this.m_resultPath.closePathWithLine();
    }
  }
  offsetMultiPath(t2) {
    const s2 = kh(this.m_inputGeometry, 0, this.m_tolerance, 0, this.m_progressTracker, 12e3), e = s2.querySegmentIterator();
    e.resetToFirstPath();
    let i = -1;
    for (; e.nextPath(); ) i++, this.offsetPath(s2, i, t2);
  }
  offsetPath(t2, s2, e) {
    let i = t2.getPathStart(s2), n2 = t2.getPathEnd(s2);
    if (this.m_resultPath = e, t2.isClosedPath(s2)) {
      const s3 = t2.getXY(i);
      for (; n2 > i && t2.getXY(n2 - 1).equals(s3); ) n2--;
      if (n2 - i >= 2) {
        this.m_srcPtCount = n2 - i, this.m_srcPts.length = this.m_srcPtCount;
        for (let s4 = i; s4 < n2; s4++) this.progress(), this.m_srcPts[s4 - i] = G.construct(t2.getXY(s4), this.m_bUseZ ? t2.getAttributeAsDbl(1, s4, 0) : 0, this.m_bUseM ? t2.getAttributeAsDbl(2, s4, 0) : 0);
        this.buildOffset() && this.addPart(0, this.m_offsetPtCount - 1, true);
      }
    } else {
      const s3 = t2.getXY(i);
      for (; i < n2 - 1 && t2.getXY(i + 1).equals(s3); ) i++;
      const e2 = t2.getXY(n2 - 1);
      for (; i < n2 - 1 && t2.getXY(n2 - 2).equals(e2); ) n2--;
      if (n2 - i >= 2) {
        this.m_srcPtCount = 2 * (n2 - i) - 2, this.m_srcPts.length = this.m_srcPtCount;
        let e3 = G.construct(s3, this.m_bUseZ ? t2.getAttributeAsDbl(1, i, 0) : 0, this.m_bUseM ? t2.getAttributeAsDbl(2, i, 0) : 0);
        e3.type |= v + A, this.m_srcPts[0] = e3;
        let o = 1, h = this.m_srcPtCount - 1;
        for (let s4 = i + 1; s4 < n2 - 1; s4++, o++, h--) this.progress(), e3 = G.construct(t2.getXY(s4), this.m_bUseZ ? t2.getAttributeAsDbl(1, s4, 0) : 0, this.m_bUseM ? t2.getAttributeAsDbl(2, s4, 0) : 0), this.m_srcPts[o] = e3.clone(), e3.type |= A, this.m_srcPts[h] = e3.clone();
        if (e3 = G.construct(t2.getXY(n2 - 1), this.m_bUseZ ? t2.getAttributeAsDbl(1, n2 - 1, 0) : 0, this.m_bUseM ? t2.getAttributeAsDbl(2, n2 - 1, 0) : 0), e3.type |= v, this.m_srcPts[o] = e3.clone(), this.buildOffset()) if (this.m_offsetPts.length >= 2) {
          let t3 = -1, s4 = -1, e4 = 0 !== (this.m_offsetPts[this.m_offsetPtCount - 1].type & A);
          e4 || (t3 = 0);
          for (let i2 = 1; i2 < this.m_offsetPtCount; i2++) {
            this.progress();
            const n3 = 0 !== (this.m_offsetPts[i2].type & A);
            n3 ? e4 || (s4 = i2 - 1, s4 - t3 + 1 > 1 && this.addPart(t3, s4 - t3 + 1, false)) : e4 && (t3 = i2 - 1), e4 = n3;
          }
          e4 || (s4 = this.m_offsetPtCount - 1, s4 - t3 + 1 > 1 && this.addPart(t3, s4 - t3 + 1, false));
        } else {
          const t3 = 0, s4 = this.m_offsetPtCount - 1;
          s4 - t3 >= 1 && this.addPart(t3, s4 - t3 + 1, false);
        }
      }
    }
    this.m_srcPts.length = 0, this.m_srcPtCount = 0, this.m_offsetPts.length = 0, this.m_offsetPtCount = 0;
  }
  static dotSign(t2, s2, e, i) {
    const n2 = s2.x - t2.x, o = s2.y - t2.y, h = i.x - e.x, r = i.y - e.y, m2 = W(n2, o, h, r);
    return K(m2);
  }
};
var j2 = class {
  getOperatorType() {
    return 10108;
  }
  accelerateGeometry(t2, s2, e) {
    return false;
  }
  canAccelerateGeometry(t2) {
    return false;
  }
  supportsCurves() {
    return true;
  }
  executeMany(t2, s2, e, i, n2, o, h, r = 0) {
    return new k(t2, s2, e, i, n2, o, h, r);
  }
  execute(t2, s2, e, i, n2, o, h, r = 0) {
    return new k(null, s2, e, i, n2, o, h, r).offset(t2);
  }
};
var k = class extends s {
  constructor(t2, s2, e, i, n2, o, h, r) {
    super();
    __publicField(this, "m_inputGeoms");
    __publicField(this, "m_spatialReference");
    __publicField(this, "m_progressTracker");
    __publicField(this, "m_distance");
    __publicField(this, "m_miterLimit");
    __publicField(this, "m_joins");
    __publicField(this, "m_flattenError");
    __publicField(this, "m_index");
    __publicField(this, "m_options");
    this.m_progressTracker = h, this.m_index = -1, this.m_inputGeoms = t2, this.m_spatialReference = s2, this.m_distance = e, this.m_joins = i, this.m_miterLimit = n2, this.m_flattenError = o, this.m_options = r;
  }
  tock() {
    return true;
  }
  getRank() {
    return 1;
  }
  next() {
    if (!this.m_inputGeoms) return null;
    let t2;
    for (; t2 = this.m_inputGeoms.next(); ) return this.m_index = this.m_inputGeoms.getGeometryID(), this.offset(t2);
    return null;
  }
  getGeometryID() {
    return this.m_index;
  }
  offset(t2) {
    let s2;
    return j(t2), s2 = this.m_flattenError <= 0 ? zt(this.m_spatialReference, t2, true).total() : this.m_flattenError, M2(t2, this.m_distance, this.m_joins, this.m_miterLimit, s2, this.m_options, this.m_progressTracker);
  }
};
var I = new j2();
function S(t2, s2, e, i, n2, o) {
  return I.execute(t2, s2, e, i, n2, o, null);
}
function U(s2, e, i, n2, o, h) {
  const r = I.executeMany(new t(s2), e, i, n2, o, h, null);
  return Array.from(r);
}

export {
  S,
  U
};
//# sourceMappingURL=chunk-ETSDBIT5.js.map
