import {
  t
} from "./chunk-NYA5CJQN.js";
import {
  b,
  p
} from "./chunk-HUEY3GJY.js";

// node_modules/@arcgis/core/views/3d/webgl-engine/core/shaderLibrary/shading/Gamma.glsl.js
function a(a2) {
  a2.constants.add("GAMMA", "float", p).constants.add("INV_GAMMA", "float", b).code.add(t`vec3 delinearizeGamma(vec3 color) {
return pow(color, vec3(INV_GAMMA));
}
vec4 delinearizeGamma(vec4 color) {
return vec4(delinearizeGamma(color.rgb), color.a);
}
vec3 linearizeGamma(vec3 color) {
return pow(color, vec3(GAMMA));
}`);
}

export {
  a
};
//# sourceMappingURL=chunk-DLGWZ2FF.js.map
