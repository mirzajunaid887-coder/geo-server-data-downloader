import {
  an
} from "./chunk-2TXEZV2I.js";
import {
  j
} from "./chunk-QBGBPQDQ.js";
import {
  z2 as z
} from "./chunk-DU2DNIJI.js";
import {
  _
} from "./chunk-NA7PETQC.js";
import {
  S
} from "./chunk-7A5FEBIH.js";
import {
  ae
} from "./chunk-VNV5X4EQ.js";
import {
  G
} from "./chunk-FY2PQEPZ.js";
import {
  R,
  c,
  n,
  s,
  u
} from "./chunk-6HS246PE.js";
import {
  r3 as r
} from "./chunk-NXFNHCI4.js";
import {
  U
} from "./chunk-DHG2BGC2.js";

// node_modules/@arcgis/core/layers/raster/functions/rasterProjectionHelper.js
function h() {
  if (!T) throw new r("rasterprojectionhelper-project", "projection operator is not loaded");
}
var p = (e, t, n2, i = 0) => {
  if (1 === n2[0]) return [0, 0];
  let o = 1, r2 = -1, s2 = 1, a = -1;
  for (let g2 = 0; g2 < e.length; g2 += 2) isNaN(e[g2]) || (o = o > e[g2] ? e[g2] : o, r2 = r2 > e[g2] ? r2 : e[g2], s2 = s2 > e[g2 + 1] ? e[g2 + 1] : s2, a = a > e[g2 + 1] ? a : e[g2 + 1]);
  const { cols: l, rows: c2 } = t, f = (r2 - o) / l / n2[0], u2 = (a - s2) / c2 / n2[1], x = 2 * i;
  let m = 0, h2 = false, p2 = [0, 0];
  for (let g2 = 0; g2 < l - 3; g2++) {
    for (let t2 = 0; t2 < c2 - 3; t2++) {
      const n3 = g2 * c2 * 2 + 2 * t2, i2 = (e[n3] + e[n3 + 4] + e[n3 + 4 * c2] + e[n3 + 4 * c2 + 4]) / 4, o2 = (e[n3 + 1] + e[n3 + 5] + e[n3 + 4 * c2 + 1] + e[n3 + 4 * c2 + 5]) / 4, r3 = Math.abs((i2 - e[n3 + 2 * c2 + 2]) / f), s3 = Math.abs((o2 - e[n3 + 2 * c2 + 3]) / u2);
      if (r3 + s3 > m && (m = r3 + s3, p2 = [r3, s3]), x && m > x) {
        h2 = true;
        break;
      }
    }
    if (h2) break;
  }
  return p2;
};
var g = { 3395: 20037508342789244e-9, 3410: 17334193943686873e-9, 3857: 20037508342788905e-9, 3975: 17367530445161372e-9, 4087: 20037508342789244e-9, 4088: 20015108787169147e-9, 6933: 17367530445161372e-9, 32662: 20037508342789244e-9, 53001: 2001508679602057e-8, 53002: 1000754339801029e-8, 53003: 2001508679602057e-8, 53004: 2001508679602057e-8, 53016: 14152803599503474e-9, 53017: 17333573624304302e-9, 53034: 2001508679602057e-8, 53079: 20015114352186374e-9, 53080: 20015114352186374e-9, 54001: 20037508342789244e-9, 54002: 10018754171394624e-9, 54003: 20037508342789244e-9, 54004: 20037508342789244e-9, 54016: 14168658027268292e-9, 54017: 1736753044516137e-8, 54034: 20037508342789244e-9, 54079: 20037508342789244e-9, 54080: 20037508342789244e-9, 54100: 20037508342789244e-9, 54101: 20037508342789244e-9 };
var y = 32;
var d = 4;
var w = d;
var M = /* @__PURE__ */ new Map();
var R2 = /* @__PURE__ */ new Map();
var S2 = 500;
var P = 39.37;
var b = 96;
var G2;
var k;
var E;
var T = false;
async function N() {
  return G2 || (G2 = Promise.withResolvers(), k = await import("./projectOperator-N5QUK3UT.js"), E = await import("./geographicTransformationUtils-TZRR6PW7.js"), k.isLoaded() || await k.load(), E.isLoaded() || await E.load(), T = true, G2.resolve()), G2.promise;
}
function v(e, t, n2) {
  if (h(), n2) {
    const n3 = k.execute(e, t);
    return E.getTransformation(t, e.spatialReference, n3);
  }
  return E.getTransformation(e.spatialReference, t, e);
}
function j2(e, t, n2, o) {
  const r2 = e.spatialReference;
  if (!r2 || !t || r2.equals(t)) return e;
  h();
  const s2 = n2.center, a = new z({ xmin: s2.x - e.x / 2, xmax: s2.x + e.x / 2, ymin: s2.y - e.y / 2, ymax: s2.y + e.y / 2, spatialReference: r2 }), l = k.execute(a, t, { geographicTransformation: o }), c2 = Z(t);
  return null == l || null != c2 && l.width >= c2 ? C(e, r2, t) : { x: l.width, y: l.height };
}
function C(e, t, i) {
  const o = ae(t) / ae(i);
  return { x: e.x * o, y: e.y * o };
}
function _2(e, i, o) {
  const { spatialReference: r2 } = o;
  if (!r2 || !i || r2.equals(i)) return e;
  if (r2.isGeographic && i.isGeographic) return C(e, r2, i);
  if (h(), !T) throw new r("raster-projection-helper:project-dataset-resolution", "geometry engine is not loaded");
  let { x: s2, y: a } = e;
  const l = (s2 + a) / 2 * ae(r2);
  let c2 = 1;
  l > 30 && (c2 = 30 / l, s2 *= c2, a *= c2);
  const u2 = 256, x = s2 * u2 / 2, m = a * u2 / 2, { x: p2, y: g2 } = o.center, y2 = [];
  for (let t = 0; t <= u2; t++) y2.push([p2 - x, g2 - m + t * a]);
  for (let t = 1; t <= u2; t++) y2.push([p2 - x + t * s2, g2 + m]);
  for (let t = 1; t <= u2; t++) y2.push([p2 + x, g2 + m - t * a]);
  for (let t = 1; t < u2; t++) y2.push([p2 + x - t * s2, g2 - m]);
  y2.push(y2[0]);
  const d2 = new j({ rings: [y2], spatialReference: r2 }), w2 = k.execute(d2, i);
  if (!w2) return C(e, r2, i);
  const M2 = w2.extent, R3 = Z(i);
  if (null == M2 || null != R3 && M2.width >= R3) return C(e, r2, i);
  const S3 = he(w2.rings), P2 = s2 * a * u2 * u2, b2 = Math.sqrt(S3 / P2) / c2, G3 = { x: M2.width / c2 / u2, y: M2.height / c2 / u2 }, E2 = { x: s2 * b2, y: a * b2 }, N2 = G3.x * G3.y;
  return Math.abs(N2 - E2.x * E2.y) / N2 < 0.1 ? G3 : E2;
}
function z2(e, t) {
  return ("number" == typeof e ? e : (e.x + e.y) / 2) * ae(t) * b * P;
}
function L(e, t) {
  const { pixelSize: n2, extent: i } = e;
  return z2(_2(n2, t, i), t);
}
function W(e, t = 0.01) {
  return ae(e) ? t / ae(e) : 0;
}
function I(e, t, n2, i = true) {
  const o = e.spatialReference;
  if (o.equals(t)) return e;
  h();
  const r2 = k.execute(e, t, { geographicTransformation: n2 });
  return i && r2 ? (O([e], [r2], o, t), r2) : r2;
}
function O(e, t, n2, i) {
  const o = $(n2, true), r2 = $(i, true), s2 = W(n2, S2), a = W(i, S2);
  if (s2 && null != o && null != r2) for (let l = 0; l < e.length; l++) {
    const n3 = t[l];
    if (!n3) continue;
    const { x: i2 } = e[l], { x: c2 } = n3;
    c2 >= r2[1] - a && Math.abs(i2 - o[0]) < s2 ? n3.x -= r2[1] - r2[0] : c2 <= r2[0] + a && Math.abs(i2 - o[1]) < s2 && (n3.x += r2[1] - r2[0]);
  }
}
function A(e) {
  const { inSR: t, outSR: n2, preferPE: i } = e;
  if (t.equals(n2)) {
    const { points: t2 } = J(e, null);
    return t2;
  }
  if (t.isWebMercator && n2.isWGS84 || t.isWGS84 && n2.isWebMercator) return Y(e);
  if (i) {
    if (t.isGeographic) return q(e);
    if (null != B(t)) return q(e);
  }
  return F(e);
}
function F(e) {
  const { points: t } = J(e, null), { inSR: n2, outSR: i, datumTransformation: o } = e, r2 = t.map((e2) => new _(e2[0], e2[1], n2)), s2 = k.executeMany(r2, i, { geographicTransformation: o });
  return o && O(r2, s2, n2, i), s2.map((e2) => e2 ? [e2.x, e2.y] : [NaN, NaN]);
}
function q(e) {
  const { inSR: t, outSR: n2, datumTransformation: i } = e, s2 = B(t), { points: c2, mask: f } = J(e, s2);
  if (!t.isGeographic) {
    const e2 = t.wkid ? c.coordsys(t.wkid) : c.fromString(t.isGeographic ? s.PE_TYPE_GEOGCS : s.PE_TYPE_PROJCS, t.wkt2 || t.wkt);
    n.projToGeog(e2, c2.length, c2);
  }
  if (null != i && i.steps.length) {
    let e2;
    const t2 = 179.9955;
    if (n2.isGeographic && (e2 = c2.map(([e3]) => e3 > t2 ? 1 : e3 < -t2 ? -1 : 0)), i.steps.forEach((e3) => {
      const t3 = e3.wkid ? c.geogtran(e3.wkid) : c.fromString(s.PE_TYPE_GEOGTRAN, e3.wkt);
      u.geogToGeog(t3, c2.length, c2, null, e3.isInverse ? s.PE_TRANSFORM_2_TO_1 : s.PE_TRANSFORM_1_TO_2);
    }), e2) for (let n3 = 0; n3 < c2.length; n3++) {
      const i2 = e2[n3], o = c2[n3][0], r2 = o > t2 ? 1 : o < -t2 ? -1 : 0;
      i2 && r2 && i2 !== r2 && (c2[n3][0] = i2 > 0 ? o + 360 : o - 360);
    }
  }
  if (!n2.isGeographic) {
    const e2 = B(n2, true), t2 = null != e2 && e2.isEnvelope ? [e2.bbox[1], e2.bbox[3]] : [-90, 90];
    X(c2, t2);
    const i2 = n2.wkid ? c.coordsys(n2.wkid) : c.fromString(n2.isGeographic ? s.PE_TYPE_GEOGCS : s.PE_TYPE_PROJCS, n2.wkt2 || n2.wkt);
    n.geogToProj(i2, c2.length, c2);
  }
  let u2 = c2;
  if (f && c2.length !== f.length) {
    u2 = [];
    for (let e2 = 0, t2 = 0; e2 < f.length; e2++) f[e2] ? u2.push(c2[t2++]) : u2.push([NaN, NaN]);
  }
  return u2;
}
function Y(e) {
  const { cols: t, rows: n2, xres: i, yres: o, usePixelCenter: r2, inSR: s2, outSR: a } = e;
  let { xmin: l, ymax: f } = e;
  r2 && (l += i / 2, f -= o / 2);
  const x = [], m = [], h2 = Math.max(t, n2);
  for (let g2 = 0; g2 < h2; g2++) {
    const e2 = l + i * Math.min(t, g2), r3 = f - o * Math.min(n2, g2), h3 = an(new _({ x: e2, y: r3, spatialReference: s2 }), s2, a);
    g2 <= t && x.push(h3.x), g2 <= n2 && m.push(h3.y);
  }
  const p2 = [];
  for (let c2 = 0; c2 < t; c2++) for (let e2 = 0; e2 < n2; e2++) p2.push([x[c2], m[e2]]);
  return p2;
}
function B(e, t = false) {
  let n2 = e.wkid || e.wkt2 || e.wkt;
  if (!n2 || e.isGeographic) return null;
  if (n2 = String(n2), M.has(n2)) {
    const e2 = M.get(n2);
    return t ? e2 == null ? void 0 : e2.gcs : e2 == null ? void 0 : e2.pcs;
  }
  const i = e.wkid ? c.coordsys(e.wkid) : c.fromString(e.isGeographic ? s.PE_TYPE_GEOGCS : s.PE_TYPE_PROJCS, e.wkt2 || e.wkt), s2 = U2(i, W(e, 1e-4)), a = U2(i, 0, true);
  return M.set(n2, { pcs: s2, gcs: a }), t ? a : s2;
}
function U2(e, t = 0, n2 = false) {
  const i = R.generate(e), o = n2 ? e.horizonGcsGenerate() : e.horizonPcsGenerate();
  if (!i || !(o == null ? void 0 : o.length)) return null;
  let r2 = false, a = o.find((e2) => 1 === e2.getInclusive() && 1 === e2.getKind());
  if (!a) {
    if (a = o.find((e2) => 1 === e2.getInclusive() && 0 === e2.getKind()), !a) return null;
    r2 = true;
  }
  const l = n2 ? 0 : (2 === i.getNorthPoleLocation() ? 1 : 0) | (2 === i.getSouthPoleLocation() ? 2 : 0), c2 = i.isPannableRectangle(), f = a.getCoord();
  if (r2) return { isEnvelope: r2, isPannable: c2, vertices: f, coef: null, bbox: [f[0][0] - t, f[0][1] - t, f[1][0] + t, f[1][1] + t], poleLocation: l };
  let u2 = 0;
  const x = [];
  let [m, h2] = f[0], [p2, g2] = f[0];
  for (let s2 = 0, y2 = f.length; s2 < y2; s2++) {
    u2++, u2 === y2 && (u2 = 0);
    const [e2, t2] = f[s2], [n3, i2] = f[u2];
    if (i2 === t2) x.push([e2, n3, t2, i2, 2]);
    else {
      const o2 = (n3 - e2) / (i2 - t2 || 1e-4), r3 = e2 - o2 * t2;
      t2 < i2 ? x.push([o2, r3, t2, i2, 0]) : x.push([o2, r3, i2, t2, 1]);
    }
    m = m < e2 ? m : e2, h2 = h2 < t2 ? h2 : t2, p2 = p2 > e2 ? p2 : e2, g2 = g2 > t2 ? g2 : t2;
  }
  return { isEnvelope: false, isPannable: c2, vertices: f, coef: x, bbox: [m, h2, p2, g2], poleLocation: l };
}
function J(e, t) {
  const n2 = [], { cols: i, rows: o, xres: r2, yres: s2, usePixelCenter: a } = e;
  let { xmin: l, ymax: c2 } = e;
  if (a && (l += r2 / 2, c2 -= s2 / 2), null == t) {
    for (let e2 = 0; e2 < i; e2++) for (let t2 = 0; t2 < o; t2++) n2.push([l + r2 * e2, c2 - s2 * t2]);
    return { points: n2 };
  }
  const f = new Uint8Array(i * o);
  if (t.isEnvelope) {
    const { isPannable: e2, bbox: [a2, u3, x2, m] } = t;
    for (let t2 = 0, h2 = 0; t2 < i; t2++) {
      const i2 = l + r2 * t2, p2 = e2 || i2 >= a2 && i2 <= x2;
      for (let e3 = 0; e3 < o; e3++, h2++) {
        const t3 = c2 - s2 * e3;
        p2 && t3 >= u3 && t3 <= m && (n2.push([i2, t3]), f[h2] = 1);
      }
    }
    return { points: n2, mask: f };
  }
  const u2 = t.coef, x = [];
  for (let m = 0; m < o; m++) {
    const e2 = c2 - s2 * m, t2 = [], n3 = [];
    for (let o2 = 0; o2 < u2.length; o2++) {
      const [i3, r3, s3, a2, l2] = u2[o2];
      if (e2 === s3 && s3 === a2) t2.push(i3), t2.push(r3), n3.push(2), n3.push(2);
      else if (e2 >= s3 && e2 <= a2) {
        const o3 = i3 * e2 + r3;
        t2.push(o3), n3.push(l2);
      }
    }
    let i2 = t2;
    if (t2.length > 2) {
      let e3 = 2 === n3[0] ? 0 : n3[0], o2 = t2[0];
      i2 = [];
      for (let r3 = 1; r3 < n3.length; r3++) 2 === n3[r3] && r3 !== n3.length - 1 || (n3[r3] !== e3 && (i2.push(0 === e3 ? Math.min(o2, t2[r3 - 1]) : Math.max(o2, t2[r3 - 1])), e3 = n3[r3], o2 = t2[r3]), r3 === n3.length - 1 && i2.push(0 === n3[r3] ? Math.min(o2, t2[r3]) : Math.max(o2, t2[r3])));
      i2.sort((e4, t3) => e4 - t3);
    } else t2[0] > t2[1] && (i2 = [t2[1], t2[0]]);
    x.push(i2);
  }
  for (let m = 0, h2 = 0; m < i; m++) {
    const e2 = l + r2 * m;
    for (let t2 = 0; t2 < o; t2++, h2++) {
      const i2 = c2 - s2 * t2, o2 = x[t2];
      if (2 === o2.length) e2 >= o2[0] && e2 <= o2[1] && (n2.push([e2, i2]), f[h2] = 1);
      else if (o2.length > 2) {
        let t3 = false;
        for (let n3 = 0; n3 < o2.length; n3 += 2) if (e2 >= o2[n3] && e2 <= o2[n3 + 1]) {
          t3 = true;
          break;
        }
        t3 && (n2.push([e2, i2]), f[h2] = 1);
      }
    }
  }
  return { points: n2, mask: f };
}
function X(e, t) {
  const [n2, i] = t;
  for (let o = 0; o < e.length; o++) {
    const t2 = e[o][1];
    (t2 < n2 || t2 > i) && (e[o] = [NaN, NaN]);
  }
}
function K(e, t) {
  const n2 = Z(e[0].spatialReference);
  if (e.length < 2 || null == n2) return e[0];
  if (t = t ?? W(e[0].spatialReference), 1 === (e = e.filter((e2) => e2.width > t)).length) return e[0];
  let { xmin: o, xmax: r2, ymin: s2, ymax: a } = e[0];
  for (let i = 1; i < e.length; i++) {
    const t2 = e[i];
    r2 = t2.xmax + n2 * i, s2 = Math.min(s2, t2.ymin), a = Math.max(a, t2.ymax);
  }
  return new z({ xmin: o, xmax: r2, ymin: s2, ymax: a, spatialReference: e[0].spatialReference });
}
function D(t, n2, o = null, r2 = true) {
  const s2 = t.spatialReference;
  if (s2.equals(n2) || !n2) return t;
  const a = te(t), l = Z(s2, true), c2 = Z(n2);
  if (0 === a || null == l || null == c2) {
    const e = V(t, n2, o, r2);
    if (e && null == l && null != c2 && Math.abs(e.width - c2) < W(n2) && k.isLoaded()) {
      const i = B(s2);
      if (null != i && 0 === i.poleLocation && t.width < (i.bbox[2] - i.bbox[0]) / 2) return Q(t, n2) || e;
    }
    return e;
  }
  const f = t.clone().normalize();
  if (1 === f.length && t.xmax < l && t.xmax - l / 2 > W(s2)) {
    const { xmin: e, xmax: n3 } = t;
    for (let o2 = 0; o2 <= a; o2++) {
      const r3 = 0 === o2 ? e : -l / 2, c3 = o2 === a ? n3 - l * o2 : l / 2;
      f[o2] = new z({ xmin: r3, xmax: c3, ymin: t.ymin, ymax: t.ymax, spatialReference: s2 });
    }
  }
  const u2 = f.map((e) => V(e, n2, o, r2)).filter(U);
  return 0 === u2.length ? null : K(u2);
}
function H(e, t, n2) {
  if ("extent" === e.type) {
    const { xmin: t2, ymin: n3, xmax: i, ymax: o, spatialReference: r2 } = e;
    e = new j({ rings: [[[t2, o], [i, o], [i, n3], [t2, n3], [t2, o]]], spatialReference: r2 });
  }
  return e.spatialReference.equals(t) ? e : (h(), k.execute(e, t, { geographicTransformation: n2 }));
}
function Q(e, t) {
  const n2 = Z(t);
  if (null == n2) return null;
  let { xmin: o, ymin: r2, xmax: s2, ymax: a } = e;
  const l = e.spatialReference, c2 = new j({ spatialReference: l, rings: [[[o, r2], [s2, r2], [s2, a], [o, a], [o, r2]]] }), u2 = k.execute(c2, t);
  if (2 !== u2.rings.length || !u2.rings[0].length || !u2.rings[1].length) return null;
  const { rings: x } = u2, m = W(l), h2 = new z({ spatialReference: t });
  for (let i = 0; i < 2; i++) {
    o = s2 = x[i][0][0], r2 = a = x[i][0][1];
    for (let e2 = 0; e2 < x[i].length; e2++) o = o > x[i][e2][0] ? x[i][e2][0] : o, s2 = s2 < x[i][e2][0] ? x[i][e2][0] : s2, r2 = r2 > x[i][e2][1] ? x[i][e2][1] : r2, a = a < x[i][e2][1] ? x[i][e2][1] : a;
    if (0 === i) h2.ymin = r2, h2.ymax = a, h2.xmin = o, h2.xmax = s2;
    else if (h2.ymin = Math.min(h2.ymin, r2), h2.ymax = Math.max(h2.ymax, a), Math.abs(s2 - n2 / 2) < m) h2.xmin = o, h2.xmax = h2.xmax + n2;
    else {
      if (!(Math.abs(o + n2 / 2) < m)) return null;
      h2.xmax = s2 + n2;
    }
  }
  return h2;
}
function V(e, t, n2, i = true, o = true) {
  const r2 = e.spatialReference;
  if (r2.equals(t) || !t) return e;
  h();
  const s2 = k.execute(e, t, { geographicTransformation: n2 });
  if (o && t.isWebMercator && s2 && (s2.ymax = Math.min(20037508342787e-6, s2.ymax), s2.ymin = Math.max(-20037508342787e-6, s2.ymin), s2.ymin >= s2.ymax)) return null;
  if (!i || !s2) return s2;
  const a = $(r2, true), l = $(t, true);
  if (null == a || null == l) return s2;
  const f = W(r2, 1e-3), u2 = W(r2, S2), x = W(t, 1e-3);
  if (Math.abs(s2.xmin - l[0]) < x && Math.abs(s2.xmax - l[1]) < x) {
    const i2 = Math.abs(e.xmin - a[0]), o2 = Math.abs(a[1] - e.xmax);
    if (i2 < f && o2 > u2) {
      s2.xmin = l[0];
      const i3 = [];
      i3.push(new _(e.xmax, e.ymin, r2)), i3.push(new _(e.xmax, (e.ymin + e.ymax) / 2, r2)), i3.push(new _(e.xmax, e.ymax, r2));
      const o3 = i3.map((e2) => I(e2, t, n2)).filter((e2) => !isNaN(e2 == null ? void 0 : e2.x)).map((e2) => e2.x);
      s2.xmax = Math.max.apply(null, o3);
    }
    if (o2 < f && i2 > u2) {
      s2.xmax = l[1];
      const i3 = [];
      i3.push(new _(e.xmin, e.ymin, r2)), i3.push(new _(e.xmin, (e.ymin + e.ymax) / 2, r2)), i3.push(new _(e.xmin, e.ymax, r2));
      const o3 = i3.map((e2) => I(e2, t, n2)).filter((e2) => !isNaN(e2 == null ? void 0 : e2.x)).map((e2) => e2.x);
      s2.xmin = Math.min.apply(null, o3);
    }
  } else {
    const e2 = W(t, 1e-3);
    Math.abs(s2.xmin - l[0]) < e2 && (s2.xmin = l[0]), Math.abs(s2.xmax - l[1]) < e2 && (s2.xmax = l[1]);
  }
  return s2;
}
function Z(e, t = false) {
  if (!e) return null;
  const n2 = t ? 20037508342787e-6 : 20037508342788905e-9;
  return e.isWebMercator ? 2 * n2 : e.wkid && e.isGeographic ? 360 : 2 * g[e.wkid] || null;
}
function $(e, t = false) {
  if (e.isGeographic) return [-180, 180];
  const n2 = Z(e, t);
  return n2 ? [-n2 / 2, n2 / 2] : null;
}
function ee(e, t, n2, i) {
  let o = (e - t) / n2;
  return o - Math.floor(o) !== 0 ? o = Math.floor(o) : i && (o -= 1), o;
}
function te(e, t = false) {
  const n2 = Z(e.spatialReference);
  if (null == n2) return 0;
  const i = t ? 0 : -(n2 / 2), o = W(e.spatialReference), r2 = !t && Math.abs(e.xmax - n2 / 2) < o ? n2 / 2 : e.xmax, s2 = !t && Math.abs(e.xmin + n2 / 2) < o ? -n2 / 2 : e.xmin;
  return ee(r2, i, n2, true) - ee(s2, i, n2, false);
}
function ne(e) {
  const t = e.storageInfo.origin.x, n2 = Z(e.spatialReference, true);
  if (null == n2) return { originX: t, halfWorldWidth: null, pyramidsInfo: null };
  const i = n2 / 2, { nativePixelSize: o, storageInfo: r2, extent: s2 } = e, { maximumPyramidLevel: a, blockWidth: l, pyramidScalingFactor: c2 } = r2;
  let f = o.x;
  const u2 = [], x = null != e.transform && "gcs-shift" === e.transform.type, m = t + (x ? 0 : i), h2 = x ? n2 - t : i - t;
  for (let p2 = 0; p2 <= a; p2++) {
    const e2 = (s2.xmax - t) / f / l, n3 = e2 - Math.floor(e2) === 0 ? e2 : Math.ceil(e2), i2 = h2 / f / l, o2 = i2 - Math.floor(i2) === 0 ? i2 : Math.ceil(i2), r3 = Math.floor(m / f / l), a2 = Math.round(m / f) % l, x2 = (l - Math.round(h2 / f) % l) % l;
    u2.push({ resolutionX: f, blockWidth: l, datasetColumnCount: n3, worldColumnCountFromOrigin: o2, leftMargin: a2, rightPadding: x2, originColumnOffset: r3 }), f *= c2;
  }
  return { originX: t, halfWorldWidth: i, pyramidsInfo: u2, hasGCSSShiftTransform: x };
}
function ie(e) {
  if (!e || e.isGeographic) return e;
  const t = String(e.wkid || e.wkt2 || e.wkt);
  let n2;
  if (R2.has(t)) n2 = R2.get(t);
  else {
    n2 = (e.wkid ? c.coordsys(e.wkid) : c.fromString(s.PE_TYPE_PROJCS, e.wkt2 || e.wkt)).getGeogcs().getCode(), R2.set(t, n2);
  }
  return new S({ wkid: n2 });
}
function oe(e) {
  const t = e.isAdaptive && null == e.spacing;
  let n2 = e.spacing || [y, y], i = re(e), o = { cols: i.size[0] + 1, rows: i.size[1] + 1 };
  const r2 = i.outofBoundPointCount > 0 && i.outofBoundPointCount < i.offsets.length / 2;
  let s2 = i.outofBoundPointCount === i.offsets.length / 2 || t && r2 ? [0, 0] : p(i.offsets, o, n2, w);
  const a = (s2[0] + s2[1]) / 2, l = e.projectedExtent.spatialReference, c2 = e.srcBufferExtent.spatialReference;
  if (t && (r2 || a > w) && (l.isGeographic || B(l), n2 = [d, d], i = re({ ...e, spacing: n2 }), o = { cols: i.size[0] + 1, rows: i.size[1] + 1 }, s2 = p(i.offsets, o, n2, w)), i.error = s2, n2[0] > 1 && (i.coefficients = se(i.offsets, o, r2)), e.includeGCSGrid && !l.isGeographic && !l.isWebMercator) if (c2.isGeographic) i.gcsGrid = { offsets: i.offsets, coefficients: i.coefficients, spacing: n2 };
  else {
    const t2 = B(l);
    if (null != t2 && !t2.isEnvelope) {
      const t3 = ie(l), s3 = D(e.projectedExtent, t3), { offsets: a2 } = re({ ...e, srcBufferExtent: s3, spacing: n2 }), c3 = se(a2, o, r2);
      i.gcsGrid = { offsets: a2, coefficients: c3, spacing: n2 };
    }
  }
  return i;
}
function re(e) {
  const { projectedExtent: t, srcBufferExtent: n2, pixelSize: i, datumTransformation: o, rasterTransform: r2 } = e, s2 = t.spatialReference, a = n2.spatialReference;
  h();
  const { xmin: l, ymin: f, xmax: u2, ymax: x } = t, m = Z(a), p2 = null != m && (e.hasWrapAround || "gcs-shift" === (r2 == null ? void 0 : r2.type)), g2 = e.spacing || [y, y], w2 = g2[0] * i.x, M2 = g2[1] * i.y, R3 = 1 === g2[0], P2 = Math.ceil((u2 - l) / w2 - 0.1 / g2[0]) + (R3 ? 0 : 1), b2 = Math.ceil((x - f) / M2 - 0.1 / g2[1]) + (R3 ? 0 : 1), G3 = A({ cols: P2, rows: b2, xmin: l, ymax: x, xres: w2, yres: M2, inSR: s2, outSR: a, datumTransformation: o, preferPE: g2[0] <= d, usePixelCenter: R3 }), k2 = [];
  let E2, T2 = 0;
  const N2 = R3 ? -1 : NaN, { xmin: v2, xmax: j3, ymax: C2, width: _3, height: z3 } = n2, L2 = W(a, S2), I2 = null != m && v2 > 0 && j3 > m / 2, O2 = B(s2), F2 = null != O2 && O2.poleLocation > 0;
  for (let h2 = 0; h2 < P2; h2++) {
    const e2 = [];
    for (let t2 = 0; t2 < b2; t2++) {
      let n3 = G3[h2 * b2 + t2];
      if (p2 && n3[0] > j3 && n3[0] > m / 2 - L2 ? n3[0] -= m : p2 && 0 === h2 && n3[0] < 0 && I2 && !r2 && (n3[0] += m), !n3 || isNaN(n3[0]) || isNaN(n3[1])) k2.push(N2), k2.push(N2), e2.push(null), T2++;
      else {
        if (r2) {
          const e3 = r2.inverseTransform(new _({ x: n3[0], y: n3[1], spatialReference: a }));
          n3 = [e3.x, e3.y];
        }
        e2.push(n3), h2 > 0 && p2 && E2[t2] && n3[0] < E2[t2][0] && (n3[0] += m, F2 && n3[0] > j3 && n3[0] > m && (n3[0] -= m)), k2.push((n3[0] - v2) / _3), k2.push((C2 - n3[1]) / z3);
      }
    }
    E2 = e2;
  }
  return { offsets: k2, error: null, coefficients: null, outofBoundPointCount: T2, spacing: g2, size: R3 ? [P2, b2] : [P2 - 1, b2 - 1] };
}
function se(e, t, n2) {
  const { cols: i, rows: o } = t, r2 = new Float32Array((i - 1) * (o - 1) * 2 * 6), s2 = new Float32Array([-0, -1, 1, -1, 1, -0, 1, -0, -0]), a = new Float32Array([-1, 1, 0, 0, -1, 1, 1, 0, 0]);
  for (let l = 0; l < i - 1; l++) {
    for (let t2 = 0; t2 < o - 1; t2++) {
      let n3 = l * o * 2 + 2 * t2;
      const c2 = e[n3], f = e[n3 + 1], u2 = e[n3 + 2], x = e[n3 + 3];
      n3 += 2 * o;
      const m = e[n3], h2 = e[n3 + 1], p2 = e[n3 + 2], g2 = e[n3 + 3];
      let y2 = 0, d2 = 12 * (t2 * (i - 1) + l);
      for (let e2 = 0; e2 < 3; e2++) r2[d2++] = s2[y2++] * c2 + s2[y2++] * u2 + s2[y2++] * p2;
      y2 = 0;
      for (let e2 = 0; e2 < 3; e2++) r2[d2++] = s2[y2++] * f + s2[y2++] * x + s2[y2++] * g2;
      y2 = 0;
      for (let e2 = 0; e2 < 3; e2++) r2[d2++] = a[y2++] * c2 + a[y2++] * m + a[y2++] * p2;
      y2 = 0;
      for (let e2 = 0; e2 < 3; e2++) r2[d2++] = a[y2++] * f + a[y2++] * h2 + a[y2++] * g2;
    }
    if (n2) for (let e2 = 0; e2 < r2.length; e2++) isNaN(r2[e2]) && (r2[e2] = -1);
  }
  return r2;
}
function ae2(e, t) {
  const n2 = e.clone().normalize();
  return 1 === n2.length ? n2[0] : K(n2, t);
}
function le(e) {
  const { spatialReference: t } = e, n2 = G(t);
  if (!n2) return e;
  const [o, r2] = n2.valid, s2 = r2 - o;
  let a = 0;
  if (e.xmin < o) {
    const t2 = o - e.xmin;
    a = Math.ceil(t2 / s2);
  } else if (e.xmin > r2) {
    const t2 = e.xmin - r2;
    a = -Math.ceil(t2 / s2);
  }
  return new z({ spatialReference: e.spatialReference, xmin: e.xmin + a * s2, ymin: e.ymin, xmax: e.xmax + a * s2, ymax: e.ymax });
}
function ce(e, t, i) {
  var _a;
  const { storageInfo: o, pixelSize: r2 } = t;
  let s2 = 0, a = false;
  const { pyramidResolutions: l } = o, f = "mixed" === ((_a = o.tileInfo.format) == null ? void 0 : _a.toLowerCase()) ? Math.max(1, Math.min(3, o.tileInfo.dpi / 96)) : 1, u2 = (e.x + e.y) / 2 / f;
  if (null != l && l.length) {
    const e2 = l[l.length - 1], o2 = (e2.x + e2.y) / 2, f2 = (r2.x + r2.y) / 2;
    if (u2 <= f2) s2 = 0;
    else if (u2 >= o2) s2 = l.length, a = u2 / o2 > 8;
    else {
      let e3, t2 = f2;
      for (let n2 = 1; n2 <= l.length; n2++) {
        if (e3 = (l[n2 - 1].x + l[n2 - 1].y) / 2, u2 <= e3) {
          u2 === e3 ? s2 = n2 : "down" === i ? (s2 = n2 - 1, a = u2 / t2 > 8) : s2 = "up" === i || u2 - t2 > e3 - u2 || u2 / t2 > 2 ? n2 : n2 - 1;
          break;
        }
        t2 = e3;
      }
    }
    const x2 = 0 === s2 ? r2 : l[s2 - 1];
    if (a) {
      Math.min(x2.x, x2.y) * ae(t.spatialReference) > 19567 && (a = false);
    }
    return { pyramidLevel: s2, pyramidResolution: new _({ x: x2.x, y: x2.y, spatialReference: t.spatialReference }), excessiveReading: a };
  }
  const x = Math.log(e.x / r2.x) / Math.LN2, m = Math.log(e.y / r2.y) / Math.LN2, h2 = t.storageInfo.maximumPyramidLevel || 0;
  s2 = "down" === i ? Math.floor(Math.min(x, m)) : "up" === i ? Math.ceil(Math.max(x, m)) : Math.round((x + m) / 2), s2 < 0 ? s2 = 0 : s2 > h2 && (a = s2 > h2 + 3, s2 = h2);
  const p2 = 2 ** s2;
  return { pyramidLevel: s2, pyramidResolution: new _({ x: p2 * t.nativePixelSize.x, y: p2 * t.nativePixelSize.y, spatialReference: t.spatialReference }), excessiveReading: a };
}
function fe(e, t, n2 = false) {
  const { pixelSize: i, extent: o } = e, r2 = v(o, t, false), s2 = D(ae2(o, (i.x + i.y) / 16), t, r2);
  return !s2 || n2 || 0 === te(s2) ? s2 : V(o, t, r2);
}
function ue(e, t, n2) {
  var _a;
  const i = (n2 == null ? void 0 : n2.tileSize) ?? 512, o = (n2 == null ? void 0 : n2.alignGlobalDatasetWithAGOL) ?? true, r2 = !!(n2 == null ? void 0 : n2.limitToSrcResolution), { extent: s2, pixelSize: a } = e, l = _2(a, t, s2);
  if (null == l) return { projectedPixelSize: null, scales: null, srcResolutions: null, isCustomTilingScheme: false };
  const c2 = (l.x + l.y) / 2, f = z2(c2, t), u2 = 256 / i, x = t.isGeographic ? 2958287637958547e-7 * u2 : 591657527591555e-6 * u2, m = (t.isGeographic ? 0.703125 : 156543.03392800014) * u2;
  let h2 = "vector-magdir" === e.dataType || "vector-uv" === e.dataType;
  const p2 = fe(e, t, true), g2 = Math.min(Math.ceil(Math.log(Math.min(e.width, e.height) / 32) / Math.LN2), Math.ceil(Math.log(x / 2 / f) / Math.LN2));
  if (!h2 && p2 && o && (t.isGeographic || t.isWebMercator)) {
    const n3 = Z(t);
    if (h2 = te(p2) > 0 || null != n3 && p2.width > n3 / 4, !h2 && null != n3) {
      let t2 = -1;
      if (g2 < 3) t2 = 2 ** g2 * c2 * i;
      else if (e.storageInfo) {
        const { maximumPyramidLevel: n4 = 0, pyramidScalingFactor: o3 = 2 } = e.storageInfo;
        t2 = o3 ** n4 * c2 * i;
      }
      const o2 = Math.ceil(n3 / t2);
      h2 = 1 === o2 || 2 === o2 && n3 / 2 - p2.xmax < t2;
    }
  }
  let y2, d2 = f;
  const w2 = 1.001, M2 = Math.min(2, Math.max(1.414, ((_a = e.storageInfo) == null ? void 0 : _a.pyramidScalingFactor) || 2));
  if (h2) {
    d2 = x;
    const n3 = xe(e, t);
    y2 = { x: n3.x * m, y: n3.y * m };
  } else {
    y2 = { x: a.x, y: a.y };
    let e2 = 0;
    for (; d2 < x * (w2 / 2) && e2 < g2; ) e2++, d2 *= M2, y2.x *= M2, y2.y *= M2;
    Math.max(d2, x) / Math.min(d2, x) <= w2 && (d2 = x);
  }
  const R3 = [d2], S3 = [{ x: y2.x, y: y2.y }], P2 = 70.5310735, b2 = Math.min(P2, f) / w2;
  for (; d2 >= b2; ) d2 /= M2, y2.x /= M2, y2.y /= M2, R3.push(d2), S3.push({ x: y2.x, y: y2.y });
  if (r2) {
    const e2 = 1e-3 * a.x;
    let t2 = S3.findIndex((t3) => t3.x >= a.x - e2 && t3.x <= a.x + e2);
    t2 > -1 ? (S3.length = t2 + 1, R3.length = t2 + 1) : (t2 = S3.findIndex((t3) => t3.x <= a.x + e2), t2 > 0 && (S3.length = t2, R3.length = t2));
  }
  return { projectedPixelSize: l, scales: R3, srcResolutions: S3, isCustomTilingScheme: !h2 };
}
function xe(e, t) {
  if (t.isWGS84 || t.isWebMercator) {
    const n3 = t.isGeographic ? 1341104507446289e-21 : 0.29858214164761665, i2 = t.isGeographic ? 4326 : 3857, o = fe(e, new S({ wkid: i2 }), true), r2 = _2({ x: n3, y: n3 }, e.spatialReference, o);
    return { x: r2.x / n3, y: r2.y / n3 };
  }
  const n2 = _2(e.pixelSize, t, e.extent), i = (n2.x + n2.y) / 2;
  return { x: e.pixelSize.x / i, y: e.pixelSize.y / i };
}
function he(e) {
  let t = 0;
  for (const n2 of e) {
    const e2 = n2.length;
    let i = n2[0][0] * (n2[1][1] - n2[e2 - 2][1]);
    for (let t2 = 1; t2 < e2 - 1; t2++) i += n2[t2][0] * (n2[t2 + 1][1] - n2[t2 - 1][1]);
    t += i / 2;
  }
  return Math.abs(t);
}

export {
  N,
  v,
  j2 as j,
  L,
  I,
  D,
  H,
  Z,
  te,
  ne,
  oe,
  le,
  ce,
  fe,
  ue
};
//# sourceMappingURL=chunk-WJ3ES3UM.js.map
