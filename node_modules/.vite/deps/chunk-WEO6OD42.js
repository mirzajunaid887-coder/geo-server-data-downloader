import {
  c
} from "./chunk-ULPLLKJC.js";
import {
  a as a2
} from "./chunk-EH7RNYGW.js";
import {
  e as e3
} from "./chunk-YVHZBCEM.js";
import {
  r as r2
} from "./chunk-7BJKTGZL.js";
import {
  a
} from "./chunk-DLGWZ2FF.js";
import {
  e as e2
} from "./chunk-UEK4F7VK.js";
import {
  r
} from "./chunk-URRK3ZOJ.js";
import {
  e
} from "./chunk-VKNLVFIM.js";
import {
  o
} from "./chunk-EUHRUHW5.js";
import {
  s
} from "./chunk-BCEJLVA4.js";
import {
  t
} from "./chunk-NYA5CJQN.js";
import {
  n
} from "./chunk-S2HMSOEZ.js";
import {
  u
} from "./chunk-OSHDQB3V.js";

// node_modules/@arcgis/core/chunks/SSAO.glsl.js
var m = 16;
function v() {
  const r3 = new s(), v2 = r3.fragment;
  return r3.include(o), r3.include(c), v2.include(a2), v2.include(a), v2.uniforms.add(new r2("radius", (e4) => p(e4.camera))).code.add(t`vec3 sphere[16] = vec3[16](
vec3(0.186937, 0.0, 0.0),
vec3(0.700542, 0.0, 0.0),
vec3(-0.864858, -0.481795, -0.111713),
vec3(-0.624773, 0.102853, -0.730153),
vec3(-0.387172, 0.260319, 0.007229),
vec3(-0.222367, -0.642631, -0.707697),
vec3(-0.01336, -0.014956, 0.169662),
vec3(0.122575, 0.1544, -0.456944),
vec3(-0.177141, 0.85997, -0.42346),
vec3(-0.131631, 0.814545, 0.524355),
vec3(-0.779469, 0.007991, 0.624833),
vec3(0.308092, 0.209288,0.35969),
vec3(0.359331, -0.184533, -0.377458),
vec3(0.192633, -0.482999, -0.065284),
vec3(0.233538, 0.293706, -0.055139),
vec3(0.417709, -0.386701, 0.442449)
);
float fallOffFunction(float vv, float vn, float bias) {
float f = max(radius * radius - vv, 0.0);
return f * f * f * max(vn - bias, 0.0);
}`), v2.code.add(t`float aoValueFromPositionsAndNormal(vec3 C, vec3 n_C, vec3 Q) {
vec3 v = Q - C;
float vv = dot(v, v);
float vn = dot(normalize(v), n_C);
return fallOffFunction(vv, vn, 0.1);
}`), r3.outputs.add("fragOcclusion", "float"), v2.uniforms.add(new e("normalMap", (e4) => e4.normalTexture), new e("depthMap", (e4) => e4.depthTexture), new r("projScale", (e4) => e4.projScale), new e("rnm", (e4) => e4.noiseTexture), new e3("rnmScale", (r4, t2) => u(g, t2.camera.fullWidth / r4.noiseTexture.descriptor.width, t2.camera.fullHeight / r4.noiseTexture.descriptor.height)), new r("intensity", (e4) => e4.intensity), new e2("screenSize", (r4) => u(g, r4.camera.fullWidth, r4.camera.fullHeight))).main.add(t`
    float depth = depthFromTexture(depthMap, uv);

    // Early out if depth is out of range, such as in the sky
    if (depth >= 1.0 || depth <= 0.0) {
      fragOcclusion = 1.0;
      return;
    }

    // get the normal of current fragment
    ivec2 iuv = ivec2(uv * vec2(textureSize(normalMap, 0)));
    vec4 norm4 = texelFetch(normalMap, iuv, 0);
    if(norm4.a != 1.0) {
      fragOcclusion = 1.0;
      return;
    }
    vec3 norm = normalize(norm4.xyz * 2.0 - 1.0);

    float currentPixelDepth = linearizeDepth(depth);
    vec3 currentPixelPos = reconstructPosition(gl_FragCoord.xy, currentPixelDepth);

    float sum = 0.0;
    vec3 tapPixelPos;

    vec3 fres = normalize(2.0 * texture(rnm, uv * rnmScale).xyz - 1.0);

    // note: the factor 2.0 should not be necessary, but makes ssao much nicer.
    // bug or deviation from CE somewhere else?
    float ps = projScale / (2.0 * currentPixelPos.z * zScale.x + zScale.y);

    for(int i = 0; i < ${t.int(m)}; ++i) {
      vec2 unitOffset = reflect(sphere[i], fres).xy;
      vec2 offset = vec2(-unitOffset * radius * ps);

      // don't use current or very nearby samples
      if( abs(offset.x) < 2.0 || abs(offset.y) < 2.0){
        continue;
      }

      vec2 tc = vec2(gl_FragCoord.xy + offset);
      if (tc.x < 0.0 || tc.y < 0.0 || tc.x > screenSize.x || tc.y > screenSize.y) continue;
      vec2 tcTap = tc / screenSize;
      float occluderFragmentDepth = linearDepthFromTexture(depthMap, tcTap);

      tapPixelPos = reconstructPosition(tc, occluderFragmentDepth);

      sum += aoValueFromPositionsAndNormal(currentPixelPos, norm, tapPixelPos);
    }

    // output the result
    float A = max(1.0 - sum * intensity / float(${t.int(m)}), 0.0);

    // Anti-tone map to reduce contrast and drag dark region farther: (x^0.2 + 1.2 * x^4) / 2.2
    A = (pow(A, 0.2) + 1.2 * pow(A, 4.0)) * INV_GAMMA;

    fragOcclusion = A;
  `), r3;
}
function p(e4) {
  return Math.max(10, 20 * e4.computeScreenPixelSizeAtDist(Math.abs(4 * e4.relativeElevation)));
}
var g = n();
var h = Object.freeze(Object.defineProperty({ __proto__: null, build: v, getRadius: p }, Symbol.toStringTag, { value: "Module" }));

export {
  v,
  p,
  h
};
//# sourceMappingURL=chunk-WEO6OD42.js.map
