import {
  e as e4,
  f as f2
} from "./chunk-DITDYFBM.js";
import {
  r as r3
} from "./chunk-DDSKY4JI.js";
import {
  o
} from "./chunk-APFTN4C3.js";
import {
  r as r2
} from "./chunk-JSFR2L6V.js";
import {
  u
} from "./chunk-CAZJRW52.js";
import {
  e
} from "./chunk-2VXC2MF5.js";
import {
  d
} from "./chunk-F6VYKY5P.js";
import {
  h
} from "./chunk-RZ3N43WD.js";
import {
  d as d2,
  e as e3,
  f,
  u as u2
} from "./chunk-QWT5D5LM.js";
import {
  e as e2
} from "./chunk-B6MIGAJF.js";
import {
  i as i2
} from "./chunk-WRAAMEHT.js";
import {
  r
} from "./chunk-7BJKTGZL.js";
import {
  s
} from "./chunk-BCEJLVA4.js";
import {
  n,
  t
} from "./chunk-NYA5CJQN.js";
import {
  i
} from "./chunk-F4DT2IMP.js";
import {
  Q
} from "./chunk-LY4SN26E.js";
import {
  __decorate
} from "./chunk-SS5CLIUY.js";

// node_modules/@arcgis/core/views/3d/webgl-engine/shaders/PatternTechniqueConfiguration.js
var s2 = class extends r2 {
  constructor() {
    super(...arguments), this.cullFace = 0, this.style = 0, this.emissionSource = 0, this.hasVertexColors = false, this.hasOccludees = false, this.hasVVColor = false, this.draped = false, this.textureCoordinateType = 0, this.discardInvisibleFragments = true, this.writeDepth = true, this.hasVVInstancing = false, this.hasVVSize = false, this.hasVVOpacity = false, this.overlayEnabled = false, this.snowCover = false;
  }
};
function i3(o2, t2, e5, s3) {
  return o2.draped ? null : o2.hasVVColor ? s3 : o2.hasVertexColors ? e5 : t2;
}
__decorate([i2({ count: 3 })], s2.prototype, "cullFace", void 0), __decorate([i2({ count: 6 })], s2.prototype, "style", void 0), __decorate([i2({ count: 8 })], s2.prototype, "emissionSource", void 0), __decorate([i2()], s2.prototype, "hasVertexColors", void 0), __decorate([i2()], s2.prototype, "hasOccludees", void 0), __decorate([i2()], s2.prototype, "hasVVColor", void 0), __decorate([i2()], s2.prototype, "draped", void 0);

// node_modules/@arcgis/core/views/3d/webgl-engine/shaders/PatternLayouts.js
function n2(r4) {
  const o2 = Q().vec3f("position").vec4f("uvMapSpace");
  return r4.draped ? r4.hasVVColor ? o2.f32("colorFeatureAttribute") : r4.hasVertexColors && o2.vec4u8("color", { glNormalized: true }) : o2.u32("textureElementIndex", { integer: true }), e() && o2.vec4u8("olidColor"), o2.freeze();
}
var u3 = [{ type: "mat3f32", name: "boundingRect" }];
var f3 = new i(u3);
var i4 = new i([...u3, { type: "vec4unorm8", name: "color" }]);
var a = new i([...u3, { type: "f32", name: "colorFeatureAttribute" }]);
function c(e5) {
  return i3(e5, f3, i4, a);
}

// node_modules/@arcgis/core/views/3d/webgl-engine/shaders/PatternTextureBuffer.glsl.js
var f4 = new e4("componentTextureBuffer", (e5) => e5.textureBuffer);
var i5 = new f2({ layout: f3, itemIndexAttribute: "textureElementIndex", bufferUniform: f4 });
var m = new f2({ layout: i4, itemIndexAttribute: "textureElementIndex", bufferUniform: f4 });
var a2 = new f2({ layout: a, itemIndexAttribute: "textureElementIndex", bufferUniform: f4, enableNaNSupport: true });
function x(e5) {
  return i3(e5, i5, m, a2);
}

// node_modules/@arcgis/core/chunks/Pattern.glsl.js
var f5 = 0.70710678118;
var g = f5;
var w = 0.08715574274;
var b = 10;
var h2 = 1;
function x2(x3) {
  const S2 = x(x3), j = null != S2, C = new s();
  j && C.include(S2.TextureBackedBufferModule, x3);
  const { vertex: V, fragment: T, attributes: P, varyings: $ } = C, R = 10 === x3.output;
  f(V, x3), C.include(o);
  let A = "";
  j ? (x3.hasVVColor && (A = S2.getTextureAttribute("colorFeatureAttribute")), x3.hasVertexColors ? (C.varyings.add("vColor", "vec4"), C.vertex.code.add(t`void forwardVertexColor() { vColor = ${S2.getTextureAttribute("color")}; }`)) : C.vertex.code.add(t`void forwardVertexColor() {}`), P.add("textureElementIndex", "uint")) : (C.include(r3, x3), x3.hasVVColor && (P.add("colorFeatureAttribute", "float"), A = "colorFeatureAttribute")), P.add("position", "vec3"), V.inputs.add("position", () => "position"), C.include(u, x3), C.include(d, x3), T.include(h, x3), C.include(u2, x3), x3.draped && V.uniforms.add(new r("worldToScreenRatio", (e5) => 1 / e5.screenToPCSRatio)), P.add("uvMapSpace", "vec4"), x3.hasVertexColors || $.add("vColor", "vec4"), $.add("vpos", "vec3", { invariant: true }), $.add("vuv", "vec2"), V.uniforms.add(new e2("uColor", (e5) => e5.color));
  const L = 3 === x3.style || 4 === x3.style || 5 === x3.style;
  return L && V.code.add(t`
      const mat2 rotate45 = mat2(${t.float(f5)}, ${t.float(-g)},
                                 ${t.float(g)}, ${t.float(f5)});
    `), !x3.draped && j && (d2(V, x3), V.uniforms.add(new r("worldToScreenPerDistanceRatio", (e5) => 1 / e5.camera.perScreenPixelRatio)), V.code.add(t`vec3 projectPointToLineSegment(vec3 center, vec3 halfVector, vec3 point) {
float projectedLength = dot(halfVector, point - center) / dot(halfVector, halfVector);
return center + halfVector * clamp(projectedLength, -1.0, 1.0);
}`), V.code.add(t`vec3 intersectRayPlane(vec3 rayDir, vec3 rayOrigin, vec3 planeNormal, vec3 planePoint) {
float d = dot(planeNormal, planePoint);
float t = (d - dot(planeNormal, rayOrigin)) / dot(planeNormal, rayDir);
return rayOrigin + t * rayDir;
}`), V.code.add(t`
      float boundingRectDistanceToCamera() {
        vec3 center = ${S2.getTextureAttribute("boundingRect")}[0];
        vec3 halfU = ${S2.getTextureAttribute("boundingRect")}[1];
        vec3 halfV = ${S2.getTextureAttribute("boundingRect")}[2];
        vec3 n = normalize(cross(halfU, halfV));

        vec3 viewDir = - vec3(view[0][2], view[1][2], view[2][2]);

        float viewAngle = dot(viewDir, n);
        float minViewAngle = ${t.float(w)};

        if (abs(viewAngle) < minViewAngle) {
          // view direction is (almost) parallel to plane -> clamp it to min angle
          float normalComponent = sign(viewAngle) * minViewAngle - viewAngle;
          viewDir = normalize(viewDir + normalComponent * n);
        }

        // intersect view direction with infinite plane that contains bounding rect
        vec3 planeProjected = intersectRayPlane(viewDir, cameraPosition, n, center);

        // clip to bounds by projecting to u and v line segments individually
        vec3 uProjected = projectPointToLineSegment(center, halfU, planeProjected);
        vec3 vProjected = projectPointToLineSegment(center, halfV, planeProjected);

        // use to calculate the closest point to camera on bounding rect
        vec3 closestPoint = uProjected + vProjected - center;

        return length(closestPoint - cameraPosition);
      }
    `)), V.code.add(t`
    vec2 scaledUV() {
      vec2 uv = uvMapSpace.xy ${n(L, " * rotate45")};
      vec2 uvCellOrigin = uvMapSpace.zw ${n(L, " * rotate45")};

      ${n(!x3.draped, t`float distanceToCamera = boundingRectDistanceToCamera();
               float worldToScreenRatio = worldToScreenPerDistanceRatio / distanceToCamera;`)}

      // Logarithmically discretize ratio to avoid jittering
      float step = 0.1;
      float discreteWorldToScreenRatio = log(worldToScreenRatio);
      discreteWorldToScreenRatio = ceil(discreteWorldToScreenRatio / step) * step;
      discreteWorldToScreenRatio = exp(discreteWorldToScreenRatio);

      vec2 uvOffset = mod(uvCellOrigin * discreteWorldToScreenRatio, ${t.float(b)});
      return uvOffset + (uv * discreteWorldToScreenRatio);
    }
  `), V.main.add(t`
    vuv = scaledUV();
    vpos = position;
    forwardVertexColor();
    forwardObjectAndLayerIdColor();
    ${x3.hasVertexColors ? "vColor *= uColor;" : x3.hasVVColor ? t`vColor = uColor * interpolateVVColor(${A});` : "vColor = uColor;"}
    gl_Position = transformPosition(proj, view, vpos);
  `), T.include(e3), x3.draped && T.uniforms.add(new r("texelSize", (e5) => 1 / e5.camera.pixelRatio)), R || (T.code.add(t`
      const float lineWidth = ${t.float(h2)};
      const float spacing = ${t.float(b)};
      const float spacingINV = ${t.float(1 / b)};

      float coverage(float p, float txlSize) {
        p = mod(p, spacing);

        float halfTxlSize = txlSize / 2.0;

        float start = p - halfTxlSize;
        float end = p + halfTxlSize;

        float coverage = (ceil(end * spacingINV) - floor(start * spacingINV)) * lineWidth;
        coverage -= min(lineWidth, mod(start, spacing));
        coverage -= max(lineWidth - mod(end, spacing), 0.0);

        return coverage / txlSize;
      }
    `), x3.draped || T.code.add(t`const int maxSamples = 5;
float sampleAA(float p) {
vec2 dxdy = abs(vec2(dFdx(p), dFdy(p)));
float fwidth = dxdy.x + dxdy.y;
ivec2 samples = 1 + ivec2(clamp(dxdy, 0.0, float(maxSamples - 1)));
vec2 invSamples = 1.0 / vec2(samples);
float accumulator = 0.0;
for (int j = 0; j < maxSamples; j++) {
if(j >= samples.y) {
break;
}
for (int i = 0; i < maxSamples; i++) {
if(i >= samples.x) {
break;
}
vec2 step = vec2(i,j) * invSamples - 0.5;
accumulator += coverage(p + step.x * dxdy.x + step.y * dxdy.y, fwidth);
}
}
accumulator /= float(samples.x * samples.y);
return accumulator;
}`)), T.main.add(t`
    discardBySlice(vpos);
    vec4 color = vColor;
    ${n(!R, t`color.a *= ${y(x3)};`)}
    outputColorHighlightOLID(applySlice(color, vpos), color.rgb);
  `), C;
}
function y(e5) {
  function o2(o3) {
    return e5.draped ? t`coverage(vuv.${o3}, texelSize)` : t`sampleAA(vuv.${o3})`;
  }
  switch (e5.style) {
    case 3:
    case 0:
      return o2("y");
    case 4:
    case 1:
      return o2("x");
    case 5:
    case 2:
      return t`1.0 - (1.0 - ${o2("x")}) * (1.0 - ${o2("y")})`;
    default:
      return "0.0";
  }
}
var S = Object.freeze(Object.defineProperty({ __proto__: null, build: x2 }, Symbol.toStringTag, { value: "Module" }));

export {
  s2 as s,
  n2 as n,
  c,
  x2 as x,
  S
};
//# sourceMappingURL=chunk-U3VPOLTI.js.map
