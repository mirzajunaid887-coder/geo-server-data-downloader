import {
  _
} from "./chunk-HTB4CXXP.js";
import {
  r as r3
} from "./chunk-RTHBDN6P.js";
import {
  f,
  h,
  l
} from "./chunk-QDSSZK56.js";
import {
  r as r2
} from "./chunk-WOVTCWAG.js";
import {
  o,
  s
} from "./chunk-42RXVBJ6.js";
import {
  n as n2
} from "./chunk-HGT5JKWU.js";
import {
  C,
  S,
  c3 as c,
  d,
  e,
  i2 as i,
  p
} from "./chunk-M3TUHHNI.js";
import {
  n2 as n,
  r3 as r
} from "./chunk-NXFNHCI4.js";
import {
  F,
  v
} from "./chunk-DHG2BGC2.js";

// node_modules/@arcgis/core/views/support/Yield.js
var o2 = Symbol("Yield");

// node_modules/@arcgis/core/views/support/PromiseQueue.js
var c2 = class {
  constructor() {
    this._tasks = new Array(), this._numPendingTasks = r3(0), this._readyToRun = r3(false);
  }
  get length() {
    return this._tasks.length;
  }
  get updating() {
    return this._numPendingTasks.value > 0;
  }
  get readyToRun() {
    return this._readyToRun.value;
  }
  _updateReadyToRun() {
    this._readyToRun.value = this._tasks.length > 0;
  }
  destroy() {
    this.cancelAll();
  }
  runTask(t) {
    if (0 === this.length) return o2;
    for (; !t.done && this._process(t); ) t.madeProgress();
  }
  push(t, e2, s2) {
    return this._addTask((r4, o3) => new p2(r4, o3, t, e2, s2), Array.prototype.push);
  }
  unshift(t, e2, s2) {
    return this._addTask((r4, o3) => new p2(r4, o3, t, e2, s2), Array.prototype.unshift);
  }
  pushGenerator(t, e2, s2) {
    return this._addTask((r4, o3) => new _2(r4, o3, t, e2, s2), Array.prototype.push);
  }
  _process(t) {
    if (0 === this._tasks.length) return false;
    const e2 = this._tasks.shift();
    this._updateReadyToRun();
    try {
      if (c(e2.signal)) this._cancelTask(e2, i());
      else switch (e2.type) {
        case 0:
          this._processSimple(e2, t);
          break;
        case 1:
          this._processGenerator(e2, t);
          break;
        case 2:
          this._processIterator(e2, t);
      }
    } catch (o3) {
      e2.reject(o3);
    }
    return true;
  }
  cancelAll() {
    const t = i();
    for (const e2 of this._tasks) this._cancelTask(e2, t);
    this._tasks.length = 0, this._updateReadyToRun();
  }
  _cancelTask(t, e2) {
    if (t.abortCallback) {
      const s2 = t.abortCallback(e2);
      2 === t.type && t.iterator.return && h2(t.iterator.return()), S(s2) ? s2.then(t.resolve, t.reject) : t.resolve(s2);
    } else 2 === t.type && t.iterator.throw && h2(t.iterator.throw(e2)), t.reject(e2);
  }
  _onIteratorResult(t, e2) {
    e2.done ? t.resolve(e2.value) : (this._tasks.unshift(t), this._updateReadyToRun());
  }
  _processSimple(t, e2) {
    const s2 = t.callback(e2);
    S(s2) ? s2.then(t.resolve, t.reject) : t.resolve(s2);
  }
  _processGenerator(t, e2) {
    const s2 = t.generatorFunction(e2), r4 = new d2(t.resolve, t.reject, s2, t.signal, t.abortCallback);
    this._processIterator(r4, e2);
  }
  _processIterator(t, e2) {
    const s2 = t.iterator.next(e2);
    S(s2) ? s2.then((e3) => this._onIteratorResult(t, e3), t.reject) : this._onIteratorResult(t, s2);
  }
  _addTask(t, e2) {
    return new Promise((s2, r4) => {
      const o3 = t(s2, r4);
      e2.call(this._tasks, o3), ++this._numPendingTasks.value, this._updateReadyToRun();
    }).finally(() => --this._numPendingTasks.value);
  }
};
function h2(t) {
  S(t) ? t.then(l2, l2) : l2(t);
}
function l2(s2) {
  p(s2) || s2 instanceof Error || s2 instanceof r || null != s2 && "object" == typeof s2 && "done" in s2 && s2.done || n.getLogger("esri.layers.support.PromiseQueue").warn("Generator iterator was aborted, but it is not done.");
}
var u = class {
  constructor(t, e2, s2, r4) {
    this.resolve = t, this.reject = e2, this.signal = s2, this.abortCallback = r4;
  }
};
var p2 = class extends u {
  constructor(t, e2, s2, r4, o3) {
    super(t, e2, r4, o3), this.callback = s2, this.type = 0;
  }
};
var _2 = class extends u {
  constructor(t, e2, s2, r4, o3) {
    super(t, e2, r4, o3), this.generatorFunction = s2, this.type = 1;
  }
};
var d2 = class extends u {
  constructor(t, e2, s2, r4, o3) {
    super(t, e2, r4, o3), this.iterator = s2, this.type = 2;
  }
};

// node_modules/@arcgis/core/views/support/Scheduler.js
function A() {
  return new C2();
}
var p3 = { RESOURCE_CONTROLLER_IMMEDIATE: "immediate", RESOURCE_CONTROLLER: "schedule", SLIDE: "slide", STREAM_DATA_LOADER: "stream loader", ELEVATION_QUERY: "elevation query", TERRAIN_SURFACE: "terrain", SURFACE_GEOMETRY_UPDATES: "surface geometry updates", LOD_RENDERER: "LoD renderer", GRAPHICS_CORE: "Graphics3D", I3S: "I3S", LYR3D: "Lyr3D", TILES3D: "3DTiles", POINT_CLOUD_LAYER: "point cloud", FEATURE_TILE_FETCHER: "feature fetcher", STREAM_CONTROLLER: "stream controller", OCCLUSION_QUERY: "occlusion query", OVERLAY: "overlay", OVERLAY_RENDERER: "overlay renderer", STAGE: "stage", GRAPHICS_DECONFLICTOR: "graphics deconflictor", FILTER_VISIBILITY: "graphics filter visibility", SCALE_VISIBILITY: "graphics scale visibility", FRUSTUM_VISIBILITY: "graphics frustum visibility", POINT_OF_INTEREST_FREQUENT: "POI frequent", POINT_OF_INTEREST_INFREQUENT: "POI infrequent", LABELER: "labeler", FEATURE_QUERY_ENGINE: "feature query", FEATURE_TILE_TREE: "feature tile tree", FEATURE_TILE_TREE_ACTIVE: "fast feature tile tree", ELEVATION_ALIGNMENT: "elevation alignment", ELEVATION_ALIGNMENT_SCENE: "elevation alignment scene", TEXT_TEXTURE_ATLAS: "text texture atlas", TEXTURE_UNLOAD: "texture unload", LINE_OF_SIGHT_TOOL: "line of sight tool", LINE_OF_SIGHT_TOOL_INTERACTIVE: "interactive line of sight tool", VOLUME_MEASUREMENT: "volume measurement", ELEVATION_PROFILE: "elevation profile", SNAPPING: "snapping", SHADOW_ACCUMULATOR: "shadow accumulator", CLOUDS_GENERATOR: "clouds generator", FLOW_GENERATOR: "flow generator", GAUSSIAN_SPLAT_SORTING: "gaussian splat sorting", GAUSSIAN_SPLAT_TEXTURE_ATLAS: "gaussian splat texture atlas", MAPVIEW_FETCH_QUEUE: "mapview fetch queue", MAPVIEW_LAYERVIEW_UPDATE: "mapview layerview update", MAPVIEW_VECTOR_TILE_PARSING_QUEUE: "mapview vector tile parsing queue", NONE: 0, TEST_PRIO: 1 };
var L = 0;
var f2 = /* @__PURE__ */ new Map([[p3.RESOURCE_CONTROLLER_IMMEDIATE, L], [p3.RESOURCE_CONTROLLER, 4], [p3.SLIDE, L], [p3.STREAM_DATA_LOADER, L], [p3.ELEVATION_QUERY, L], [p3.TERRAIN_SURFACE, 1], [p3.SURFACE_GEOMETRY_UPDATES, 1], [p3.LOD_RENDERER, 2], [p3.GRAPHICS_CORE, 2], [p3.I3S, 2], [p3.LYR3D, 2], [p3.TILES3D, 3], [p3.POINT_CLOUD_LAYER, 2], [p3.FEATURE_TILE_FETCHER, 2], [p3.STREAM_CONTROLLER, 2], [p3.CLOUDS_GENERATOR, 2], [p3.OCCLUSION_QUERY, 2], [p3.OVERLAY, 4], [p3.OVERLAY_RENDERER, 4], [p3.STAGE, 4], [p3.GRAPHICS_DECONFLICTOR, 4], [p3.FILTER_VISIBILITY, 4], [p3.SCALE_VISIBILITY, 4], [p3.FRUSTUM_VISIBILITY, 4], [p3.POINT_OF_INTEREST_FREQUENT, 6], [p3.POINT_OF_INTEREST_INFREQUENT, 30], [p3.LABELER, 8], [p3.FEATURE_QUERY_ENGINE, 8], [p3.FEATURE_TILE_TREE, 16], [p3.FEATURE_TILE_TREE_ACTIVE, L], [p3.ELEVATION_ALIGNMENT, 12], [p3.ELEVATION_ALIGNMENT_SCENE, 14], [p3.TEXT_TEXTURE_ATLAS, 12], [p3.TEXTURE_UNLOAD, 12], [p3.LINE_OF_SIGHT_TOOL, 16], [p3.LINE_OF_SIGHT_TOOL_INTERACTIVE, L], [p3.VOLUME_MEASUREMENT, 4], [p3.SNAPPING, L], [p3.SHADOW_ACCUMULATOR, 30], [p3.FLOW_GENERATOR, 12], [p3.GAUSSIAN_SPLAT_SORTING, 2], [p3.GAUSSIAN_SPLAT_TEXTURE_ATLAS, 12], [p3.MAPVIEW_FETCH_QUEUE, L], [p3.MAPVIEW_LAYERVIEW_UPDATE, 2], [p3.MAPVIEW_VECTOR_TILE_PARSING_QUEUE, L]]);
function O(e2) {
  return f2.has(e2) ? f2.get(e2) : "number" == typeof e2 ? e2 : 1;
}
var S2 = n2(6.5);
var N = n2(1);
var U = n2(30);
var b = n2(1e3 / 30);
var P = n2(100);
var k = 0.9;
var C2 = class {
  get updating() {
    return this._updating.value;
  }
  _updatingChanged() {
    this._updating.value = this._tasks.some((e2) => e2.needsUpdate);
  }
  constructor() {
    this._updating = r3(true), this._microTaskQueued = false, this._frameNumber = 0, this.performanceInfo = { total: new s("total"), tasks: /* @__PURE__ */ new Map() }, this._frameTaskTimes = /* @__PURE__ */ new Map(), this._budget = new F2(), this.state = 1, this._tasks = new Array(), this._runQueue = new Array(), this._load = 0, this._forceTask = false, this._debug = false, this._debugHandle = l(() => _.SCHEDULER_LOG_SLOW_TASKS, (e2) => this._debug = e2, h);
    for (const e2 of Object.keys(p3)) this.performanceInfo.tasks.set(p3[e2], new s(String(p3[e2])));
  }
  destroy() {
    this._tasks.forEach((e2) => e2.remove()), this._tasks.length = 0, this._runQueue.length = 0, e(this._debugHandle), this._microTaskQueued = false, this._updatingChanged();
  }
  taskRunningChanged(e2) {
    this._updatingChanged(), e2 && this._budget.remaining > 0 && !this._microTaskQueued && (this._microTaskQueued = true, queueMicrotask(() => {
      this._microTaskQueued && (this._microTaskQueued = false, this._budget.remaining > 0 && this._schedule() && this._runFrame());
    }));
  }
  registerTask(e2, t) {
    const s2 = new y(this, e2, t);
    return this._tasks.push(s2), this._updatingChanged(), this.performanceInfo.tasks.has(e2) || this.performanceInfo.tasks.set(e2, new s(e2)), s2;
  }
  get load() {
    return this._load;
  }
  frame(e2) {
    if (this._startFrameTaskTimes(), this._updateBudget(e2)) {
      const e3 = this._budget.now();
      this._runFrame();
      const t = this._budget.now();
      this._recordFrameTaskTimes(t - e3);
      const s2 = 2 === this.state ? "Idle" : 1 === this.state ? "Interacting" : "Animating";
      return o(`${s2} Frame Tasks`, e3, t, "Maps SDK", "Scheduling"), true;
    }
    return this._recordFrameTaskTimes(0), false;
  }
  _updateBudget(e2) {
    this._test && (this._test.usedBudget = 0), ++this._frameNumber;
    let t = S2, s2 = e2.frameDuration, r4 = N;
    switch (this.state) {
      case 2:
        t = n2(0), s2 = n2(Math.max(P, e2.frameDuration)), r4 = U;
        break;
      case 1:
        s2 = n2(Math.max(b, e2.frameDuration));
    }
    return s2 = n2(s2 - e2.elapsedFrameTime - t), 2 !== this.state && s2 < N && !this._forceTask ? (this._forceTask = true, false) : (s2 = n2(Math.max(s2, r4)), this._budget.reset(s2), this._updateLoad(), this._schedule());
  }
  _runFrame() {
    this._forceTask = false, this._microTaskQueued = false, this._run(), this._test && (this._test.usedBudget = this._budget.elapsed);
  }
  stopFrame() {
    this._budget.reset(n2(0)), this._budget.madeProgress();
  }
  removeTask(t) {
    v(this._tasks, t), v(this._runQueue, t), this._updatingChanged();
  }
  _updateTask(e2) {
    this._tasks.forEach((t) => {
      t.name === e2 && t.setPriority(e2);
    });
  }
  _getState(e2) {
    if (this._runQueue.some((t2) => t2.name === e2)) return "s";
    let t = "i";
    return this._tasks.forEach((s2) => {
      s2.name === e2 && s2.needsUpdate && (s2.schedulePriority <= 1 ? t = "r" : "r" !== t && (t = "w"));
    }), t;
  }
  _getRuntime(e2) {
    let t = 0;
    return this._tasks.forEach((s2) => {
      s2.name === e2 && (t += s2.runtime);
    }), t;
  }
  _resetRuntimes() {
    this._tasks.forEach((e2) => e2.runtime = 0);
  }
  _getRunning() {
    const e2 = /* @__PURE__ */ new Map();
    if (this._tasks.forEach((t2) => {
      t2.needsUpdate && e2.set(t2.name, (e2.get(t2.name) || 0) + 1);
    }), 0 === e2.size) return null;
    let t = "";
    return e2.forEach((e3, s2) => {
      t += e3 > 1 ? ` ${e3}x ${s2}` : ` ${s2}`;
    }), t;
  }
  _updateLoad() {
    const e2 = this._tasks.reduce((e3, t) => t.needsUpdate ? ++e3 : e3, 0);
    this._load = this._load * k + e2 * (1 - k);
  }
  _schedule() {
    for (F(this._runQueue, (e2) => !!e2.needsUpdate || (e2.schedulePriority = e2.basePriority, false)), this._tasks.forEach((e2) => {
      e2.basePriority === L && e2.needsUpdate && !this._runQueue.includes(e2) && e2.blockFrame !== this._frameNumber && this._runQueue.unshift(e2);
    }); 0 === this._runQueue.length; ) {
      let e2 = false, t = 0;
      if (this._tasks.forEach((s2) => {
        if (s2.needsUpdate && 0 !== s2.schedulePriority && s2.basePriority !== L && s2.blockFrame !== this._frameNumber) if (e2 = true, t = Math.max(t, s2.basePriority), 1 === s2.schedulePriority) s2.schedulePriority = 0, this._runQueue.push(s2);
        else --s2.schedulePriority;
      }), !e2) return this._updatingChanged(), false;
    }
    return this._updatingChanged(), true;
  }
  _run() {
    do {
      for (; this._runQueue.length > 0; ) {
        const t = this._budget.now(), s2 = this._runQueue.pop();
        this._budget.resetProgress();
        try {
          s2.task.runTask(this._budget) === o2 && (s2.blockFrame = this._frameNumber);
        } catch (e2) {
          n.getLogger("esri.views.support.Scheduler").error(`Exception in task "${s2.name}"`, e2), s2.blockFrame = this._frameNumber;
        }
        !this._budget.hasProgressed && s2.blockFrame !== this._frameNumber && s2.needsUpdate && (s2.name, p3.I3S, s2.blockFrame = this._frameNumber), s2.schedulePriority = s2.basePriority;
        const i2 = this._budget.now(), n3 = i2 - t;
        if (o(`${s2.name}`, t, i2, "Maps SDK", "Scheduling"), s2.runtime += n3, this._frameTaskTimes.set(s2.priority, this._frameTaskTimes.get(s2.priority) + n3), this._budget.remaining <= 0) return void this._updatingChanged();
      }
    } while (this._schedule());
    this._updatingChanged();
  }
  _startFrameTaskTimes() {
    for (const e2 of Object.keys(p3)) this._frameTaskTimes.set(p3[e2], 0);
  }
  _recordFrameTaskTimes(e2) {
    this._frameTaskTimes.forEach((e3, t) => this.performanceInfo.tasks.get(t).push(e3)), this.performanceInfo.total.push(e2);
  }
  get test() {
    return this._test;
  }
};
var y = class {
  get task() {
    return this._task.value;
  }
  get readyToRun() {
    return this._queue.readyToRun;
  }
  get updating() {
    return this._queue.updating;
  }
  constructor(e2, t, r4) {
    this._scheduler = e2, this.name = t, this.blockFrame = 0, this.runtime = 0, this._queue = new c2(), this._handles = new r2(), this._basePriority = O(t), this.schedulePriority = this._basePriority, this._task = r3(null != r4 ? r4 : this._queue), this._handles.add(f(() => this.task.readyToRun, (t2) => e2.taskRunningChanged(t2)));
  }
  remove() {
    this.processQueue(w), this._scheduler.removeTask(this), this.schedule = G.schedule, this.reschedule = G.reschedule, this.scheduleGenerator = G.scheduleGenerator, this._handles.destroy();
  }
  get basePriority() {
    return this._basePriority;
  }
  setPriority(e2) {
    if (this.name === e2) return;
    this.name = e2;
    const t = O(e2);
    this._basePriority !== L && 0 === this.schedulePriority || (this.schedulePriority = t), this._basePriority = t;
  }
  get priority() {
    return this.name;
  }
  set priority(e2) {
    this.setPriority(e2);
  }
  get needsUpdate() {
    return this.readyToRun || !this.task.destroyed && this.task.readyToRun;
  }
  schedule(e2, t, s2) {
    return this._queue.push(e2, t, s2);
  }
  reschedule(e2, t, s2) {
    return this._queue.unshift(e2, t, s2);
  }
  scheduleGenerator(e2, t, s2) {
    return this._queue.pushGenerator(e2, t, s2);
  }
  processQueue(e2) {
    return this._queue.runTask(e2);
  }
};
var F2 = class {
  constructor() {
    this._begin = (performance == null ? void 0 : performance.now()) ?? 0, this._budget = 0, this._done = false, this._progressed = false, this._enabled = true;
  }
  run(e2) {
    return !this.done && (true === e2() && this.madeProgress(), true);
  }
  get done() {
    return this._done;
  }
  get budget() {
    return this._budget;
  }
  madeProgress() {
    return this._progressed = true, this._done = this.elapsed >= this._budget && this._enabled, this._done;
  }
  get enabled() {
    return this._enabled;
  }
  set enabled(e2) {
    this._enabled = e2;
  }
  reset(e2) {
    this._begin = this.now(), this._budget = e2, this.resetProgress();
  }
  get remaining() {
    return Math.max(this._budget - this.elapsed, 0);
  }
  now() {
    return performance.now();
  }
  get elapsed() {
    return this.now() - this._begin;
  }
  resetProgress() {
    this._progressed = false, this._done = false;
  }
  get hasProgressed() {
    return this._progressed;
  }
};
var w = new F2();
w.enabled = false;
var M = class {
  remove() {
  }
  processQueue() {
  }
  schedule(e2, t, s2) {
    try {
      if (c(t)) {
        const e3 = i();
        return s2 ? Promise.resolve(s2(e3)) : Promise.reject(e3);
      }
      return C(e2(w));
    } catch (r4) {
      return Promise.reject(r4);
    }
  }
  reschedule(e2, t, s2) {
    return this.schedule(e2, t, s2);
  }
  async scheduleGenerator(e2, t, s2) {
    if (c(t)) {
      const e3 = i();
      if (s2) return s2(e3);
      throw e3;
    }
    const r4 = e2(w);
    for (; ; ) {
      const e3 = r4.next(w), i2 = S(e3) ? await e3 : e3;
      if (c(t)) {
        const e4 = i();
        if (s2) {
          const t3 = s2(e4), i3 = r4.return(null);
          return S(i3) && await d(i3), t3;
        }
        const t2 = r4.throw(e4);
        throw S(t2) && await d(t2), e4;
      }
      if (i2.done) return i2.value;
    }
  }
};
var G = new M();

export {
  o2 as o,
  c2 as c,
  A,
  p3 as p,
  w,
  G
};
//# sourceMappingURL=chunk-HHU5U6DA.js.map
