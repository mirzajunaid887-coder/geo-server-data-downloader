import {
  i,
  r as r3,
  s
} from "./chunk-KQONKZEG.js";
import {
  x
} from "./chunk-J3W37ILL.js";
import {
  c
} from "./chunk-T6SW4LHC.js";
import {
  t
} from "./chunk-TSYPNJDK.js";
import {
  t as t2
} from "./chunk-OL6GC7Q5.js";
import {
  o as o2
} from "./chunk-Y7KJ4KU2.js";
import {
  f
} from "./chunk-BWDTBVI2.js";
import {
  p2 as p
} from "./chunk-W57OB6I4.js";
import {
  e as e2
} from "./chunk-2DLRKVUE.js";
import {
  X,
  Y,
  dt,
  nt
} from "./chunk-AUSULX52.js";
import {
  r as r2
} from "./chunk-K3SO5J4Z.js";
import {
  e
} from "./chunk-JSNQ74AP.js";
import {
  u as u3
} from "./chunk-M2ZZO4JZ.js";
import {
  n as n2
} from "./chunk-Z5NAMREB.js";
import {
  u,
  u2
} from "./chunk-CEJOXQ2Y.js";
import {
  has,
  n2 as n,
  o3 as o,
  r3 as r
} from "./chunk-Z3IZZR23.js";

// node_modules/@arcgis/core/views/2d/engine/webgl/shaderGraph/techniques/FeatureInstanceStore.js
var s2 = class {
  constructor() {
    this._instanceById = /* @__PURE__ */ new Map();
  }
  destroy() {
    this._instanceById.clear();
  }
  get size() {
    return this._instanceById.size;
  }
  entries() {
    return this._instanceById.entries();
  }
  find(t3) {
    for (const e4 of this.values()) if (e4.techniqueRef.type === t3) return e4;
    return null;
  }
  updateStart(t3) {
    t3 && (this._instanceByIdNext = /* @__PURE__ */ new Map(), this._shaderCountByMesh = /* @__PURE__ */ new Map(), this._shaderIndices = /* @__PURE__ */ new Map(), this._instanceIdToOrder = null);
  }
  updateEnd(t3) {
    if (t3) {
      if (!this._instanceByIdNext) throw new Error("InternalError: Found updateEnd call without corresponding updateStart");
      for (const t4 of this._instanceById.keys()) this._instanceByIdNext.has(t4) || this._instanceById.delete(t4);
      for (const [t4, e4] of this._instanceByIdNext.entries()) {
        const n3 = this._instanceById.get(t4);
        n3 ? n3.setInput(e4.getInput()) : this._instanceById.set(t4, e4);
      }
      this._instanceByIdNext = null, this._shaderCountByMesh = null, this._shaderIndices = null;
    }
  }
  values() {
    return this._instanceById.values();
  }
  ensureInstance(s4, i2, r4) {
    let a3;
    if ("object" == typeof i2 && "optionalAttributes" in i2 && "uniforms" in i2 && null == r4) {
      const t3 = `${s4.type}.${JSON.stringify(i2.optionalAttributes)}`, e4 = t3 + `.${JSON.stringify(i2.uniforms)}`;
      let n3 = 0;
      null != this._instanceByIdNext && (this._shaderIndices.has(e4) ? n3 = this._shaderIndices.get(e4) : (n3 = this._shaderCountByMesh.get(t3) ?? 0, this._shaderCountByMesh.set(t3, n3 + 1), this._shaderIndices.set(e4, n3))), a3 = t3 + `.${n3}`;
    } else {
      const t3 = r4 ? `${r4.name}.${r4.index}` : "";
      a3 = `${s4.type}.${JSON.stringify(i2)}.${t3}`;
    }
    const d3 = o(a3);
    if (null != r4 && this._ensureInstanceIdToOrder(d3, r4), this._instanceByIdNext) {
      const t3 = new i(r3(d3), s4, i2);
      return this._instanceByIdNext.set(d3, t3), t3;
    }
    if (!this._instanceById.has(d3)) {
      const t3 = new i(r3(d3), s4, i2);
      this._instanceById.set(d3, t3);
    }
    return this._instanceById.get(d3);
  }
  getInstance(t3) {
    const e4 = this._instanceById.get(t3);
    if (null == e4) throw new Error(`InternalError: Unable to get instance for ${t3}`);
    return e4;
  }
  getInstanceOrder() {
    return this._instanceIdToOrder;
  }
  _ensureInstanceIdToOrder(t3, e4) {
    null == this._instanceIdToOrder && (this._instanceIdToOrder = /* @__PURE__ */ new Map()), this._instanceIdToOrder.set(t3, e4.index);
  }
};

// node_modules/@arcgis/core/views/2d/layers/features/FeatureTilePresentStrategy.js
var e3 = class {
  constructor() {
    this._mode = "eager";
  }
  updateCoverage(e4) {
    this._coverage = e4;
  }
  updateStrategy(e4) {
    this._mode = e4;
  }
  update(e4) {
    if (!this._coverage) return;
    const t3 = /* @__PURE__ */ new Map();
    for (const d3 of e4) t3.set(d3.id, d3);
    switch (this._mode) {
      case "eager":
        return this._updateEager(this._coverage, t3);
      case "target-defer":
        return this._updateTargetDefer(this._coverage, t3);
    }
  }
  setPresentMode(e4) {
    this._mode = e4;
  }
  _updateEager(e4, t3) {
    const d3 = /* @__PURE__ */ new Set(), n3 = /* @__PURE__ */ new Set();
    for (const r4 of e4.values()) {
      const e5 = r4.id, i2 = t3.get(e5);
      if (i2 == null ? void 0 : i2.presenting) {
        d3.add(e5), n3.add(e5);
        continue;
      }
      let s4 = true;
      if (i2 == null ? void 0 : i2.ended) {
        if (has("esri-2d-update-debug") && console.debug(`Tile[${e5}] PresentStrategy.update: Promoting ended tile.`), i2.decluttering) {
          d3.add(e5), n3.add(e5);
          continue;
        }
        n3.add(e5), s4 = false;
      }
      if (this._addPresentingParent(d3, n3, r4, t3, s4)) {
        has("esri-2d-update-debug") && console.debug(`Tile[${e5}] PresentStrategy.update: Found parent placeholder.`);
        continue;
      }
      this._addPresentingChildren(d3, n3, r4, t3, s4) ? has("esri-2d-update-debug") && console.debug(`Tile[${e5}] PresentStrategy.update: Found child placeholder.`) : i2 && (has("esri-2d-update-debug") && console.debug(`Tile[${e5}] PresentStrategy.update: Promoting tile. No placeholder found.`), d3.add(e5), n3.add(e5));
    }
    this._updateTiles(t3, n3, d3);
  }
  _updateTargetDefer(e4, t3) {
    const d3 = Array.from(e4.values()), n3 = /* @__PURE__ */ new Set(), r4 = /* @__PURE__ */ new Set();
    if (d3.every((e5) => {
      var _a;
      return (_a = t3.get(e5.id)) == null ? void 0 : _a.presenting;
    })) {
      for (const e5 of d3) n3.add(e5.id), r4.add(e5.id);
      return this._updateTiles(t3, r4, n3);
    }
    if (d3.every((e5) => {
      var _a;
      return (_a = t3.get(e5.id)) == null ? void 0 : _a.decluttering;
    })) for (const i2 of d3) r4.add(i2.id), n3.add(i2.id);
    if (d3.every((e5) => {
      var _a;
      return (_a = t3.get(e5.id)) == null ? void 0 : _a.ended;
    })) for (const i2 of d3) r4.add(i2.id);
    for (const i2 of d3) {
      const e5 = i2.id, d4 = t3.get(e5);
      if (this._addPresentingParent(n3, r4, i2, t3, true)) continue;
      this._addPresentingChildren(n3, r4, i2, t3, true) || d4 && (n3.add(e5), r4.add(e5));
    }
    this._updateTiles(t3, r4, n3);
  }
  _updateTiles(e4, t3, d3) {
    for (const n3 of e4.values()) {
      const e5 = n3.decluttering, r4 = n3.presenting;
      n3.decluttering = t3.has(n3.id), e5 !== n3.decluttering && n3.requestDeclutter(), n3.presenting = d3.has(n3.id), has("esri-2d-update-debug") && (e5 === n3.decluttering && r4 === n3.presenting || console.debug(`Tile[${n3.key.id}] PresentStrategy.update [decluttering: ${e5}->${n3.decluttering}, presenting: ${r4}->${n3.presenting}`));
    }
  }
  _addPresentingParent(e4, t3, d3, n3, r4) {
    let i2 = false;
    for (const s4 of n3.values()) if (s4.presenting && s4.key.containsChild(d3)) {
      e4.add(s4.key.id), r4 && t3.add(s4.key.id), i2 = true;
      break;
    }
    return i2;
  }
  _addPresentingChildren(e4, t3, d3, n3, r4) {
    let i2 = false;
    for (const s4 of n3.values()) s4.presenting && d3.containsChild(s4.key) && (e4.add(s4.key.id), r4 && t3.add(s4.key.id), i2 = true);
    return i2;
  }
};

// node_modules/@arcgis/core/views/2d/layers/features/RenderState.js
var a = 1e3;
var d = class {
  constructor(t3, i2, r4, n3) {
    this.getStage = t3, this.getSubscriptionVersion = i2, this.version = r4, this._tileInfoView = n3, this._pendingUpdates = new t(a), this._locked = false, this._tiles = /* @__PURE__ */ new Map(), this._updating = r2(false);
  }
  destroy() {
    var _a;
    for (const e4 of this.tiles()) (_a = this._fader) == null ? void 0 : _a.unregisterFeatureTile(e4), e4.destroy();
    this._pendingUpdates.clear(), this._tiles.clear();
  }
  get _fader() {
    var _a;
    return (_a = this.getStage()) == null ? void 0 : _a.labelManager.symbolFader;
  }
  tiles() {
    return Array.from(this._tiles.values());
  }
  size() {
    return this._tiles.size;
  }
  get usedMemory() {
    let e4 = 0;
    for (const t3 of this._tiles.values()) e4 += t3.usedMemory;
    for (const t3 of this._pendingUpdates.entries) t3 && (e4 += l(t3) ? t3.messages.reduce((e5, t4) => e5 + u4(t4.inner), 0) : u4(t3.inner));
    return e4;
  }
  get requiresRefresh() {
    for (const e4 of this._tiles.values()) if (e4.requiresRefresh) return true;
    return false;
  }
  get updating() {
    return this._updating.value;
  }
  getTile(e4) {
    return this._tiles.get(e4);
  }
  setTiles(e4) {
    this._tiles.clear();
    for (const t3 of e4) this._tiles.set(t3.key.id, t3);
  }
  lockUploads() {
    this._locked = true;
  }
  unlockUploads() {
    this._locked = false;
  }
  enqueueUpdate(e4) {
    this._pendingUpdates.enqueue(e4), this._notify();
  }
  enqueueLevelReplace(e4, t3) {
    this._pendingUpdates.enqueue({ type: "level-replace", level: e4, messages: t3 }), this._notify();
  }
  update(e4) {
    if (!this._locked) for (; this._pendingUpdates.size; ) {
      const t3 = this._pendingUpdates.peek();
      if (null == t3 || p2(t3) > e4) break;
      this._applyUpdate(t3), this._pendingUpdates.dequeue();
    }
    this._notify();
  }
  flush(e4) {
    for (; this._pendingUpdates.size; ) {
      const t3 = this._pendingUpdates.dequeue();
      if (null != t3) {
        const i2 = p2(t3);
        i2 > e4 && console.warn("Tried to flush RenderState, but patch more recent than current", { patchEpoch: i2, currentEpoch: e4 }), this._applyUpdate(t3);
      }
    }
    this._notify();
  }
  removeTile(e4) {
    var _a;
    const t3 = this._tiles.get(e4);
    has("esri-2d-update-debug") && console.debug(`Tile[${e4}] RenderState.removeTile`), t3 && ((_a = this._fader) == null ? void 0 : _a.unregisterFeatureTile(t3), t3.destroy()), this._tiles.delete(e4);
  }
  _clearLevelCpuState(e4) {
    for (const t3 of this._tiles.values()) t3.key.level === e4 && t3.clearCpuState();
  }
  _notify() {
    this._updating.mutate((e4) => 0 !== this._pendingUpdates.size);
  }
  _applyUpdate(e4) {
    if (l(e4)) {
      this._clearLevelCpuState(e4.level);
      for (const t3 of e4.messages) this._updateTile(t3);
    } else this._updateTile(e4);
  }
  _updateTile(e4) {
    var _a;
    const { inner: t3, objectIdMap: i2 } = e4, s4 = this.getSubscriptionVersion(t3.id);
    if (s4 !== t3.subscriptionVesrion) {
      if (has("esri-2d-update-debug")) {
        const e5 = `${t3.subscriptionVesrion} != ${s4}`;
        console.debug(`Version[${e5}] Tile[${t3.id}] FeatureContainer - Dropping message, outdated version]`, t3);
      }
      return;
    }
    if (has("esri-2d-update-debug")) {
      const e5 = ((_a = t3.debugInfo) == null ? void 0 : _a.chunkId) ?? "<EnsureEnd>";
      console.debug(`Version[${t3.version}] Tile[${t3.id}] Chunk[${e5}] RenderState.updateTile [${t3.type}]`, t3);
    }
    const r4 = this._ensureTile(t3.id);
    if ("update" === t3.type) {
      const [e5, ...s5] = t3.modify;
      r4.update({ type: "update", modify: e5, remove: t3.remove, end: t3.end, attributeEpoch: t3.attributeEpoch, objectIdMap: i2 });
      for (const r5 of s5) {
        const e6 = this._tiles.get(r5.tileId);
        e6 && e6.update({ type: "update", modify: r5, remove: t3.remove, end: false, isPixelBuffer: true, attributeEpoch: null, objectIdMap: i2 });
      }
      return;
    }
    if (null == t3.append) return void r4.update({ type: "append", debugInfo: t3.debugInfo, end: t3.end, attributeEpoch: t3.attributeEpoch, objectIdMap: i2 });
    const [n3, ...o3] = t3.append;
    r4.update({ type: "append", append: n3, debugInfo: t3.debugInfo, end: t3.end, attributeEpoch: t3.attributeEpoch, objectIdMap: i2 });
    for (const a3 of o3) {
      const e5 = this._tiles.get(a3.tileId);
      e5 && e5.update({ type: "update", modify: a3, remove: [], sort: false, end: false, isPixelBuffer: true, attributeEpoch: null, objectIdMap: i2 });
    }
  }
  _ensureTile(e4) {
    if (!this._tiles.has(e4)) {
      const t3 = this._createTile(e4);
      this._copyPixelBufferedEntitiesInto(t3), this._tiles.set(e4, t3);
    }
    return this._tiles.get(e4);
  }
  _createTile(e4) {
    var _a;
    has("esri-2d-update-debug") && console.debug(`Version[${this.version}] Tile[${e4}] RenderState.createTile`);
    const s4 = new e2(e4), a3 = this._tileInfoView.getTileBounds(u3(), s4), d3 = a3[0], u6 = a3[3], l3 = this._tileInfoView.getTileResolution(s4.level), p5 = new c(s4, l3, d3, u6, this._fader);
    if ((_a = this._fader) == null ? void 0 : _a.registerFeatureTile(p5), p5.stage = this.getStage(), !p5.stage) {
      const e5 = new r("featurelayerview:webgl", "Cannot create tile with empty stage");
      n.getLogger("esri.views.2d.layers.features.RenderState").error(e5);
    }
    return p5;
  }
  _copyPixelBufferedEntitiesInto(e4) {
    let t3 = 7;
    const i2 = this._tileInfoView.getLODInfoAt(e4.key);
    for (let s4 = -1; s4 <= 1; s4++) for (let r4 = -1; r4 <= 1; r4++) {
      if (0 === s4 && 0 === r4) continue;
      const n3 = e4.key.getNormalizedNeighbor(r4, s4, i2).id, o3 = this._tiles.get(n3);
      if (null != o3) {
        const i3 = 1 << t3;
        e4.copyPixelBufferedEntitesFrom(o3, i3, r4, s4);
      }
      t3--;
    }
  }
};
function u4(e4) {
  switch (e4.type) {
    case "update":
      return e4.modify.reduce((e5, t3) => e5 + c2(t3), 0);
    case "append":
      return e4.append ? e4.append.reduce((e5, t3) => e5 + c2(t3), 0) : 0;
  }
}
function l(e4) {
  return "type" in e4 && "level-replace" === e4.type;
}
function p2(e4) {
  if (!l(e4)) return e4.inner.attributeEpoch;
  let t3 = -1 / 0;
  for (const i2 of e4.messages) t3 = Math.max(t3, i2.inner.attributeEpoch);
  return t3;
}
function c2(e4) {
  return e4.entities.byteLength + e4.data.reduce((e5, t3) => e5 + f2(t3), 0);
}
function f2(e4) {
  var _a;
  return e4.indices.byteLength + e4.vertices.byteLength + (((_a = e4.metrics) == null ? void 0 : _a.byteLength) ?? 0);
}

// node_modules/@arcgis/core/views/2d/layers/features/FeatureContainer.js
var c3 = class {
  constructor(e4) {
    this.inner = e4, this.resolver = Promise.withResolvers();
  }
};
var p3 = class {
  constructor(e4) {
    this.level = e4, this.id = performance.now(), this.messages = [];
  }
};
var f3 = class {
  constructor(e4, t3) {
    this.id = e4, this.version = t3;
  }
};
var _ = class extends s {
  constructor(e4) {
    super(e4.view.featuresTilingScheme), this.referenceScale = null, this._updatingChildren = r2(false), this._hitTestsRequests = [], this._renderState = r2(null), this._renderStateNext = r2(null), this._store = new s2(), this._subscriptions = /* @__PURE__ */ new Map(), this._presentation = new e3(), this._updateStatisticsRequests = [], this._lockStatisticUpdates = false, this._supportsInstanceCleanup = true, this._shouldUnlockAttributeView = false, this._layerView = e4, this.addTransitionable(this._layerView.featureEffectView);
  }
  destroy() {
    super.destroy(), this._renderState.value = u(this._renderState.value), this._renderStateNext.value = u(this._renderStateNext.value);
  }
  get batching() {
    return has("featurelayer-strict-draw-order") ? 1 : has("featurelayer-force-marker-text-draw-order") ? 2 : 0;
  }
  get updating() {
    const e4 = this._updatingChildren.value, t3 = !this._renderState.value || this._renderState.value.updating, r4 = !!this._renderStateNext.value, s4 = this.attributeView.updating;
    return e4 || t3 || r4 || s4;
  }
  subscriptions() {
    return this._subscriptions.values();
  }
  get hasLabels() {
    return null !== this.labelSchema;
  }
  get hasHighlight() {
    return this._layerView.hasHighlight;
  }
  get children() {
    return this._renderState.value ? this._renderState.value.tiles().filter((e4) => e4.presenting) : [];
  }
  get usedMemory() {
    let e4 = 0;
    return this._renderState.value && (e4 += this._renderState.value.usedMemory), this._renderStateNext.value && (e4 += this._renderStateNext.value.usedMemory), e4 += this.attributeView.usedMemory, e4;
  }
  get instanceStore() {
    return this._store;
  }
  get labelingCollisionInfos() {
    if (!this._deconflictionInfo) throw new Error("InternalError: DeconflictionInfo not defined. Was update called?");
    return this._deconflictionInfo;
  }
  get labelSchema() {
    var _a;
    return ((_a = this._schema) == null ? void 0 : _a.mesh.factory.labels) ?? null;
  }
  get layerOrder() {
    return this._layerView.view.allLayerViews.items.indexOf(this._layerView);
  }
  get tiles() {
    var _a;
    return (_a = this._renderState.value) == null ? void 0 : _a.tiles();
  }
  get requiresRefresh() {
    var _a;
    return !!((_a = this._renderState.value) == null ? void 0 : _a.requiresRefresh);
  }
  get _instanceStore() {
    return this._store;
  }
  _getHeatmapInstance(e4) {
    if (null == this._instanceStore || !(e4.drawPhase & x.heatmap.drawPhase)) return null;
    for (const t3 of this._instanceStore.values()) if (v(t3)) return t3;
    return null;
  }
  update(e4, t3) {
    this._schema = e4, this._deconflictionInfo = t3;
  }
  setCoverage(e4) {
    this._presentation.updateCoverage(e4);
  }
  getPresenting() {
    var _a;
    const e4 = /* @__PURE__ */ new Set();
    for (const t3 of ((_a = this._renderState.value) == null ? void 0 : _a.tiles()) ?? []) t3.presenting && e4.add(t3.id);
    return e4;
  }
  beforeRender(e4) {
    if (super.beforeRender(e4), this._renderState.value) {
      for (const t3 of this._renderState.value.tiles()) t3.setTransform(e4.state);
      if (!e4.decluttering) {
        this._presentation.update(this._renderState.value.tiles());
        for (const e5 of this._renderState.value.tiles()) e5.upload(this._instanceStore, this.batching);
      }
      this._updatingChildren.value = !this.children.every((e5) => e5.ended && e5.presenting);
    }
  }
  renderChildren(e4) {
    switch (super.renderChildren(e4), e4.drawPhase) {
      case 1:
        return this._renderMapPhase(e4);
      case 16:
        return this._renderHighlightPhase(e4);
      case 2:
        return this._renderLabelPhase(e4);
    }
  }
  afterRender(e4) {
    var _a;
    super.afterRender(e4), e4.decluttering || (this._finializeUpdate(), this._updateAttributeView(), this._handleSwapRenderState(), (_a = this._renderState.value) == null ? void 0 : _a.update(this.attributeView.currentEpoch)), this._hitTestsRequests.length && this.requestRender();
  }
  updateStart(e4) {
    var _a;
    return this.attributeView.lockTextureUploads(), this._supportsInstanceCleanup = !e4 && "dictionary" !== ((_a = this._schema) == null ? void 0 : _a.mesh.factory.symbology.type), this.instanceStore.updateStart(this._supportsInstanceCleanup), this.instanceStore;
  }
  async updateEnd(e4, t3, r4) {
    return this._updateResolver = new c3({ schema: e4, deconflictionInfo: t3, invalidateMesh: r4 }), this.requestRender(), e4.mesh.factory.referenceScale && (this.referenceScale = e4.mesh.factory.referenceScale), this._updateResolver.resolver.promise;
  }
  updateAttributeView(e4) {
    this.requestRender(), this.attributeView.requestUpdate(e4);
  }
  updateSubscriptions(e4) {
    for (const { tileId: t3, version: r4 } of e4.subscribe) {
      if (!this._subscriptions.has(t3)) {
        const e5 = new f3(t3, r4);
        this._subscriptions.set(t3, e5);
        continue;
      }
      this._subscriptions.get(t3).version = r4;
    }
    for (const t3 of e4.unsubscribe) this._subscriptions.delete(t3), this.removeTile(t3);
  }
  isFeatureFiltered(e4) {
    const t3 = this.attributeView.getFilterFlags(e4), r4 = t3 & X, s4 = null == this._layerView.featureEffectView.featureEffect || this._layerView.featureEffectView.featureEffect.excludedLabelsVisible || t3 & Y;
    return !(r4 && s4);
  }
  getDisplayStatistics(e4, t3) {
    const r4 = this._statisticsByLevel.get(e4);
    return r4 ? r4.get(t3) : null;
  }
  updateStatistics(e4, t3) {
    if (this._lockStatisticUpdates) return void this._updateStatisticsRequests.push({ level: e4, statistics: t3 });
    let r4 = this._statisticsByLevel.get(e4);
    r4 || (r4 = /* @__PURE__ */ new Map(), this._statisticsByLevel.set(e4, r4));
    for (const s4 of t3) r4.set(s4.fieldName, { minValue: s4.minValue, maxValue: s4.maxValue });
  }
  beginLevelMeshUpdate(e4) {
    if (null != this._pendingLevelUpdate) throw new Error("InternalError: Pending level mesh update in progress");
    return this._pendingLevelUpdate = new p3(e4), has("esri-2d-update-debug") && console.debug(`FeatureContainer.beginLevelMeshUpdate(level=${e4}, id=${this._pendingLevelUpdate.id})`), this._pendingLevelUpdate.id;
  }
  applyLevelMeshUpdate(e4) {
    if (null == this._pendingLevelUpdate) throw new Error("InternalError: No level mesh update to apply");
    if (this._pendingLevelUpdate.id !== e4) throw new Error("InternalError: Unexpected id");
    if (has("esri-2d-update-debug")) {
      const { id: e5, level: t4 } = this._pendingLevelUpdate;
      console.debug(`FeatureContainer.applyLevelMeshUpdate(level=${t4}, id=${e5})`);
    }
    const { level: t3, messages: r4 } = this._pendingLevelUpdate, s4 = this._renderStateNext.value ?? this._renderState.value;
    if (!s4) throw new Error("InternalError: No renderState defined");
    s4.enqueueLevelReplace(t3, r4), this._pendingLevelUpdate = null, this.requestRender();
  }
  lockForOverrides() {
    var _a;
    (_a = this._renderState.value) == null ? void 0 : _a.lockUploads(), this._lockStatisticUpdates = true, this.attributeView.locked || (this.attributeView.lockTextureUploads(), this._shouldUnlockAttributeView = true);
  }
  unlockForOverrides() {
    var _a, _b;
    (_a = this._renderState.value) == null ? void 0 : _a.unlockUploads(), this._shouldUnlockAttributeView && (this.attributeView.unlockTextureUploads() && this.hasLabels && this.requestDeclutter(), this._shouldUnlockAttributeView = false), this._lockStatisticUpdates = false;
    for (const e4 of this._updateStatisticsRequests) this.updateStatistics(e4.level, e4.statistics);
    this._updateStatisticsRequests = [], (_b = this._renderState.value) == null ? void 0 : _b.flush(this.attributeView.currentEpoch), this.requestRender();
  }
  async updateRenderState(e4) {
    if (this._renderStateNext.value) throw new Error("InternalError: renderStateNext already exists");
    has("esri-2d-update-debug") && console.debug(`Version[${e4}] FeatureContainer.updateRenderState`), this._renderStateNext.value = new d(() => this._stage, (e5) => {
      var _a;
      return (_a = this._subscriptions.get(e5)) == null ? void 0 : _a.version;
    }, e4, this.tilingScheme);
  }
  async swapRenderState() {
    if (!this._renderStateNext.value) throw new Error("InternalError: renderStateNext not yet created");
    if (this._renderStateSwapResolver) throw new Error("InternalError: renderState swap already in progress");
    this._renderStateSwapResolver = Promise.withResolvers(), await this._renderStateSwapResolver.promise;
  }
  async onMessage(e4, r4) {
    if (u2(r4), !this._pendingLevelUpdate) return this._applyMessage(e4);
    this._pendingLevelUpdate.messages.push(e4);
  }
  removeTile(e4) {
    (this._renderState.value || this._renderStateNext.value) && (this._renderState.value && this._renderState.value.removeTile(e4), this._renderStateNext.value && this._renderStateNext.value.removeTile(e4));
  }
  hitTest(e4) {
    const t3 = f(e4);
    if (!t3) return console.warn("Unexpected hittest input:", e4), Promise.resolve({ trueHitIds: [], candidateHitIds: [] });
    let r4 = this._hitTestsRequests.find(({ query: e5 }) => t3.hittestType === e5.hittestType && t3.input === e5.input);
    const s4 = Promise.withResolvers();
    return r4 ? r4.resolvers.push(s4) : (r4 = { query: t3, resolvers: [s4] }, this._hitTestsRequests.push(r4)), this.requestRender(), s4.promise;
  }
  getSortKeys(e4) {
    const t3 = new Set(e4), r4 = /* @__PURE__ */ new Map();
    for (const s4 of this.children) if (s4.getSortKeys(t3).forEach((e5, t4) => r4.set(t4, e5)), r4.size === t3.size) break;
    return r4;
  }
  get hasAnimation() {
    return this.hasLabels;
  }
  doRender(e4) {
    const { minScale: t3, maxScale: r4 } = this._layerView.layer.effectiveScaleRange, s4 = e4.state.scale;
    s4 <= (t3 || 1 / 0) && s4 >= r4 && super.doRender(e4);
  }
  setStencilReference(e4) {
    if (null == this._getHeatmapInstance(e4)) super.setStencilReference(e4);
    else for (const t3 of this.children) t3.stencilRef = x.heatmap.getStencilReference(t3);
  }
  createRenderParams(e4) {
    const t3 = super.createRenderParams(e4);
    return t3.referenceScale = this.referenceScale, t3;
  }
  _applyMessage(e4) {
    const t3 = e4.inner;
    if (!this._subscriptions.has(t3.id)) return;
    const r4 = this._subscriptions.get(t3.id);
    if (r4.version !== t3.subscriptionVesrion) {
      if (has("esri-2d-update-debug")) {
        const e5 = `${t3.subscriptionVesrion} != ${r4.version}`;
        console.debug(`Version[${e5}] Tile[${t3.id}] FeatureContainer - Dropping message, outdated version]`, t3);
      }
      return;
    }
    const s4 = this._renderStateNext.value ?? this._renderState.value;
    if (!s4) throw new Error("InternalError: No renderState defined");
    s4.version !== t3.version && console.error(`InternalError: Version mismatch. [renderState: ${s4.version}, message: ${t3.version}]`), s4.enqueueUpdate(e4), this.requestRender();
  }
  _finializeUpdate() {
    if (!this._updateResolver) return;
    const { schema: e4, deconflictionInfo: t3, invalidateMesh: r4 } = this._updateResolver.inner;
    this._schema = e4, this._deconflictionInfo = t3, this.attributeView.unlockTextureUploads() && this.hasLabels && this.requestDeclutter(), this.instanceStore.updateEnd(this._supportsInstanceCleanup), r4 && (this.restartAllAnimations(), this.swapRenderState());
    const s4 = "heatmap" === e4.mesh.factory.symbology.type ? "target-defer" : "eager";
    this._presentation.updateStrategy(s4), this._updateResolver.resolver.resolve(), this._updateResolver = null, this.requestRender();
  }
  _handleSwapRenderState() {
    if (this._renderStateSwapResolver && this._renderStateNext.value) {
      has("esri-2d-update-debug") && console.debug(`Version[${this._renderStateNext.value}] FeatureContainer.update.swapRenderState`);
      const e4 = this._renderState.value, t3 = /* @__PURE__ */ new Set(), r4 = /* @__PURE__ */ new Set();
      for (const s4 of (e4 == null ? void 0 : e4.tiles()) ?? []) s4.decluttering && t3.add(s4.id), s4.presenting && r4.add(s4.id);
      this._renderState.value = this._renderStateNext.value, this._renderStateNext.value = null, this._renderState.value.flush(this.attributeView.currentEpoch), e4 == null ? void 0 : e4.destroy(), this._renderStateSwapResolver.resolve(), this._renderStateSwapResolver = null;
      for (const s4 of this._renderState.value.tiles()) t3.has(s4.id) && (s4.decluttering = true), r4.has(s4.id) && (s4.presenting = true);
      this.requestRender(), this.hasLabels && this.requestDeclutter();
    }
  }
  _renderMapPhase(e4) {
    this._layerView.featureEffectView.hasEffects ? (this._renderOutsideEffect(e4), this._renderInsideEffect(e4)) : this._renderFeatures(e4, 0), this._hitTestsRequests.length > 0 && this._renderHittest(e4);
  }
  _renderHighlightPhase(e4) {
    this.hasHighlight && p(e4, false, (e5) => {
      this._renderFeatures(e5, 1);
    });
  }
  _renderLabelPhase(e4) {
    this._renderFeatures(e4, 0);
  }
  _renderInsideEffect(e4) {
    const t3 = e4.painter.effects.insideEffect;
    t3.bind(e4), this._renderFeatures(e4, 2), t3.draw(e4, this._layerView.featureEffectView.includedEffects), t3.unbind();
  }
  _renderOutsideEffect(e4) {
    const t3 = e4.painter.effects.outsideEffect;
    t3.bind(e4), this._renderFeatures(e4, 3), t3.draw(e4, this._layerView.featureEffectView.excludedEffects), t3.unbind();
  }
  _renderHittest(e4) {
    const { context: t3 } = e4, r4 = e4.painter.effects.hittest, s4 = t3.boundFramebuffer, i2 = t3.getViewport(), n3 = e4.passOptions, a3 = e4.drawPhase;
    r4.bind(e4), e4.passOptions = r4.createOptions(e4, this._hitTestsRequests), e4.drawPhase = 8;
    const { distance: o3, smallSymbolDistance: l3 } = e4.passOptions, d3 = Math.max(o3, l3), h2 = e4.passOptions.hittestType;
    for (const u6 of this.children) u6.visible && ("rect" === h2 ? u6.containsScreenRect(e4.state, e4.passOptions.tlbr) && this._renderTile(u6, e4, 0) : u6.containsScreenPoint(e4.state, e4.passOptions.position, 2 * d3) && this._renderTile(u6, e4, 0));
    r4.draw(e4), r4.unbind(e4), t3.bindFramebuffer(s4), t3.restoreViewport(i2), e4.passOptions = n3, e4.drawPhase = a3;
  }
  _renderFeatures(e4, t3) {
    const r4 = this._getHeatmapInstance(e4);
    null != r4 ? this._renderHeatmapFeatures(e4, t3, r4) : this._renderGeometryFeatures(e4, t3);
  }
  _renderGeometryFeatures(e4, t3) {
    for (const r4 of this.children) r4.visible && this._renderTile(r4, e4, t3);
  }
  _renderHeatmapFeatures(e4, t3, r4) {
    for (const s4 of this.children) s4.visible && this._renderTile(s4, e4, t3, 19);
    r4.techniqueRef.renderResolvePass(e4, r4);
  }
  _renderTile(e4, t3, r4, s4) {
    var _a;
    t3.selection = r4, (_a = e4.getDisplayList()) == null ? void 0 : _a.render(t3, s4);
  }
};
function v(e4) {
  return 19 === e4.techniqueRef.type;
}

// node_modules/@arcgis/core/symbols/cim/SDFHelper.js
var a2 = 0.45;
function s3(t3) {
  var _a;
  switch (t3.type) {
    case "CIMPointSymbol": {
      const o3 = t3.symbolLayers;
      if (!o3 || 1 !== o3.length) return null;
      const n3 = o3[0];
      return "CIMVectorMarker" !== n3.type ? null : s3(n3);
    }
    case "CIMVectorMarker": {
      const o3 = t3.markerGraphics;
      if (!o3 || 1 !== o3.length) return null;
      const n3 = o3[0];
      if (!n3) return null;
      const r4 = n3.geometry, e4 = c4(r4);
      if (!e4) return null;
      const i2 = n3.symbol;
      if (!i2 || "CIMPolygonSymbol" !== i2.type && "CIMLineSymbol" !== i2.type || ((_a = i2.symbolLayers) == null ? void 0 : _a.some((t4) => !!t4.effects))) return null;
      let a3 = 0;
      for (const t4 of i2.symbolLayers ?? []) a3 = Math.max("CIMSolidStroke" === t4.type ? t4.width : 0, a3);
      const { sdfTextureSize: s4 } = d2(e4, t3.frame, t3.size, t3.anchorPoint, "Relative" !== t3.anchorPointUnits, a3, t3.scaleSymbolsProportionally);
      return { type: "sdf", geometry: r4, sdfTextureSize: s4, sdfPaddingRatio: 0.5, asFill: "CIMPolygonSymbol" === i2.type };
    }
  }
}
function l2(t3) {
  return t3 ? t3.rings ? t3.rings : t3.paths ? t3.paths : void 0 !== t3.xmin && void 0 !== t3.ymin && void 0 !== t3.xmax && void 0 !== t3.ymax ? [[[t3.xmin, t3.ymin], [t3.xmin, t3.ymax], [t3.xmax, t3.ymax], [t3.xmax, t3.ymin], [t3.xmin, t3.ymin]]] : null : null;
}
function f4(t3) {
  let o3 = 1 / 0, n3 = -1 / 0, e4 = 1 / 0, i2 = -1 / 0;
  for (const r4 of t3) for (const t4 of r4) t4[0] < o3 && (o3 = t4[0]), t4[0] > n3 && (n3 = t4[0]), t4[1] < e4 && (e4 = t4[1]), t4[1] > i2 && (i2 = t4[1]);
  return new t2(o3, e4, n3 - o3, i2 - e4);
}
function m(t3) {
  let o3 = 1 / 0, n3 = -1 / 0, r4 = 1 / 0, e4 = -1 / 0;
  for (const i2 of t3) for (const t4 of i2) t4[0] < o3 && (o3 = t4[0]), t4[0] > n3 && (n3 = t4[0]), t4[1] < r4 && (r4 = t4[1]), t4[1] > e4 && (e4 = t4[1]);
  return [o3, r4, n3, e4];
}
function c4(t3) {
  return t3 ? t3.rings ? m(t3.rings) : t3.paths ? m(t3.paths) : n2(t3) ? [t3.xmin, t3.ymin, t3.xmax, t3.ymax] : null : null;
}
function h(t3, o3, n3) {
  const [r4, e4, a3, s4] = c4(t3), l3 = a3 - r4, f5 = s4 - e4, m2 = o3, h2 = dt, u6 = Math.floor(0.5 * (m2 * n3 - h2)), y2 = (m2 - 2 * (u6 + h2)) / Math.max(l3, f5), d3 = Math.round(l3 * y2), x3 = Math.round(f5 * y2);
  return { pixelDimensions: [l3, f5], texelDimensions: [Math.round((d3 + 2 * u6) / y2), Math.round((x3 + 2 * u6) / y2)] };
}
var u5 = 1.61803;
var y = 512;
function d2(t3, n3, r4, s4, l3, f5, m2) {
  const [c5, h2, d3, x3] = t3;
  if (d3 < c5 || x3 < h2) return { frameSizeRatio: 0, anchorX: 0, anchorY: 0, widthRatio: 1, sdfPaddingRatio: 0.5, sdfTextureSize: nt };
  const M2 = d3 - c5, g2 = x3 - h2, p5 = Math.max(M2, g2);
  let S, R = 0.5;
  {
    if (!m2 && null != n3 && r4 > 0) {
      f5 *= (n3.ymax - n3.ymin) / r4;
    }
    const t4 = f5 / (f5 + p5), o3 = 0.1;
    t4 > a2 && t4 < 1 && (R = Math.min(t4 + o3, 0.99));
  }
  {
    const t4 = n3 ? n3.ymax - n3.ymin : 0, a3 = t4 > 0 ? t4 : g2, s5 = a3 > 0 ? (p5 + f5) / a3 : 1, l4 = e(r4) * s5;
    let m3 = Math.floor(l4 * u5 + 2 * dt);
    m3 = Math.min(m3, y), m3 = Math.max(m3, nt), S = 1 << 32 - Math.clz32(m3 - 1);
  }
  const w = S, P = dt, b = Math.floor(0.5 * (w * R - P)), z = (w - 2 * (b + P)) / p5, C = Math.round(M2 * z) + 2 * b, I = Math.round(g2 * z) + 2 * b;
  let j = 1;
  if (n3) {
    j = I * (1 - R) / ((n3.ymax - n3.ymin) * z);
  }
  let v2 = 0, T = 0, k = 1;
  s4 && (l3 ? n3 && r4 && n3.ymax - n3.ymin > 0 && (k = (n3.xmax - n3.xmin) / (n3.ymax - n3.ymin), v2 = s4.x / (r4 * k), T = s4.y / r4) : (v2 = s4.x, T = s4.y)), n3 && (v2 = 0.5 * (n3.xmax + n3.xmin) + v2 * (n3.xmax - n3.xmin), T = 0.5 * (n3.ymax + n3.ymin) + T * (n3.ymax - n3.ymin)), v2 -= c5, T -= h2, v2 *= z, T *= z, v2 += b, T += b;
  let F = v2 / C - 0.5, L = T / I - 0.5;
  return l3 && r4 && (F *= r4 * k, L *= r4), { frameSizeRatio: j, anchorX: F, anchorY: L, widthRatio: k, sdfTextureSize: S, sdfPaddingRatio: R };
}
function x2(t3) {
  const o3 = l2(t3.geometry), n3 = f4(o3), r4 = t3.sdfTextureSize, e4 = dt, a3 = Math.floor(0.5 * (r4 * t3.sdfPaddingRatio - e4)), s4 = r4 - 2 * (a3 + e4), m2 = s4 / Math.max(n3.width, n3.height), c5 = Math.round(n3.width * m2) + 2 * a3, h2 = Math.round(n3.height * m2) + 2 * a3, u6 = [];
  for (const i2 of o3) if (i2 && i2.length > 1) {
    const o4 = [];
    for (const r5 of i2) {
      let [e5, i3] = r5;
      e5 -= n3.x, i3 -= n3.y, e5 *= m2, i3 *= m2, e5 += a3 - 0.5, i3 += a3 - 0.5, t3.asFill ? o4.push([e5, i3]) : o4.push([Math.round(e5), Math.round(i3)]);
    }
    if (t3.asFill) {
      const t4 = o4.length - 1;
      o4[0][0] === o4[t4][0] && o4[0][1] === o4[t4][1] || o4.push(o4[0]);
    }
    u6.push(o4);
  }
  const y2 = M(u6, c5, h2, a3);
  return t3.asFill && g(u6, c5, h2, a3, y2), { data: p4(y2, a3), width: c5, height: h2, sdfPaddingRatio: t3.sdfPaddingRatio, sdfDecodeCoeff: 2 * a3 / s4 };
}
function M(t3, o3, n3, r4) {
  const e4 = o3 * n3, i2 = new Array(e4), a3 = r4 * r4 + 1;
  for (let s4 = 0; s4 < e4; ++s4) i2[s4] = a3;
  for (const s4 of t3) {
    const t4 = s4.length;
    for (let e5 = 1; e5 < t4; ++e5) {
      const t5 = s4[e5 - 1], a4 = s4[e5];
      let l3, f5, m2, c5;
      t5[0] < a4[0] ? (l3 = t5[0], f5 = a4[0]) : (l3 = a4[0], f5 = t5[0]), t5[1] < a4[1] ? (m2 = t5[1], c5 = a4[1]) : (m2 = a4[1], c5 = t5[1]);
      let h2 = Math.floor(l3) - r4, u6 = Math.floor(f5) + r4, y2 = Math.floor(m2) - r4, d3 = Math.floor(c5) + r4;
      h2 < 0 && (h2 = 0), u6 > o3 && (u6 = o3), y2 < 0 && (y2 = 0), d3 > n3 && (d3 = n3);
      const x3 = a4[0] - t5[0], M2 = a4[1] - t5[1], g2 = x3 * x3 + M2 * M2;
      for (let r5 = h2; r5 < u6; r5++) for (let e6 = y2; e6 < d3; e6++) {
        const s5 = r5 + 0.5, l4 = e6 + 0.5;
        let f6, m3, c6 = (s5 - t5[0]) * x3 + (l4 - t5[1]) * M2;
        c6 < 0 ? (f6 = t5[0], m3 = t5[1]) : c6 > g2 ? (f6 = a4[0], m3 = a4[1]) : (c6 /= g2, f6 = t5[0] + c6 * x3, m3 = t5[1] + c6 * M2);
        const h3 = (s5 - f6) * (s5 - f6) + (l4 - m3) * (l4 - m3), u7 = (n3 - e6 - 1) * o3 + r5;
        h3 < i2[u7] && (i2[u7] = h3);
      }
    }
  }
  for (let s4 = 0; s4 < e4; ++s4) i2[s4] = Math.sqrt(i2[s4]);
  return i2;
}
function g(t3, o3, n3, r4, e4) {
  for (const i2 of t3) {
    const t4 = i2.length;
    for (let a3 = 1; a3 < t4; ++a3) {
      const t5 = i2[a3 - 1], s4 = i2[a3];
      let l3, f5, m2, c5;
      t5[0] < s4[0] ? (l3 = t5[0], f5 = s4[0]) : (l3 = s4[0], f5 = t5[0]), t5[1] < s4[1] ? (m2 = t5[1], c5 = s4[1]) : (m2 = s4[1], c5 = t5[1]);
      let h2 = Math.floor(l3), u6 = Math.floor(f5) + 1, y2 = Math.floor(m2), d3 = Math.floor(c5) + 1;
      h2 < r4 && (h2 = r4), u6 > o3 - r4 && (u6 = o3 - r4), y2 < r4 && (y2 = r4), d3 > n3 - r4 && (d3 = n3 - r4);
      for (let i3 = y2; i3 < d3; ++i3) {
        if (t5[1] > i3 == s4[1] > i3) continue;
        const a4 = i3 + 0.5, l4 = (n3 - i3 - 1) * o3;
        for (let o4 = h2; o4 < u6; ++o4) {
          o4 + 0.5 < (s4[0] - t5[0]) * (a4 - t5[1]) / (s4[1] - t5[1]) + t5[0] && (e4[l4 + o4] = -e4[l4 + o4]);
        }
        for (let t6 = r4; t6 < h2; ++t6) e4[l4 + t6] = -e4[l4 + t6];
      }
    }
  }
}
function p4(o3, n3) {
  const r4 = 2 * n3, e4 = o3.length, i2 = new Uint8Array(4 * e4);
  for (let a3 = 0; a3 < e4; ++a3) {
    const n4 = 0.5 - o3[a3] / r4;
    o2(n4, i2, 4 * a3);
  }
  return i2;
}

export {
  s3 as s,
  c4 as c,
  h,
  d2 as d,
  x2 as x,
  _
};
//# sourceMappingURL=chunk-64HOLB7R.js.map
