import type Accessor from "../../../../core/Accessor.js";

/**
 * Implements a queuing system to support `DrawTool`'s management of
 * plugin lifecycles. Helps to ensure that plugins are set up,
 * canceled, and completed in a predictable manner.
 *
 * @internal
 */
export abstract class PluginLifecycleManager extends Accessor {}