/**
 * Provides a utility function to determine whether a graphic's origin is a [AnnotationFeatureGraphicOrigin](https://developers.arcgis.com/javascript/latest/references/core/graphic/AnnotationFeatureGraphicOrigin/).
 *
 * @since 5.1
 * @example
 * // Use hitTest() to get a graphic, then check whether it originated from a AnnotationFeatureLayer.
 * view.hitTest(screenPoint).then((response) => {
 *   const graphic = response.results[0]?.graphic;
 *   if (!graphic) {
 *     return;
 *   }
 *   if (isAnnotationFeatureGraphicOrigin(graphic.origin)) {
 *     // hitTest returned a graphic from a AnnotationFeatureLayer.
 *     // Use this info for your processing logic.
 *   } else {
 *     console.log("graphic's origin is NOT a AnnotationFeatureLayer");
 *   }
 * });
 */
import type AnnotationFeatureGraphicOrigin from "./AnnotationFeatureGraphicOrigin.js";
import type GraphicOrigin from "./GraphicOrigin.js";

/**
 * Utility function that determines whether a graphic’s origin is a [AnnotationFeatureGraphicOrigin](https://developers.arcgis.com/javascript/latest/references/core/graphic/AnnotationFeatureGraphicOrigin/).
 *
 * @param origin - The graphic origin to check.
 * @returns Returns `true` if the graphic origin is of type [AnnotationFeatureGraphicOrigin](https://developers.arcgis.com/javascript/latest/references/core/graphic/AnnotationFeatureGraphicOrigin/), `false` otherwise.
 */
export function isAnnotationFeatureGraphicOrigin(origin: GraphicOrigin | null | undefined): origin is AnnotationFeatureGraphicOrigin;