import type Accessor from "../../../core/Accessor.js";

/**
 * Support object that manages the available plugin metadata
 * for Sketch and SketchViewModel.
 *
 * Implements compatibility between legacy modes of configuring
 * available create tools, symbology, etc.
 *
 * @internal
 */
export abstract class PluginManager extends Accessor {}