import type { __FeatureLayerSansType } from "./FeatureLayer.js";
import type SpatialReference from "../geometry/SpatialReference.js";
import type FeatureLayer from "./FeatureLayer.js";
import type Field from "./support/Field.js";
import type FieldConfiguration from "./support/FieldConfiguration.js";
import type PortalItem from "../portal/PortalItem.js";
import type FeatureSet from "../rest/support/FeatureSet.js";
import type { LayerSaveAsOptions, LayerSaveOptions } from "./types.js";
import type { ConstantElevation as IConstantElevation, ElevationSource as IElevationSource, Accuracy, OrientedImageryType, TimeIntervalUnit, SequenceOrderFieldInfoJSON } from "./orientedImagery/types.js";
import type { PortalItemProperties } from "../portal/PortalItem.js";
import type { RequestOptions } from "../request/types.js";
import type { QueryProperties } from "../rest/support/Query.js";
import type { ObjectId } from "../views/types.js";
import type { FieldProperties } from "./support/Field.js";
import type { SpatialReferenceProperties } from "../geometry/SpatialReference.js";
import type { FeatureLayerProperties } from "./FeatureLayer.js";

export interface OrientedImageryLayerProperties extends FeatureLayerProperties, Partial<Pick<OrientedImageryLayer, "cameraHeading" | "cameraHeight" | "cameraPitch" | "cameraRoll" | "coveragePercent" | "demPathPrefix" | "demPathSuffix" | "depthImagePathPrefix" | "depthImagePathSuffix" | "elevationSource" | "farDistance" | "horizontalFieldOfView" | "horizontalMeasurementUnit" | "imagePathPrefix" | "imagePathSuffix" | "imageRotation" | "maximumDistance" | "nearDistance" | "orientationAccuracy" | "orientedImageryType" | "sequenceOrderFields" | "timeIntervalUnit" | "verticalFieldOfView" | "verticalMeasurementUnit" | "videoPathPrefix" | "videoPathSuffix">> {
  /**
   * Copyright information for the layer.
   *
   * @since 4.29
   */
  copyright?: string | null;
  /**
   * The SQL where clause used to filter features on the client. Only the features that satisfy the definition
   * expression are displayed in the [View](https://developers.arcgis.com/javascript/latest/references/core/views/View/). Setting a definition expression is useful
   * when the dataset is large and you don't want to bring all features to the client for analysis.
   * Definition expressions may be set when a layer is constructed prior to it loading in the view or
   * after it has been added to the map. If the definition expression is set after the layer has been added to the map, the view will
   * automatically refresh itself to display the features that satisfy the new definition expression.
   *
   * @since 4.29
   * @example
   * // Set definition expression in constructor to only display trees with scientific name Ulmus pumila
   * const layer = new FeatureLayer({
   *   url: "https://services.arcgis.com/V6ZHFr6zdgNZuVG0/arcgis/rest/services/Landscape_Trees/FeatureServer/0",
   *   definitionExpression: "Sci_Name = 'Ulmus pumila'"
   * });
   * @example
   * // Set the definition expression directly on layer instance to only display trees taller than 50ft
   * layer.definitionExpression = "HEIGHT > 50";
   */
  definitionExpression?: string | null;
  /**
   * The name of the layer's primary display field. The value of this property matches the name of one of the fields of the layer.
   *
   * @since 4.29
   */
  displayField?: string | null;
  /**
   * An array of [FieldConfiguration](https://developers.arcgis.com/javascript/latest/references/core/layers/support/FieldConfiguration/) objects that control how fields are displayed in popups and other UI elements. Each object specifies options for an individual field, such as its display name, via `alias`, and formatting rules, via `fieldFormat`. Assign this property before the layer loads to ensure the configurations take effect. For more information, see the [FieldConfiguration](https://developers.arcgis.com/javascript/latest/references/core/layers/support/FieldConfiguration/) documentation.
   * If no field configurations are provided, they are automatically created when a [popupTemplate](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#popupTemplate) is assigned to the layer. If no [popupTemplate](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#popupTemplate) is set, you can manually define field configurations to customize how fields are shown.
   * > [!WARNING]
   * >
   * > Support is limited in version 5.1. For details, see the [FieldConfiguration](https://developers.arcgis.com/javascript/latest/references/core/layers/support/FieldConfiguration/) documentation.
   *
   * To modify field configurations, clone [fieldConfigurations](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#fieldConfigurations), make the changes, and then reassign the updated clone back to [fieldConfigurations](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#fieldConfigurations).
   *
   * @since 5.1
   * @see [OrientedImageryLayer.getFieldConfiguration](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#getFieldConfiguration)
   * @see [OrientedImageryLayer.getFieldAlias](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#getFieldAlias)
   * @example
   * ```js
   * // Adding a new field configuration
   * const addNewConfig = (layer, fieldName, alias, fieldFormat) => {
   *   // Check if there is already an existing configuration for fieldName
   *   const existingConfig = layer.getFieldConfiguration(fieldName);
   *   if (!existingConfig) {
   *     const newConfig = new FieldConfiguration({ name: fieldName, alias, fieldFormat });
   *     const newConfigs = clone(layer.fieldConfigurations);
   *     newConfigs.push(newConfig);
   *     layer.fieldConfigurations = newConfigs;
   *   }
   * };
   * ```
   * @example
   * ```js
   * // Updating an existing field configuration
   * const updateConfig = (layer, fieldName, alias, fieldFormat) => {
   *   const existingConfig = layer.getFieldConfiguration(fieldName);
   *   if (existingConfig) {
   *     const newConfig = existingConfig.clone();
   *     newConfig.alias = alias;
   *     newConfig.fieldFormat = fieldFormat;
   *
   *     const index = layer.fieldConfigurations.indexOf(existingConfig);
   *     const newConfigs = clone(layer.fieldConfigurations);
   *     newConfigs[index] = newConfig;
   *     layer.fieldConfigurations = newConfigs;
   *   }
   * };
   * ```
   * @example
   * ```js
   * // Deleting an existing field configuration
   * const deleteConfig = (layer, fieldName) => {
   *   const existingConfig = layer.getFieldConfiguration(fieldName);
   *   if (existingConfig) {
   *     const index = layer.fieldConfigurations.indexOf(existingConfig);
   *     var newConfigs = clone(layer.fieldConfigurations);
   *     newConfigs.splice(index, 1);
   *     layer.fieldConfigurations = newConfigs;
   *   }
   * };
   * ```
   * @see [FeatureLayer.getFieldConfiguration](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/#getFieldConfiguration)
   * @see [FeatureLayer.getFieldAlias](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/#getFieldAlias)
   * @example
   * ```js
   * // Adding a new field configuration
   * const addNewConfig = (layer, fieldName, alias, fieldFormat) => {
   *   // Check if there is already an existing configuration for fieldName
   *   const existingConfig = layer.getFieldConfiguration(fieldName);
   *   if (!existingConfig) {
   *     const newConfig = new FieldConfiguration({ name: fieldName, alias, fieldFormat });
   *     const newConfigs = clone(layer.fieldConfigurations);
   *     newConfigs.push(newConfig);
   *     layer.fieldConfigurations = newConfigs;
   *   }
   * };
   * ```
   * @example
   * ```js
   * // Updating an existing field configuration
   * const updateConfig = (layer, fieldName, alias, fieldFormat) => {
   *   const existingConfig = layer.getFieldConfiguration(fieldName);
   *   if (existingConfig) {
   *     const newConfig = existingConfig.clone();
   *     newConfig.alias = alias;
   *     newConfig.fieldFormat = fieldFormat;
   *
   *     const index = layer.fieldConfigurations.indexOf(existingConfig);
   *     const newConfigs = clone(layer.fieldConfigurations);
   *     newConfigs[index] = newConfig;
   *     layer.fieldConfigurations = newConfigs;
   *   }
   * };
   * ```
   * @example
   * ```js
   * // Deleting an existing field configuration
   * const deleteConfig = (layer, fieldName) => {
   *   const existingConfig = layer.getFieldConfiguration(fieldName);
   *   if (existingConfig) {
   *     const index = layer.fieldConfigurations.indexOf(existingConfig);
   *     var newConfigs = clone(layer.fieldConfigurations);
   *     newConfigs.splice(index, 1);
   *     layer.fieldConfigurations = newConfigs;
   *   }
   * };
   * ```
   */
  fieldConfigurations?: FieldConfiguration[] | null;
  /**
   * An array of fields in the layer. Each field represents an attribute
   * that may contain a value for each feature in the layer. For example,
   * a field named `POP_2015`, stores information about total population as a
   * numeric value for each feature; this value represents the total number
   * of people living within the geographic bounds of the feature.
   *
   * @since 4.29
   * @see [source](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#source)
   * @see [objectIdField](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#objectIdField)
   * @see [Add an array of client-side features](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#client-side)
   * @see [source](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/#source)
   * @see [objectIdField](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/#objectIdField)
   * @see [Add an array of client-side features](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/#client-side)
   * @example
   * // define each field's schema
   * const fields = [
   *  new Field({
   *    name: "ObjectID",
   *    alias: "ObjectID",
   *    type: "oid"
   *  }), new Field({
   *    name: "description",
   *    alias: "Description",
   *    type: "string"
   *  }), new Field ({
   *    name: "title",
   *    alias: "Title",
   *    type: "string"
   *  })
   * ];
   *
   * // See the sample snippet for the source and renderer properties
   * const layer = new FeatureLayer({
   *   // geometryType and spatialReference are inferred
   *   // from the input source features
   *   source: features,
   *   // Object ID field is inferred from the fields array
   *   fields: fields,
   *   renderer: renderer
   * });
   */
  fields?: FieldProperties[];
  /**
   * Indicates whether to display labels for this layer. If `true`, labels will
   * appear as defined in the [labelingInfo](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#labelingInfo) property.
   *
   * @default true
   * @since 4.29
   * @see [labelingInfo](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#labelingInfo)
   */
  labelsVisible?: boolean;
  /**
   * Indicates whether the layer will be included in the legend.
   *
   * @default true
   * @since 4.31
   */
  legendEnabled?: boolean;
  /**
   * The name of an `oid` [field](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#fields) containing
   * a unique value or identifier for each feature in the layer.
   *
   * @since 4.29
   * @see [fields](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#fields)
   * @see [Add an array of client-side features](https://developers.arcgis.com/javascript/latest/references/core/layers/mixins/FeatureLayerBase/#client-side)
   * @example
   * // See the sample snippet for the source and fields properties
   * const layer = new FeatureLayer({
   *   source: features,
   *   fields: fields,
   *   objectIdField: "ObjectID",  // field name of the Object IDs
   *   geometryType: "point",
   *   renderer: <renderer>
   * });
   */
  objectIdField?: string;
  /**
   * An array of field names to include in the OrientedImageryLayer. If not specified, the layer will include all fields.
   *
   * @since 4.29
   * @see [FeatureLayerView.availableFields](https://developers.arcgis.com/javascript/latest/references/core/views/layers/FeatureLayerView/#availableFields)
   * @see [fieldUtils](https://developers.arcgis.com/javascript/latest/references/core/layers/support/fieldUtils/)
   * @example
   * // Includes all fields from the service in the layer
   * fl.outFields = ["*"];
   * @example
   * // Get the specified fields from the service in the layer
   * // These fields will be added to FeatureLayerView.availableFields
   * // along with rendering and labeling fields. Use these fields
   * // for client-side filtering and querying.
   * fl.outFields = ["NAME", "POP_2010", "FIPS", "AREA"];
   * @example
   * // set the outFields for the layer coming from webmap
   * webmap.when(function () {
   *   layer = webmap.layers.at(1);
   *   layer.outFields = ["*"];
   * });
   */
  outFields?: string[] | null;
  /**
   * Indicates whether to display popups when features in the layer are clicked. The layer needs to have a [popupTemplate](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#popupTemplate) to define what
   * information should be displayed in the popup. Alternatively, a default popup template may be automatically used if
   * [Popup.defaultPopupTemplateEnabled](https://developers.arcgis.com/javascript/latest/references/core/widgets/Popup/#defaultPopupTemplateEnabled) is set to `true`.
   *
   * @default true
   * @since 4.29
   * @see [SceneView.popup](https://developers.arcgis.com/javascript/latest/references/core/views/SceneView/#popup)
   * @see [View2D.popup](https://developers.arcgis.com/javascript/latest/references/core/views/View2D/#popup)
   * @see [createPopupTemplate()](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/#createPopupTemplate)
   * @see [SceneView.popup](https://developers.arcgis.com/javascript/latest/references/core/views/SceneView/#popup)
   * @see [View2D.popup](https://developers.arcgis.com/javascript/latest/references/core/views/View2D/#popup)
   */
  popupEnabled?: boolean;
  /**
   * The portal item referencing the Oriented Imagery layer item from which the OrientedImageryLayer is loaded.
   *
   * @since 4.29
   * @example
   * // While this example uses FeatureLayer, this same pattern can be
   * // used for other layers that may be loaded from portalItem ids.
   * const layer = new FeatureLayer({
   *   portalItem: {  // autocasts as new PortalItem()
   *     id: "caa9bd9da1f4487cb4989824053bb847"
   *   }  // the first layer in the service is returned
   * });
   * @example
   * // Set hostname when using an on-premise portal (default is ArcGIS Online)
   * // esriConfig.portalUrl = "http://myHostName.esri.com/arcgis";
   *
   * // While this example uses FeatureLayer, this same pattern can be
   * // used for SceneLayers.
   * const layer = new FeatureLayer({
   *   portalItem: {  // autocasts as new PortalItem()
   *     id: "8d26f04f31f642b6828b7023b84c2188"
   *   },
   *   // loads the third item in the given feature service
   *   layerId: 2
   * });
   * @example
   * // Initialize GeoJSONLayer by referencing a portalItem id pointing to geojson file.
   * const layer = new GeoJSONLayer({
   *   portalItem: new PortalItem({
   *     id: "81e769cd7031482797e1b0768f23c7e1",
   *     // optionally define the portal, of the item.
   *     // if not specified, the default portal defined is used.
   *     // see https://developers.arcgis.com/javascript/latest/references/core/config/#portalUrl
   *     portal: new Portal({
   *       url: "https://jsapi.maps.arcgis.com/"
   *     })
   *   }
   * });
   * @example
   * // This snippet loads a table hosted in ArcGIS Online.
   * const table = new FeatureLayer({
   *   portalItem: { // autocasts as esri/portal/PortalItem
   *     id: "123f4410054b43d7a0bacc1533ceb8dc"
   *   }
   * });
   *
   * // Before adding the table to the map, it must first be loaded and confirm it is the right type.
   * table.load().then(() => {
   *   if (table.isTable) {
   *     map.tables.add(table);
   *   }
   * });
   * @example
   * // While this example uses FeatureLayer, this same pattern can be
   * // used for other layers that may be loaded from portalItem ids.
   * const layer = new FeatureLayer({
   *   portalItem: { // autocasts as esri/portal/PortalItem
   *     id: "caa9bd9da1f4487cb4989824053bb847",
   *     // Set an API key to access a secure portal item configured with API key authentication.
   *     apiKey: "APIKEY"
   *   }
   * });
   */
  portalItem?: PortalItemProperties | null;
  /**
   * The spatial reference of the layer. The default value is WGS84.
   * This property can be set explicitly to project the longitude and latitude
   * coordinates when the OrientedImageryLayer is loaded. While not required, explicitly setting
   * the spatial reference of the layer will improve the performance
   * when projecting to a spatial reference other than the one used by the
   * coordinates in the raw data.
   *
   * @default SpatialReference.WGS84
   * @since 4.29
   * @see [SpatialReference.WGS84](https://developers.arcgis.com/javascript/latest/references/core/geometry/SpatialReference/#WGS84)
   */
  spatialReference?: SpatialReferenceProperties;
  /**
   * The URL of the Oriented Imagery layer service.
   *
   * @since 4.29
   * @example
   * // Pass the service url to create an Oriented Imagery layer.
   *
   *   const layer = new OrientedImageryLayer({
   *     url: "url-to-oriented-imagery-service"
   *   });
   *   map.add(layer);  // adds the layer to the map
   * });
   * @example
   * // Hosted Feature Service on ArcGIS Online
   * layer.url = "http://services.arcgis.com/V6ZHFr6zdgNZuVG0/arcgis/rest/services/origins/FeatureServer/0";
   * @example
   * // Layer from Map Service on ArcGIS Server
   * layer.url = "http://sampleserver6.arcgisonline.com/arcgis/rest/services/Census/MapServer/2";
   * @example
   * // Can also be used if URL points to service and not layer
   * const layer = new FeatureLayer({
   *   // Notice that the url doesn't end with /2
   *   url: "http://services.arcgis.com/V6ZHFr6zdgNZuVG0/arcgis/rest/services/MonterreyBayCanyon_WFL/FeatureServer",
   *   layerId: 2
   * });
   * @example
   * // Non-spatial table in San Francisco incidents service.
   * const table = new FeatureLayer({
   *   url: "https://sampleserver6.arcgisonline.com/arcgis/rest/services/SF311/FeatureServer/1"
   * });
   * // table must be loaded so it can be used in the app.
   * table.load().then(function() {
   *   // table is loaded. ready to be queried.
   * });
   */
  url?: string | null;
}

/**
 * * [Overview](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#overview)
 * * [Creating an OrientedImageryLayer](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#creating-an-orientedimagerylayer)
 *
 * <span id="overview"></span>
 * ## Overview
 *
 * An Oriented Imagery Layer is a single spatial layer that can be created from a
 * [Feature Service](https://doc.esri.com/en/arcgis-enterprise/latest/administer/what-is-a-feature-service-.html); or
 * ArcGIS Online or ArcGIS Enterprise portal items.
 *
 * Oriented Imagery layers are composed of discrete features (or camera locations), each of which has a [Geometry](https://developers.arcgis.com/javascript/latest/references/core/geometry/Geometry/)
 * that allows it to be rendered in either a 2D [MapView](https://developers.arcgis.com/javascript/latest/references/core/views/MapView/) or
 * 3D [SceneView](https://developers.arcgis.com/javascript/latest/references/core/views/SceneView/) as a
 * [Graphic](https://developers.arcgis.com/javascript/latest/references/core/Graphic/) with spatial context. These features also contain data
 * [Graphic.attributes](https://developers.arcgis.com/javascript/latest/references/core/Graphic/#attributes) that provide additional information about
 * the real-world feature it represents; attributes may be viewed in [popup](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#popupTemplate) windows
 * and used for [rendering](https://developers.arcgis.com/javascript/latest/references/core/renderers/Renderer/) the layer.
 *
 * The layer fetches the imagery data once you select a point on the map. It uses a custom BestImage logic to determine the most accurate image for a selected point and display it in the viewer widget.
 *
 * OrientedImageryLayers may be [queried](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#queryFeatures), [analyzed](https://developers.arcgis.com/javascript/latest/spatial-analysis/intro-geometry-operators/),
 * and [rendered](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#renderer) to visualize data in a spatial context.
 *
 *
 * <span id="creating-an-orientedimagerylayer"></span>
 * ## Creating an OrientedImageryLayer
 *
 * OrientedImageryLayers may be created in one of two ways: from a [service URL](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#url) or an ArcGIS portal [item ID](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#portalItem).
 *
 * <details>
 * <summary>Read More</summary>
 *
 * ### Reference a service URL
 *
 * To create an OrientedImageryLayer instance from a service, you must set the [url](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#url) property
 * to the REST endpoint of a layer in a Feature Service.
 * For a layer to be visible in a view, it must be added to the [Map](https://developers.arcgis.com/javascript/latest/references/core/Map/)
 * referenced by the view. See [Map.add()](https://developers.arcgis.com/javascript/latest/references/core/Map/#add) for information about adding layers to a map.
 *
 * ```js
 * const OrientedImageryLayer = await $arcgis.import("@arcgis/core/layers/OrientedImageryLayer.js");
 * const layer = new OrientedImageryLayer({
 *   url: "url-to-oriented-imagery-service"
 * });
 * map.add(layer);  // adds the layer to the map
 * ```
 *
 * ### Reference an ArcGIS portal Item ID
 *
 * You can also create an OrientedImageryLayer from its ID if it exists as an item in ArcGIS Online or ArcGIS Enterprise.
 * For example, the following snippet shows how to add a new OrientedImageryLayer instance to a map using the
 * [portalItem](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#portalItem) property.
 *
 * ```js
 * // points to a hosted Feature Layer with Oriented Imagery Layer as a sub layer in ArcGIS Online
 * const layer = new OrientedImageryLayer({
 *   portalItem: {  // autocasts as esri/portal/PortalItem
 *     id: "2c65c47b75654a078038abae52c58f70"
 *   }
 * });
 * map.add(layer);  // adds the layer to the map
 * ```
 *
 * </details>
 *
 * > [!WARNING]
 * >
 * > **Known Limitations**
 * >
 * > Locations with a very high density of features may not display all available features at small scales.
 * > Very large datasets may require potentially long initial load times, particularly at small scales.
 * > Server-side and client-side feature tile caching allow features to load much faster after the initial data download.
 * > We are continuously improving our feature fetching strategy and load time efficiency in each release.
 *
 * @since 4.28
 * @see [OrientedImageryViewer](https://developers.arcgis.com/javascript/latest/references/core/widgets/OrientedImageryViewer/)
 * @see [Sample - Intro to OrientedImageryLayer](https://developers.arcgis.com/javascript/latest/sample-code/layers-orientedimagerylayer/)
 * @see [Sample - OrientedImageryLayer in SceneView](https://developers.arcgis.com/javascript/latest/sample-code/layers-orientedimagerylayer-sceneview/)
 */
export default class OrientedImageryLayer extends __FeatureLayerSansType {
  /**
   * @example
   * const layer = new OrientedImageryLayer({
   *    url: "url-to-image-service"
   * });
   * map.add(layer);  // adds the layer to the map
   * @see [Client-side FeatureLayer](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/#client-side)
   * @example
   * // Typical usage
   * // Create featurelayer from feature service
   * const layer = new FeatureLayer({
   *   // URL to the service
   *   url: "https://sampleserver6.arcgisonline.com/arcgis/rest/services/USA/MapServer/0"
   * });
   *
   * // Typical usage
   * // Create featurelayer from client-side graphics
   * const layer = new FeatureLayer({
   *   source: graphics,
   *   fields: [{
   *     name: "ObjectID",
   *     alias: "ObjectID",
   *     type: "oid"
   *   }, {
   *     name: "place",
   *     alias: "Place",
   *     type: "string"
   *   }],
   *   objectIdField: "ObjectID",
   *   geometryType: "point"
   * });
   */
  constructor(properties?: OrientedImageryLayerProperties);
  /**
   * Camera orientation defining the first rotation around z-axis of the camera. Defined in degrees. Heading values are measured in the positive clockwise direction where north is defined as 0 degrees.
   * -999 is used when the orientation is unknown.
   *
   * @example
   * // point images without cameraHeading to north using camera heading
   * layer.cameraHeading = 90;
   */
  accessor cameraHeading: number | null | undefined;
  /**
   * The height of camera above the ground when the imagery was captured. The units are in meters. Camera height is used to determine the visible extent of the image, large values will result in a greater
   * view extent. Values should not be less than 0.
   *
   * @example
   * // Adds value to images without a cameraHeight
   * layer.cameraHeight = 100;
   */
  accessor cameraHeight: number | null | undefined;
  /**
   * Camera orientation defining the second rotation around x-axis of the camera in the positive counterclockwise direction. Defined in degrees. The pitch is 0 degrees when the camera is
   * facing straight down to ground. The valid range of pitch value is from 0 to 180 degrees, with 180 degrees for a camera facing straight up and 90 degrees for a camera facing horizon.
   *
   * @example
   * // Adds pitch value to images without a cameraPitch
   * layer.cameraPitch = 90;
   */
  accessor cameraPitch: number | null | undefined;
  /**
   * Camera orientation defining the final rotation around z-axis of the camera in the positive clockwise direction. The camera housing rotation is defined in degrees. Valid values range from -90 to +90.
   *
   * @example
   * // Adds roll value to images without a cameraRoll
   * layer.cameraRoll = 0;
   */
  accessor cameraRoll: number | null | undefined;
  /**
   * Copyright information for the layer.
   *
   * @since 4.29
   */
  accessor copyright: string | null | undefined;
  /**
   * Modifies the extent of the image's ground footprint. The ground footprint of each image is computed to search for images that contain the selected location, which is identified
   * as the red cross on the map. Valid values are from -50 to 50. Negative percentage values shrink the size of the ground footprint and positive values increase the size of the ground footprint.
   *
   * @example
   * // Adds a coveragePercent value to image footprint
   * layer.coveragePercent = 40;
   */
  accessor coveragePercent: number | null | undefined;
  /**
   * The SQL where clause used to filter features on the client. Only the features that satisfy the definition
   * expression are displayed in the [View](https://developers.arcgis.com/javascript/latest/references/core/views/View/). Setting a definition expression is useful
   * when the dataset is large and you don't want to bring all features to the client for analysis.
   * Definition expressions may be set when a layer is constructed prior to it loading in the view or
   * after it has been added to the map. If the definition expression is set after the layer has been added to the map, the view will
   * automatically refresh itself to display the features that satisfy the new definition expression.
   *
   * @since 4.29
   * @example
   * // Set definition expression in constructor to only display trees with scientific name Ulmus pumila
   * const layer = new FeatureLayer({
   *   url: "https://services.arcgis.com/V6ZHFr6zdgNZuVG0/arcgis/rest/services/Landscape_Trees/FeatureServer/0",
   *   definitionExpression: "Sci_Name = 'Ulmus pumila'"
   * });
   * @example
   * // Set the definition expression directly on layer instance to only display trees taller than 50ft
   * layer.definitionExpression = "HEIGHT > 50";
   */
  accessor definitionExpression: string | null | undefined;
  /** Prefix used to build the DEM url path in conjunction with the [elevationSource](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#elevationSource) attribute. */
  accessor demPathPrefix: string | null | undefined;
  /** Suffix used to build the DEM url path in conjunction with the [elevationSource](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#elevationSource) attribute. */
  accessor demPathSuffix: string | null | undefined;
  /** Prefix used to build the depth image url path in conjunction with the depth image attribute. */
  accessor depthImagePathPrefix: string | null | undefined;
  /** Suffix used to build the depth image url path in conjunction with the depth image attribute. */
  accessor depthImagePathSuffix: string | null | undefined;
  /**
   * The name of the layer's primary display field. The value of this property matches the name of one of the fields of the layer.
   *
   * @since 4.29
   */
  accessor displayField: string | null | undefined;
  /**
   * The source of elevation, that will be used to compute ground to image transformations. The elevation source can be a digital elevation model (DEM) or a constant value.
   * A tile image service can be used as the digital elevation model. The unit of constant elevation value should be in meters.
   */
  accessor elevationSource: IConstantElevation | IElevationSource | null | undefined;
  /**
   * The farthest usable distance of the imagery from the camera position. FarDistance is used to determine the extent of the image footprint, which is used to determine if an image
   * is returned when you click on the map, and for creating optional footprint features. The units are in meters. Far distance should be always greater than 0.
   *
   * @example
   * // Adds farDistance value to features without it
   * layer.farDistance = 60;
   */
  accessor farDistance: number | null | undefined;
  /**
   * An array of [FieldConfiguration](https://developers.arcgis.com/javascript/latest/references/core/layers/support/FieldConfiguration/) objects that control how fields are displayed in popups and other UI elements. Each object specifies options for an individual field, such as its display name, via `alias`, and formatting rules, via `fieldFormat`. Assign this property before the layer loads to ensure the configurations take effect. For more information, see the [FieldConfiguration](https://developers.arcgis.com/javascript/latest/references/core/layers/support/FieldConfiguration/) documentation.
   * If no field configurations are provided, they are automatically created when a [popupTemplate](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#popupTemplate) is assigned to the layer. If no [popupTemplate](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#popupTemplate) is set, you can manually define field configurations to customize how fields are shown.
   * > [!WARNING]
   * >
   * > Support is limited in version 5.1. For details, see the [FieldConfiguration](https://developers.arcgis.com/javascript/latest/references/core/layers/support/FieldConfiguration/) documentation.
   *
   * To modify field configurations, clone [fieldConfigurations](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#fieldConfigurations), make the changes, and then reassign the updated clone back to [fieldConfigurations](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#fieldConfigurations).
   *
   * @since 5.1
   * @see [OrientedImageryLayer.getFieldConfiguration](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#getFieldConfiguration)
   * @see [OrientedImageryLayer.getFieldAlias](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#getFieldAlias)
   * @example
   * ```js
   * // Adding a new field configuration
   * const addNewConfig = (layer, fieldName, alias, fieldFormat) => {
   *   // Check if there is already an existing configuration for fieldName
   *   const existingConfig = layer.getFieldConfiguration(fieldName);
   *   if (!existingConfig) {
   *     const newConfig = new FieldConfiguration({ name: fieldName, alias, fieldFormat });
   *     const newConfigs = clone(layer.fieldConfigurations);
   *     newConfigs.push(newConfig);
   *     layer.fieldConfigurations = newConfigs;
   *   }
   * };
   * ```
   * @example
   * ```js
   * // Updating an existing field configuration
   * const updateConfig = (layer, fieldName, alias, fieldFormat) => {
   *   const existingConfig = layer.getFieldConfiguration(fieldName);
   *   if (existingConfig) {
   *     const newConfig = existingConfig.clone();
   *     newConfig.alias = alias;
   *     newConfig.fieldFormat = fieldFormat;
   *
   *     const index = layer.fieldConfigurations.indexOf(existingConfig);
   *     const newConfigs = clone(layer.fieldConfigurations);
   *     newConfigs[index] = newConfig;
   *     layer.fieldConfigurations = newConfigs;
   *   }
   * };
   * ```
   * @example
   * ```js
   * // Deleting an existing field configuration
   * const deleteConfig = (layer, fieldName) => {
   *   const existingConfig = layer.getFieldConfiguration(fieldName);
   *   if (existingConfig) {
   *     const index = layer.fieldConfigurations.indexOf(existingConfig);
   *     var newConfigs = clone(layer.fieldConfigurations);
   *     newConfigs.splice(index, 1);
   *     layer.fieldConfigurations = newConfigs;
   *   }
   * };
   * ```
   * @see [FeatureLayer.getFieldConfiguration](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/#getFieldConfiguration)
   * @see [FeatureLayer.getFieldAlias](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/#getFieldAlias)
   * @example
   * ```js
   * // Adding a new field configuration
   * const addNewConfig = (layer, fieldName, alias, fieldFormat) => {
   *   // Check if there is already an existing configuration for fieldName
   *   const existingConfig = layer.getFieldConfiguration(fieldName);
   *   if (!existingConfig) {
   *     const newConfig = new FieldConfiguration({ name: fieldName, alias, fieldFormat });
   *     const newConfigs = clone(layer.fieldConfigurations);
   *     newConfigs.push(newConfig);
   *     layer.fieldConfigurations = newConfigs;
   *   }
   * };
   * ```
   * @example
   * ```js
   * // Updating an existing field configuration
   * const updateConfig = (layer, fieldName, alias, fieldFormat) => {
   *   const existingConfig = layer.getFieldConfiguration(fieldName);
   *   if (existingConfig) {
   *     const newConfig = existingConfig.clone();
   *     newConfig.alias = alias;
   *     newConfig.fieldFormat = fieldFormat;
   *
   *     const index = layer.fieldConfigurations.indexOf(existingConfig);
   *     const newConfigs = clone(layer.fieldConfigurations);
   *     newConfigs[index] = newConfig;
   *     layer.fieldConfigurations = newConfigs;
   *   }
   * };
   * ```
   * @example
   * ```js
   * // Deleting an existing field configuration
   * const deleteConfig = (layer, fieldName) => {
   *   const existingConfig = layer.getFieldConfiguration(fieldName);
   *   if (existingConfig) {
   *     const index = layer.fieldConfigurations.indexOf(existingConfig);
   *     var newConfigs = clone(layer.fieldConfigurations);
   *     newConfigs.splice(index, 1);
   *     layer.fieldConfigurations = newConfigs;
   *   }
   * };
   * ```
   */
  accessor fieldConfigurations: FieldConfiguration[] | null | undefined;
  /**
   * An array of fields in the layer. Each field represents an attribute
   * that may contain a value for each feature in the layer. For example,
   * a field named `POP_2015`, stores information about total population as a
   * numeric value for each feature; this value represents the total number
   * of people living within the geographic bounds of the feature.
   *
   * @since 4.29
   * @see [source](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#source)
   * @see [objectIdField](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#objectIdField)
   * @see [Add an array of client-side features](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#client-side)
   * @see [source](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/#source)
   * @see [objectIdField](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/#objectIdField)
   * @see [Add an array of client-side features](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/#client-side)
   * @example
   * // define each field's schema
   * const fields = [
   *  new Field({
   *    name: "ObjectID",
   *    alias: "ObjectID",
   *    type: "oid"
   *  }), new Field({
   *    name: "description",
   *    alias: "Description",
   *    type: "string"
   *  }), new Field ({
   *    name: "title",
   *    alias: "Title",
   *    type: "string"
   *  })
   * ];
   *
   * // See the sample snippet for the source and renderer properties
   * const layer = new FeatureLayer({
   *   // geometryType and spatialReference are inferred
   *   // from the input source features
   *   source: features,
   *   // Object ID field is inferred from the fields array
   *   fields: fields,
   *   renderer: renderer
   * });
   */
  get fields(): Field[];
  set fields(value: FieldProperties[]);
  /**
   * The geometry type of features in the OrientedImageryLayer. OrientedImageryLayer only supports [Point](https://developers.arcgis.com/javascript/latest/references/core/geometry/Point/) geometry type.
   *
   * @see [Add an array of client-side features](https://developers.arcgis.com/javascript/latest/references/core/layers/mixins/FeatureLayerBase/#client-side)
   */
  get geometryType(): "point";
  /**
   * The camera's scope in horizontal direction. The units are in degrees and valid values range from 0 to 360.
   *
   * @example layer.horizontalFieldOfView = 40;
   */
  accessor horizontalFieldOfView: number | null | undefined;
  /**
   * Defines the unit that will be used for all horizontal measurements. The unit will be obtained from the layer coordinate system and will be used for display purposes only.
   *
   * @example layer.horizontalMeasurementUnit = "Meter";
   */
  accessor horizontalMeasurementUnit: string | null | undefined;
  /** Prefix used to build the image url path in conjunction with the image attribute. */
  accessor imagePathPrefix: string | null | undefined;
  /** Suffix used to build the image url path in conjunction with the image attribute. */
  accessor imagePathSuffix: string | null | undefined;
  /** The orientation of the camera in degrees relative to the scene when the image was captured. The rotation is added in addition to the camera roll. The value can range from -360 to +360. */
  accessor imageRotation: number | null | undefined;
  /**
   * Indicates whether to display labels for this layer. If `true`, labels will
   * appear as defined in the [labelingInfo](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#labelingInfo) property.
   *
   * @default true
   * @since 4.29
   * @see [labelingInfo](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#labelingInfo)
   */
  accessor labelsVisible: boolean;
  /**
   * A unique id that identifies an operational layer in a map.
   *
   * @since 4.29
   * @example
   * // loads the third layer in the given Portal Item
   * const layer = new FeatureLayer({
   *   portalItem: {
   *     id: "8d26f04f31f642b6828b7023b84c2188"
   *   },
   *   layerId: 2
   * });
   * @example
   * // If not specified, the first layer (layerId: 0) will be returned
   * const layer = new FeatureLayer({
   *   portalItem: {
   *     id: "8d26f04f31f642b6828b7023b84c2188"
   *   }
   * });
   * @example
   * // Can also be used if URL points to service and not layer
   * const layer = new FeatureLayer({
   *   // Notice that the url doesn't end with /2
   *   url: "http://services.arcgis.com/V6ZHFr6zdgNZuVG0/arcgis/rest/services/MonterreyBayCanyon_WFL/FeatureServer",
   *   layerId: 2
   * });
   * @example
   * // This code returns the same layer as the previous snippet
   * const layer = new FeatureLayer({
   *   // The layer id is specified in the URL
   *   url: "http://services.arcgis.com/V6ZHFr6zdgNZuVG0/arcgis/rest/services/MonterreyBayCanyon_WFL/FeatureServer/2",
   * });
   */
  get layerId(): number;
  /**
   * Indicates whether the layer will be included in the legend.
   *
   * @default true
   * @since 4.31
   */
  accessor legendEnabled: boolean;
  /**
   * Maximum search distance to be used while querying the feature service specified in the Oriented Imagery Layer. The maximum distance can never be less than zero.
   *
   * @example layer.maximumDistance = 100;
   */
  accessor maximumDistance: number | null | undefined;
  /** The nearest usable distance of the imagery from the camera position. The units are in meters. Near distance can never be less than 0. */
  accessor nearDistance: number | null | undefined;
  /**
   * The name of an `oid` [field](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#fields) containing
   * a unique value or identifier for each feature in the layer.
   *
   * @since 4.29
   * @see [fields](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#fields)
   * @see [Add an array of client-side features](https://developers.arcgis.com/javascript/latest/references/core/layers/mixins/FeatureLayerBase/#client-side)
   * @example
   * // See the sample snippet for the source and fields properties
   * const layer = new FeatureLayer({
   *   source: features,
   *   fields: fields,
   *   objectIdField: "ObjectID",  // field name of the Object IDs
   *   geometryType: "point",
   *   renderer: <renderer>
   * });
   */
  accessor objectIdField: string;
  /**
   * Semicolon-delimited string used to store standard deviation values. The standard deviation values are in the following order and format: Camera location in XY direction;
   * camera height; camera heading; camera pitch; camera roll; near distance; far distance; elevation (all in meters).
   */
  accessor orientationAccuracy: Accuracy;
  /**
   * String that defines the imagery type used in the particular Oriented Imagery Layer. Valid values: 360, horizontal, inspection, nadir, oblique.
   *
   * @example layer.orientedImageryType = "oblique";
   */
  accessor orientedImageryType: OrientedImageryType | null | undefined;
  /**
   * An array of field names to include in the OrientedImageryLayer. If not specified, the layer will include all fields.
   *
   * @since 4.29
   * @see [FeatureLayerView.availableFields](https://developers.arcgis.com/javascript/latest/references/core/views/layers/FeatureLayerView/#availableFields)
   * @see [fieldUtils](https://developers.arcgis.com/javascript/latest/references/core/layers/support/fieldUtils/)
   * @example
   * // Includes all fields from the service in the layer
   * fl.outFields = ["*"];
   * @example
   * // Get the specified fields from the service in the layer
   * // These fields will be added to FeatureLayerView.availableFields
   * // along with rendering and labeling fields. Use these fields
   * // for client-side filtering and querying.
   * fl.outFields = ["NAME", "POP_2010", "FIPS", "AREA"];
   * @example
   * // set the outFields for the layer coming from webmap
   * webmap.when(function () {
   *   layer = webmap.layers.at(1);
   *   layer.outFields = ["*"];
   * });
   */
  accessor outFields: string[] | null | undefined;
  /**
   * Indicates whether to display popups when features in the layer are clicked. The layer needs to have a [popupTemplate](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#popupTemplate) to define what
   * information should be displayed in the popup. Alternatively, a default popup template may be automatically used if
   * [Popup.defaultPopupTemplateEnabled](https://developers.arcgis.com/javascript/latest/references/core/widgets/Popup/#defaultPopupTemplateEnabled) is set to `true`.
   *
   * @default true
   * @since 4.29
   * @see [SceneView.popup](https://developers.arcgis.com/javascript/latest/references/core/views/SceneView/#popup)
   * @see [View2D.popup](https://developers.arcgis.com/javascript/latest/references/core/views/View2D/#popup)
   * @see [createPopupTemplate()](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/#createPopupTemplate)
   * @see [SceneView.popup](https://developers.arcgis.com/javascript/latest/references/core/views/SceneView/#popup)
   * @see [View2D.popup](https://developers.arcgis.com/javascript/latest/references/core/views/View2D/#popup)
   */
  accessor popupEnabled: boolean;
  /**
   * The portal item referencing the Oriented Imagery layer item from which the OrientedImageryLayer is loaded.
   *
   * @since 4.29
   * @example
   * // While this example uses FeatureLayer, this same pattern can be
   * // used for other layers that may be loaded from portalItem ids.
   * const layer = new FeatureLayer({
   *   portalItem: {  // autocasts as new PortalItem()
   *     id: "caa9bd9da1f4487cb4989824053bb847"
   *   }  // the first layer in the service is returned
   * });
   * @example
   * // Set hostname when using an on-premise portal (default is ArcGIS Online)
   * // esriConfig.portalUrl = "http://myHostName.esri.com/arcgis";
   *
   * // While this example uses FeatureLayer, this same pattern can be
   * // used for SceneLayers.
   * const layer = new FeatureLayer({
   *   portalItem: {  // autocasts as new PortalItem()
   *     id: "8d26f04f31f642b6828b7023b84c2188"
   *   },
   *   // loads the third item in the given feature service
   *   layerId: 2
   * });
   * @example
   * // Initialize GeoJSONLayer by referencing a portalItem id pointing to geojson file.
   * const layer = new GeoJSONLayer({
   *   portalItem: new PortalItem({
   *     id: "81e769cd7031482797e1b0768f23c7e1",
   *     // optionally define the portal, of the item.
   *     // if not specified, the default portal defined is used.
   *     // see https://developers.arcgis.com/javascript/latest/references/core/config/#portalUrl
   *     portal: new Portal({
   *       url: "https://jsapi.maps.arcgis.com/"
   *     })
   *   }
   * });
   * @example
   * // This snippet loads a table hosted in ArcGIS Online.
   * const table = new FeatureLayer({
   *   portalItem: { // autocasts as esri/portal/PortalItem
   *     id: "123f4410054b43d7a0bacc1533ceb8dc"
   *   }
   * });
   *
   * // Before adding the table to the map, it must first be loaded and confirm it is the right type.
   * table.load().then(() => {
   *   if (table.isTable) {
   *     map.tables.add(table);
   *   }
   * });
   * @example
   * // While this example uses FeatureLayer, this same pattern can be
   * // used for other layers that may be loaded from portalItem ids.
   * const layer = new FeatureLayer({
   *   portalItem: { // autocasts as esri/portal/PortalItem
   *     id: "caa9bd9da1f4487cb4989824053bb847",
   *     // Set an API key to access a secure portal item configured with API key authentication.
   *     apiKey: "APIKEY"
   *   }
   * });
   */
  get portalItem(): PortalItem | null | undefined;
  set portalItem(value: PortalItemProperties | null | undefined);
  /**
   * Determines the order in which images/records in the layer will be available for sequential navigation in the viewer. You can sort features by one or multiple fields in ascending (asc) or descending (desc) order. The sequential navigation tool is enabled only when this property has a valid value. If the property is null (default), the tool remains disabled and is not available for use.
   *
   * @since 5.1
   * @example
   * // features ordered using two fields - AssetGroup and SequenceOrder
   * layer.sequenceOrderFields = [
   *  {
   *    field: "AssetGroup",
   *    order: "asc",
   *  },
   *  {
   *    field: "SequenceOrder",
   *    order: "desc",
   *  },
   * ];
   */
  accessor sequenceOrderFields: SequenceOrderFieldInfoJSON[];
  /**
   * The spatial reference of the layer. The default value is WGS84.
   * This property can be set explicitly to project the longitude and latitude
   * coordinates when the OrientedImageryLayer is loaded. While not required, explicitly setting
   * the spatial reference of the layer will improve the performance
   * when projecting to a spatial reference other than the one used by the
   * coordinates in the raw data.
   *
   * @default SpatialReference.WGS84
   * @since 4.29
   * @see [SpatialReference.WGS84](https://developers.arcgis.com/javascript/latest/references/core/geometry/SpatialReference/#WGS84)
   */
  get spatialReference(): SpatialReference;
  set spatialReference(value: SpatialReferenceProperties);
  /**
   * Defines the unit of time used in the viewer's time selector tool. Images will be filtered in the viewer based on the Time Unit value defined here.
   * Valid values: days, hours, minutes, months, weeks, years
   *
   * @default "days"
   * @example layer.verticalFieldOfView = 20;
   */
  accessor timeIntervalUnit: TimeIntervalUnit;
  /** The layer type provides a convenient way to check the type of the layer without the need to import specific layer modules. */
  get type(): "oriented-imagery";
  /**
   * The URL of the Oriented Imagery layer service.
   *
   * @since 4.29
   * @example
   * // Pass the service url to create an Oriented Imagery layer.
   *
   *   const layer = new OrientedImageryLayer({
   *     url: "url-to-oriented-imagery-service"
   *   });
   *   map.add(layer);  // adds the layer to the map
   * });
   * @example
   * // Hosted Feature Service on ArcGIS Online
   * layer.url = "http://services.arcgis.com/V6ZHFr6zdgNZuVG0/arcgis/rest/services/origins/FeatureServer/0";
   * @example
   * // Layer from Map Service on ArcGIS Server
   * layer.url = "http://sampleserver6.arcgisonline.com/arcgis/rest/services/Census/MapServer/2";
   * @example
   * // Can also be used if URL points to service and not layer
   * const layer = new FeatureLayer({
   *   // Notice that the url doesn't end with /2
   *   url: "http://services.arcgis.com/V6ZHFr6zdgNZuVG0/arcgis/rest/services/MonterreyBayCanyon_WFL/FeatureServer",
   *   layerId: 2
   * });
   * @example
   * // Non-spatial table in San Francisco incidents service.
   * const table = new FeatureLayer({
   *   url: "https://sampleserver6.arcgisonline.com/arcgis/rest/services/SF311/FeatureServer/1"
   * });
   * // table must be loaded so it can be used in the app.
   * table.load().then(function() {
   *   // table is loaded. ready to be queried.
   * });
   */
  accessor url: string | null | undefined;
  /**
   * The camera's scope in the vertical direction. The units are in degrees and valid values range from 0 to 180.
   *
   * @example layer.verticalFieldOfView = 20;
   */
  accessor verticalFieldOfView: number | null | undefined;
  /**
   * Defines the primary unit to be used for all vertical measurements. Valid values: feet, meter.
   *
   * @example layer.verticalMeasurementUnit = "meter";
   */
  accessor verticalMeasurementUnit: "feet" | "meter" | null | undefined;
  /** Prefix used to build the video url path in conjunction with the image attribute. */
  accessor videoPathPrefix: string | null | undefined;
  /** Suffix used to build the video url path in conjunction with the image attribute. */
  accessor videoPathSuffix: string | null | undefined;
  /**
   * Creates default field configuration objects that define the display properties of fields.
   * These field configurations are generated from the layer's [fields](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#fields) property.
   *
   * @returns An array of field configuration objects, or `null` if the layer does not have any fields that require formatting.
   * @since 5.1
   * @see [OrientedImageryLayer.fieldConfigurations](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#fieldConfigurations)
   * @example
   * ```js
   * const fieldConfigs = layer.createFieldConfigurations();
   * layer.fieldConfigurations = fieldConfigs;
   * ```
   * @see [FeatureLayer.fieldConfigurations](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/#fieldConfigurations)
   * @example
   * ```js
   * const fieldConfigs = layer.createFieldConfigurations();
   * layer.fieldConfigurations = fieldConfigs;
   * ```
   */
  createFieldConfigurations(): FieldConfiguration[] | null | undefined;
  /**
   * Returns the alias of the specified field. Previously, this value was commonly retrieved from [Layer.popupTemplate.fieldInfos[x].label](https://developers.arcgis.com/javascript/latest/references/core/popup/FieldInfo/#label) or [Layer.fields[x].alias](https://developers.arcgis.com/javascript/latest/references/core/layers/support/Field/#alias). While these properties are still supported, this method is now the recommended approach for obtaining a field's alias.
   *
   * The alias is resolved in the following order:
   *
   * 1. [Field configuration alias](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#fieldConfigurations)
   * 2. [Field alias](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#fields) defined on the field.
   *
   * @param fieldName - The name of the field for which to get the alias.
   * @returns The alias of the specified field.
   * @since 5.1
   * @see [OrientedImageryLayer.fieldConfigurations](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#fieldConfigurations)
   * @see [OrientedImageryLayer.getFieldConfiguration](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#getFieldConfiguration)
   * @see [FeatureLayer.fieldConfigurations](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/#fieldConfigurations)
   * @see [FeatureLayer.getFieldConfiguration](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/#getFieldConfiguration)
   */
  getFieldAlias(fieldName: string): string | null | undefined;
  /**
   * Returns the [FieldConfigurations](https://developers.arcgis.com/javascript/latest/references/core/layers/support/FieldConfiguration/) for the specified field. The field configuration provides optional formatting and display information for the field.
   *
   * Previously, field formatting was commonly retrieved from [Layer.popupTemplate.fieldInfos[x].format](https://developers.arcgis.com/javascript/latest/references/core/popup/FieldInfo/#format). This is no longer needed, instead use [Layer.getFieldConfiguration(fieldname).fieldFormat](https://developers.arcgis.com/javascript/latest/references/core/layers/support/FieldConfiguration/#fieldFormat) to retrieve the field configuration formatting.
   *
   * @param fieldName - The name of the field for which to get the configuration.
   * @returns The field configuration of the specified field.
   * @since 5.1
   * @see [OrientedImageryLayer.fieldConfigurations](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#fieldConfigurations)
   * @see [OrientedImageryLayer.getFieldAlias](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#getFieldAlias)
   * @see [FeatureLayer.fieldConfigurations](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/#fieldConfigurations)
   * @see [FeatureLayer.getFieldAlias](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/#getFieldAlias)
   */
  getFieldConfiguration(fieldName: string): FieldConfiguration | null | undefined;
  /**
   * Executes a [Query](https://developers.arcgis.com/javascript/latest/references/core/rest/support/Query/) against the feature service or layer data and
   * returns the number of features that satisfy the query. If no parameters are specified,
   * the total number of features satisfying the layer's configuration/filters is returned.
   *
   *
   * > [!WARNING]
   * > - To query the count of features available in the view on the client-side rather than making a server-side query, use
   * > [OrientedImageryLayerView.queryFeatureCount()](https://developers.arcgis.com/javascript/latest/references/core/views/layers/OrientedImageryLayerView/#queryFeatureCount).
   * 
   *
   * @param query - Specifies the attributes and spatial filter of the query. If no parameters are specified, the total number of features satisfying the layer's configuration/filters is returned.
   * @param options - Additional request options. See [RequestOptions](https://developers.arcgis.com/javascript/latest/references/core/request/types/#RequestOptions).
   * @returns When resolved, returns the number of features that satisfy the query.
   * @since 4.30
   * @see [Sample - Query features from a FeatureLayer](https://developers.arcgis.com/javascript/latest/sample-code/featurelayer-query/)
   * @example
   * ```js
   * // Queries for the count of all features matching the layer's configurations
   * // e.g. definitionExpression
   * layer.queryFeatureCount().then((numFeatures) => {
   *   // prints the total count to the console
   *   console.log(numFeatures);
   * });
   * ```
   * @example
   * ```js
   * const query = new Query();
   * query.where = "region = 'Southern California'";
   *
   * const featureCount = await layer.queryFeatureCount(query);
   * console.log(featureCount);  // prints the number of results satisfying the query
   * ```
   * @see [Sample - Query features from a FeatureLayer](https://developers.arcgis.com/javascript/latest/sample-code/featurelayer-query/)
   * @example
   * ```js
   * // Queries for the count of all features matching the layer's configurations
   * // e.g. definitionExpression
   * layer.queryFeatureCount().then((numFeatures) => {
   *   // prints the total count to the console
   *   console.log(numFeatures);
   * });
   * ```
   * @example
   * ```js
   * const query = new Query();
   * query.where = "region = 'Southern California'";
   *
   * const featureCount = await layer.queryFeatureCount(query);
   * console.log(featureCount);  // prints the number of results satisfying the query
   * ```
   */
  queryFeatureCount(query?: QueryProperties | null | undefined, options?: RequestOptions): Promise<number>;
  /**
   * Executes a [Query](https://developers.arcgis.com/javascript/latest/references/core/rest/support/Query/) against a feature service or layer data and returns a
   * [FeatureSet](https://developers.arcgis.com/javascript/latest/references/core/rest/support/FeatureSet/) once the promise resolves. A FeatureSet contains an array of [features](https://developers.arcgis.com/javascript/latest/references/core/Graphic/).
   *
   * > [!WARNING]
   * >
   * > **Notes**
   * > - To query features visible in the view on the client-side rather than making a server-side query, use
   * > [OrientedImageryLayerView.queryFeatures()](https://developers.arcgis.com/javascript/latest/references/core/views/layers/OrientedImageryLayerView/#queryFeatures).
   * > - When querying a feature service with z-values and no [vertical coordinate system](https://doc.esri.com/en/arcgis-pro/latest/help/mapping/properties/vertical-coordinate-systems.html)
   * > information, z-values are automatically converted to match [Query.outSpatialReference](https://developers.arcgis.com/javascript/latest/references/core/rest/support/Query/#outSpatialReference) units.
   * > Example: The service has a horizontal spatial reference using `feet` units and the query is made with `outSpatialReference`
   * > based on `meter` units, then [FeatureLayer.queryFeatures()](https://developers.arcgis.com/javascript/latest/references/core/layers/FeatureLayer/#queryFeatures) automatically converts the values from `feet` to `meter` units.
   * 
   *
   * @param query - Specifies the attributes and spatial filter of the query. If no parameters are specified, all features satisfying the layer's configuration/filters are returned.
   * @param options - Additional request options. See [RequestOptions](https://developers.arcgis.com/javascript/latest/references/core/request/types/#RequestOptions).
   * @returns When resolved, returns a [FeatureSet](https://developers.arcgis.com/javascript/latest/references/core/rest/support/FeatureSet/) containing an array of graphic features.
   * @since 4.29
   * @see [Samples using queryFeatures() method](https://developers.arcgis.com/javascript/latest/sample-code/?tagged=queryfeatures)
   * @see [Query and filter guide](https://developers.arcgis.com/javascript/latest/query-filter/)
   * @example
   * ```js
   * // Queries for all the features matching the layer's configurations
   * // e.g. definitionExpression
   * const results = await layer.queryFeatures();
   * // prints the array of result graphics to the console
   * console.log(results.features);
   * ```
   * @example
   * ```js
   * // create a query object to filter features by setting desired properties
   * const query = new Query();
   * query.where = "STATE_NAME = 'Washington'";
   * query.outSpatialReference = { wkid: 102100 };
   * query.returnGeometry = true;
   * query.outFields = [ "CITY_NAME" ];
   *
   * const results = await layer.queryFeatures(query);
   * console.log(results.features);  // prints the array of features to the console
   * ```
   * @example
   * ```js
   * // Get a query object for the layer's current configuration
   * const queryParams = layer.createQuery();
   * // set a geometry for filtering features by a region of interest
   * queryParams.geometry = extentForRegionOfInterest;
   * // Add to the layer's current definitionExpression
   * queryParams.where = queryParams.where + " AND TYPE = 'Extreme'";
   *
   * // query the layer with the modified params object
   * const results = await layer.queryFeatures(queryParams);
   * // prints the array of result graphics to the console
   * console.log(results.features);
   * ```
   * @example
   * ```js
   * // query all features from the layer and only return
   * // attributes specified in outFields.
   * const query = { // autocasts as Query
   *   where: "1=1", // select all features
   *   returnGeometry: false,
   *   outFields: ["State_Name", "City_Name", "pop2010"]
   * };
   *
   * const results = await layer.queryFeatures(query);
   * console.log(results.features);  // prints the array of features to the console
   * ```
   * @see [Samples using queryFeatures() method](https://developers.arcgis.com/javascript/latest/sample-code/?tagged=queryfeatures)
   * @see [Query and filter guide](https://developers.arcgis.com/javascript/latest/query-filter/)
   * @example
   * ```js
   * // Queries for all the features matching the layer's configurations
   * // e.g. definitionExpression
   * const results = await layer.queryFeatures();
   * // prints the array of result graphics to the console
   * console.log(results.features);
   * ```
   * @example
   * ```js
   * // create a query object to filter features by setting desired properties
   * const query = new Query();
   * query.where = "STATE_NAME = 'Washington'";
   * query.outSpatialReference = { wkid: 102100 };
   * query.returnGeometry = true;
   * query.outFields = [ "CITY_NAME" ];
   *
   * const results = await layer.queryFeatures(query);
   * console.log(results.features);  // prints the array of features to the console
   * ```
   * @example
   * ```js
   * // Get a query object for the layer's current configuration
   * const queryParams = layer.createQuery();
   * // set a geometry for filtering features by a region of interest
   * queryParams.geometry = extentForRegionOfInterest;
   * // Add to the layer's current definitionExpression
   * queryParams.where = queryParams.where + " AND TYPE = 'Extreme'";
   *
   * // query the layer with the modified params object
   * const results = await layer.queryFeatures(queryParams);
   * // prints the array of result graphics to the console
   * console.log(results.features);
   * ```
   * @example
   * ```js
   * // query all features from the layer and only return
   * // attributes specified in outFields.
   * const query = { // autocasts as Query
   *   where: "1=1", // select all features
   *   returnGeometry: false,
   *   outFields: ["State_Name", "City_Name", "pop2010"]
   * };
   *
   * const results = await layer.queryFeatures(query);
   * console.log(results.features);  // prints the array of features to the console
   * ```
   */
  queryFeatures(query?: QueryProperties | null | undefined, options?: RequestOptions): Promise<FeatureSet>;
  /**
   * Executes a [Query](https://developers.arcgis.com/javascript/latest/references/core/rest/support/Query/) against the feature service and returns an
   * array of Object IDs for features that satisfy the input query. If no parameters are specified,
   * then the Object IDs of all features satisfying the layer's configuration/filters are returned.
   *
   *
   * > [!WARNING]
   * > - To query the ObjectIDs of features visible in the view on the client-side rather than making a server-side query, use
   * > [OrientedImageryLayerView.queryObjectIds()](https://developers.arcgis.com/javascript/latest/references/core/views/layers/OrientedImageryLayerView/#queryObjectIds).
   * 
   *
   * @param query - Specifies the attributes and spatial filter of the query. If no parameters are specified, then the Object IDs of all features satisfying the layer's configuration/filters are returned.
   * @param options - Additional request options. See [RequestOptions](https://developers.arcgis.com/javascript/latest/references/core/request/types/#RequestOptions).
   * @returns When resolved, returns an array of numbers representing the object IDs of the features satisfying the query.
   * @since 4.30
   * @see [Sample - Query Related Features](https://developers.arcgis.com/javascript/latest/sample-code/query-related-features/)
   * @example
   * ```js
   * // Queries for all the Object IDs of features matching the layer's configurations
   * // e.g. definitionExpression
   * layer.queryObjectIds().then((results) => {
   *   // prints the array of Object IDs to the console
   *   console.log(results);
   * });
   * ```
   * @example
   * ```js
   * const query = new Query({
   *   where: "SUB_REGION = 'Pacific'"
   * });
   *
   * const ids = await layer.queryObjectIds(query);
   * console.log(ids); // an array of object IDs
   * ```
   * @see [Sample - Query Related Features](https://developers.arcgis.com/javascript/latest/sample-code/query-related-features/)
   * @example
   * ```js
   * // Queries for all the Object IDs of features matching the layer's configurations
   * // e.g. definitionExpression
   * layer.queryObjectIds().then((results) => {
   *   // prints the array of Object IDs to the console
   *   console.log(results);
   * });
   * ```
   * @example
   * ```js
   * const query = new Query({
   *   where: "SUB_REGION = 'Pacific'"
   * });
   *
   * const ids = await layer.queryObjectIds(query);
   * console.log(ids); // an array of object IDs
   * ```
   */
  queryObjectIds(query?: QueryProperties | null | undefined, options?: RequestOptions): Promise<ObjectId[]>;
  /**
   * Saves the layer to its existing portal item in the [Portal](https://developers.arcgis.com/javascript/latest/references/core/portal/Portal/)
   * authenticated within the user's current session. If the layer is not saved to a
   * [PortalItem](https://developers.arcgis.com/javascript/latest/references/core/portal/PortalItem/), then you should use [saveAs()](https://developers.arcgis.com/javascript/latest/references/core/layers/OrientedImageryLayer/#saveAs).
   *
   * @param options - Various options for saving the layer.
   * @returns When resolved, returns the portal item to which the layer is saved.
   * @since 4.31
   * @example const portalItem = await layer.save();
   * @example const portalItem = await layer.save();
   */
  save(options?: LayerSaveOptions): Promise<PortalItem>;
  /**
   * Saves the layer to a new portal item in the [Portal](https://developers.arcgis.com/javascript/latest/references/core/portal/Portal/)
   * authenticated within the user's current session.
   *
   * @param portalItem - The portal item to which the layer will be saved.
   * @param options - Various options for saving the layer.
   * @returns When resolved, returns the portal item to which the layer is saved.
   * @since 4.31
   * @example
   * const portalItem = new PortalItem();
   * await layer.saveAs(portalItem);
   * @example
   * const portalItem = new PortalItem();
   * await layer.saveAs(portalItem);
   */
  saveAs(portalItem: PortalItemProperties, options?: LayerSaveAsOptions): Promise<PortalItem>;
}